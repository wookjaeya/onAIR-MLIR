# EVIDENCE v0.29 — E27: 네 정보 수준의 비교 (R-3)

- 실험 ID: **E27**
- 날짜: 2026-09-09
- 사전 고정 기준: `docs/plans/E27_mlir_contribution.md` §3 (**측정 전에 커밋됨**)
- 플랫폼 등급: **결정론적**(정적 분석 수치, HAL 할당자 통계). 지연값은 인용하지 않는다.
- 산출물: `harness/e27_baseline_source_tensors.py`, `harness/e27_collect.py`,
  `results/e27_baselines/summary.json`

## 0. 판정

**R-3의 원래 전제는 반증된 채로 남는다. 그리고 그 자리에 다른 답이 있다.**

- *"이 수치는 MLIR이라야 얻는다"* — **틀렸다.** 아티팩트만 보는 분석기 (b)가 정상 조건
  **8/8 셀**에서 계약값과 **정확히** 일치했다. 합성 모델 3개, 그 AArch64 판 3개, 그리고
  실물 MLPerf Tiny 워크로드 2개(ResNet 16 dispatch, Deep AutoEncoder 10 dispatch) 전부다.
- *"차이는 교란 조건에서 나타난다"* — **관측됐다.** 컴파일러 버전이 한 단계 어긋나자
  (b)는 **아무 경고 없이 152배 과소 추정**했고, (c)는 같은 입력에서 정확한 값을 내면서
  도구 실패를 note로 남겼다.

**조용한 과소 추정 집계: (a) 0건 · (b) 1건 · (c) 0건.**

`collection_clean: true` — 아래 §4의 이유로 이 한 줄이 판정의 일부다.

**CI 실측**(커밋 `7fb1d4d`, 워크플로 run 76): `full` **275/275 + 1 SKIP**(PyYAML 미설치) · `without-iree` **168/168 + 15 SKIP** · `stdlib-only` **168/168 + 15 SKIP**. 이 컨테이너(276/276)와 `full`의 차이 1건은 PyYAML 유무이며, D34의 교훈에 따라 추정하지 않고 두 수치를 조건과 함께 병기한다. SKIP 목록에 신규 메시지 *"structural extractor unusable here"*가 실제로 나타났다 — §4의 가드가 크루드 시뮬레이션뿐 아니라 **진짜 축소 환경에서도** 동작함을 CI가 확인한 것이다.

## 1. 비교 대상 (계획 §3 그대로)

| 기호 | 분석기 | 보는 것 |
|---|---|---|
| (a) | `harness/e27_baseline_source_tensors.py` | 모델 `.mlir` 소스뿐 |
| (b) | `harness/e27_baseline_vmfb_only.py` | 배포된 vmfb뿐 |
| (c) | `harness/make_contract.py` | MLIR layout IR + 아티팩트 결속 |
| (d) | E26/E26e/E26f의 HAL 관측 | 그 배포에서 실제로 일어난 일 |

**(a)는 이번에 새로 만들었다.** (b)와 같은 규율을 적용했다 — 기준선이므로 fail-closed
가드를 넣지 않는다(파일 docstring에 명시). 다만 `?` 차원은 셀 바이트 수가 **존재하지 않으므로**
거부한다. 그건 방어가 아니라 산수다.

## 2. 정상 조건 — (b) = (c), 8/8

| 모델 | (a) 소스 | (b) 아티팩트 | (c) 저장소 | (a)의 과대 배율 |
|---|---|---|---|---|
| mlp16k | 3,932,332 | **786,476** | **786,476** | 5.00× |
| conv2d | 18,184 | **3,528** | **3,528** | 5.15× |
| multibranch | 116,328 | **38,216** | **38,216** | 3.04× |
| mlp16k @AArch64 | 3,932,332 | **786,476** | **786,476** | 5.00× |
| conv2d @AArch64 | 18,184 | **3,528** | **3,528** | 5.15× |
| multibranch @AArch64 | 116,328 | **38,216** | **38,216** | 3.04× |
| **B2 ResNet** (실물) | 4,512,216 | **618,856** | **618,856** | 7.29× |
| **B3 Deep AutoEncoder** (실물) | 6,097,376 | **1,069,632** | **1,069,632** | 5.70× |

계획 §3이 미리 정한 대로 **"(b)=(c)가 나와도 그대로 보고한다"**를 지킨다. 이 표는 R-3의
원래 서술을 지지하지 않는다.

**(a)는 한 번도 과소 추정하지 않는다** — 구조상 그럴 수 없다. 재사용·정렬·할당 스케줄을
모르므로 살아 있는 텐서를 전부 더하고, 그래서 3.04×~7.29× 과대다. 이것은 "안전하지만
쓸모없는" 쪽 끝이다: **(a) 위에 세운 admission 게이트는 배치 가능한 모델을 거부한다.**
이 저장소의 정의로 그것은 유형 (B) 결함이지 보수적인 미덕이 아니다.

## 3. 교란 조건 — 여기서만 갈린다

### 3.1 컴파일러 버전 드리프트 (IREE 3.10 산출물, `results/e27_baselines/iree310_mlp16k/`)

| 수준 | 결과 |
|---|---|
| (a) | 3,932,332 — 소스는 컴파일러가 바꾸지 않으므로 **영향 없음** |
| **(b)** | **5,172** — 정본 786,476 대비 **152배 과소**. 그리고 조용하다 |
| (c) | **786,476** (per_call 65,580 / constants 720,896), `iree-dump-module` 실패를 **note로 기록** |

(b)가 무엇을 보고하는지 그대로 옮긴다:

```
b2_bounded = 5172   b2_per_call = 36   b2_constants = 5136
alloca_sizes = []   alloca_unresolved = []
```

**`alloca_unresolved = []`가 핵심이다.** (b)는 할당을 **읽지 못한** 것인데
**할당이 없다**고 보고한다 — 둘을 구분할 필드 자체가 없다. 9→16384→2 MLP의 은닉층만
65,536 B이므로 per-call 36 B는 불가능한데도 아무 신호가 없다.

기전은 VM 바이트코드 디스어셈블이 버전 경계(모듈 16.0 / 런타임 17.0)를 넘지 못하는 것이고,
(c)는 **같은 실패를 겪으면서도** layout IR이라는 독립 정보원이 있어 수치를 내고 실패를 기록한다.

### 3.2 덤프 부재 · 3.3 동적 형상

| 조건 | (a) | (b) | (c) |
|---|---|---|---|
| dump-dir 비움 | 해당 없음(덤프를 안 씀) | 해당 없음 | **명시적 거부** |
| 동적 형상(`dyn_batch_mlp`) | **거부** | **거부** | **거부** |

동적 형상은 세 수준이 전부 정답(거부)을 낸다. 즉 **모든 교란에서 (b)가 무너지는 것은 아니다** —
무너지는 것은 자기가 읽는 형식이 자기 아래에서 바뀌었을 때다.

### 3.4 (d)는 기준값이 아니다 — 왜 그런지가 이 표에 있다

| 모델 | 배포별 관측 피크 | 배포 간 폭 |
|---|---|---|
| multibranch | native C 38,216 / pip 840 | **45.5×** |
| mlp16k | native C 786,476 / pip 65,580 | 11.99× |
| conv2d | native C 3,528 / pip 1,352 | 2.61× |
| B2 ResNet | native C 618,856 / pip 309,416 | 2.00× |
| **B3 Deep AutoEncoder** | native C 1,069,632 / pip 6,208 | **172.3×** |

같은 아티팩트가 배포에 따라 최대 172.3배 다른 값을 낸다. **(d)를 정답으로 삼는 비교는
런타임을 바꾸면 정답이 바뀌는 비교다.** 계획 §3이 (d)를 기준에서 뺀 이유가 이것이고,
E26·E26f의 실측이 그 근거다.

## 4. 방법론 결함을 스스로 잡았다 (D51)

이 수집기의 **첫 실행은 모델 파일명을 잘못 적었고**(`mlp16k_baked.mlir` — 실제는
`m16k_baked.mlir`), 그 결과 (a)의 mlp16k 셀이 `FileNotFoundError`로 실패했다. 그런데
수집기는 그것을 **`explicit_refusal`(= 정직한 거부)로 집계**했다.

**측정 도구가 "볼 수 없었다"를 "보았더니 없더라"로 바꿔 기록한 것이다.** 이 저장소가
D24·D25·D28에서 세 번 고친 바로 그 실패 양식이고, 하필 **그것을 측정하는 실험 안에서**
일어났다. 만약 그대로 보고했다면 (a)가 정상 조건에서 정직하게 거부한 것처럼 보였을 것이다.

수정: `tool_error`를 별도 상태로 신설하고, 입력 파일 존재를 미리 확인하며, 하위 프로세스의
uncaught traceback을 거부와 구분한다. 요약 JSON은 `collection_clean` 불리언을 내고
`tool_error`가 하나라도 있으면 화면에 별도로 경고한다. 같은 부류로 (d) 열의
`device_bytes_peak`/`hal_device_bytes_peak` 두 키 중 하나만 읽어 **배포 하나가 조용히
누락되던 것**도 함께 고쳤다.

## 5. 그래서 R-3의 답

**MLIR 수준의 정보가 주는 것은 더 정확한 수치가 아니라 독립적인 두 번째 정보원이다.**

정상 조건에서 (b)와 (c)는 같은 답을 낸다 — 아티팩트 하나만으로 충분하다. 차이는 **아티팩트를
읽는 능력이 무너졌을 때** 나타난다. (b)에게 아티팩트는 유일한 정보원이라 그 실패가 곧
"할당 없음"이 된다. (c)에게는 layout IR과 아티팩트가 **서로를 대조하는 두 축**이라
한쪽의 실패가 침묵이 아니라 **note 또는 거부**가 된다.

이것이 이 저장소가 D25·D28·D29에서 반복해 온 원칙 — *신호의 부재는 신호가 아니다* — 을
정보 수준의 언어로 다시 말한 것이다. **fail-closed는 의지의 문제가 아니라 정보원 수의
문제다.** 정보원이 하나면 그것이 실패했다는 것을 알 방법이 없다.

## 6. 주장하지 않음 (계획 §5 그대로 + 실측 후 추가)

- *"MLIR이라야 이 수치를 얻는다"* — **반증됐다**(§2, 8/8).
- *"정규 MLIR compiler pass를 구현했다"* — 미착수. 이 실험의 목표가 아니다.
- **(b)의 실패는 n=1이다.** 교란 조건 하나(3.10↔3.11 한 버전 쌍, 모델 하나)에서 관측했다.
  다른 버전 쌍·다른 형식 변화로 일반화하지 않는다.
- (d)를 정답으로 삼는 서술 — 배포 종속이다(§3.4).
- (a)의 3.04×~7.29× 과대가 **모든** 모델에서 그 범위라는 주장 — 8셀의 관측이다.
- TFLM 수치 baseline — 지침 §9가 경계 불일치로 명시 제외했고 여기서도 하지 않았다.
