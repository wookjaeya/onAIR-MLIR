# EXPERIMENT LOG — onair-mlir-bench

규칙
- 실험 ID는 불변이다. 재실행은 새 ID(예: E5b)를 받는다.
- 각 항목은 `platform_grade`를 반드시 기록한다. FUNCTIONAL_ONLY 결과는 방향성 관측이며 논문 증거가 아니다.
- 판정 변경은 여기서 하지 않는다. 판정은 `docs/EVIDENCE_v*.md`에서 버전으로 관리하고, 여기에는 어느 문서가 판정했는지만 링크한다.
- 모든 항목은 git 커밋 해시로 산출물에 연결된다 (`git log --oneline`).

| ID | 날짜 | 실험 | 플랫폼 grade | 산출물 | 결과 요약 | 판정 문서 | 커밋 |
|---|---|---|---|---|---|---|---|
| **E25** | 2026-09-09 | OnAIR↔cFS 동일 모델·의미 동치 — 하나의 canonical 가중치에서 baked MLIR+weights.npz를 함께 생성하고 다섯 경로(NumPy reference / IREE Python 바인딩 / native C / cFS x86-64 / cFS AArch64 게스트)의 출력을 사전 고정 기준으로 대조 | 결정론적(출력 비트값·해시·계약 수치) | `harness/gen_model_canonical.py`·`harness/e25_compare.py`(신규), `native/native_learner.c`·`native/cfs_app/fsw/src/ai_learner.c`(게이트 뒤 파일 기반 동치 모드), `results/e25_equivalence/` | **PASS**. reference 대비 네 경로 합계 512/512 원소(경로당 128 = 입력 64 × 출력 2), argmax 경로당 64/64 (v0.22.1 정정: 이전 서술 "256/256"은 argmax 집계와 섞인 오기). **IREE 경로 6쌍 전부 비트 동일**(x86-64 셋은 같은 vmfb라 기대된 결과, **AArch64는 다른 vmfb인데도 비트 동일 — 계획이 예상하지 않은 결과이며 일반화하지 않음**). AArch64 반복 실행 비트 동일(결정적). 계약 값은 두 ISA 동일(786476/65580/720896/16), vmfb만 다름. **출발점**: 이전까지 OnAIR(`mlp_9x65536x2`, 외부 npz)와 cFS(`mlp16k`, baked)는 서로 다른 모델이었고 같은 것은 인터페이스뿐. **사전 고정 기준의 가치 실증**: telemetry regime은 abs 단독 1/64, normalized는 rel 단독 59/64 — 어느 한 기준만 요구했어도 정직한 결과가 FAIL. E14 cross-target 지표의 `out0_agreement` 자리를 **canonical 모델에 한해** 실제 출력 대조로 채움 (v0.22.1 정정: 이전의 "`both_sound` 갭 해소"는 오귀속 — `both_sound`는 메모리 관측 종합값이고 E25는 메모리를 계측하지 않았다, D45). 환경 실패 2건(게스트 emergency mode)은 §7로 분리 기록 | `docs/EVIDENCE_v0.22_E25.md` | (이 커밋) |
| **E25b** | 2026-09-09 | 일곱 번째 외부 검토(REVIEW_v0_22_E25) 반영 — E25 주장 범위 5건 정정 + 판정 도구를 사전 고정 기준(계획 §3.2)에 맞춤 | 결정론적(판정·수치·바이트 비교) | `harness/e25_compare.py`(쌍별 규칙: same_vmfb=비트 동일 / cross_vmfb=tolerance+argmax, `--vmfb` 필수·누락 시 거부), `harness/contract_negative_tests.py::e25_compare_rule_cases()` 8건, `results/e25_equivalence/build/comparison_all.pairrule.json`(신규, 원본 보존) | **확인·수정**. 검토 지적 8건 전부 저장소 대조로 사실 확인. **과잉 거부(유형 B) 직접 재현**: 한 원소 1 ulp 차(모든 원소가 abs·rel 기준을 각각 단독 16/16 만족)에서 옛 도구 `VERDICT: FAIL rc=1` → 새 도구 PASS. 보관 출력 재판정 판정·`vs_reference`·`argmax` 전부 불변, 규칙 배정 same 3/cross 3. 이 컨테이너 `--skip-regression` **156/156**. 정정 5건: 512 원소 · 누산 순서 철회 · `both_sound` 오귀속(D45) · "OnAIR-IREE" 명칭 · 계약 ISA 동일성 범위 | `docs/EVIDENCE_v0.22_E25.md` §11·§12 | (이 커밋) |
| **E26** | 2026-09-09 | 부분 메모리 계약 경계의 유용성 — 사전 고정 기준(`docs/plans/E26_boundary_utility.md`, 측정 전 커밋 `c8e8980`)으로 Q1 soundness · Q2 보수성 · Q3 admission 유용성 측정 | 결정론적(HAL allocator 통계·계약 수치·admission 판정). 지연값 미인용, RSS는 버킷 보고용 | `harness/e26_collect.py`(신규), `results/e26_boundary_utility/`(x86_64 native·cFS·pip_runtime, aarch64 native, comparison, summary.json) | **Q1 PASS**(21개 실행 셀 전부 `peak ≤ bounded`, 위반 0) · **Q3 PASS**(unsafe admit 0, `B−1`→DENY·`B`/`B+1`→ADMIT) · **Q2 정량화**(tightness 1.00×~45.50×). **기전 규명**: IREE가 상수를 `stream.resource.try_map`+`scf.if(%did_map)`로 감싸므로 `bounded`는 두 분기의 최댓값이고 soundness는 구조적으로 성립, tightness만 분기의 함수다. 같은 vmfb의 peak가 **런타임 배포에 따라 최대 45.5배** 달라짐을 네 배포에서 실측 — 런타임 계측 상한은 배포 종속, **정적 계약은 배포 독립**. 분기 결정 요인 4가지 실측 배제, 최종 요인은 미확정. **E14의 `both_sound: null`을 실제 실행으로 채움**(3모델 true, D45가 철회했던 항목) | `docs/EVIDENCE_v0.25_E26.md` | (이 커밋) |
| **E44** | 2026-09-11 | **예산 출처(budget provenance)** — 로드맵 §6.2가 요구한 세 필드(`budget_source`·`budget_scope`·`reservation_semantics`)를 착수 전 실측으로 **하나로 좁혔다**. 사전 고정 기준은 `docs/plans/E44_budget_provenance.md`(커밋 `2aa93cf`, 측정 이전). `budget_source`는 **이미 있었고**(`ai_learner.c` 8회 + 시험 5곳이 pin), `budget_scope`는 **이미 세 곳**(`resources.scope`·`accounting_rules.excluded`·admission `per_app_local_budget`)에 있어 **네 번째 이름은 D65의 형태**라 만들지 않았다. 남은 하나는 계약 필드로 신설하는 대신 **E39a 비교표의 A5를 저장소에서 유도**하도록 바꿨다 | 결정론적(라벨·스캔 계수·표 diff) | `harness/budget_provenance.py`(신규) · `harness/mk_e44_summary.py`(신규) · `harness/mk_prior_art_table.py`(A5 유도) · `native/native_learner.c`(`argv` 라벨) · `plugins/compiled_learner/compiled_learner_plugin.py`(`deployment_config`/`none`) · `results/e44_budget_provenance/` · `docs/EVIDENCE_v0.48_E44.md` | **Q1~Q5 PASS**. 네 행이 전부 자기 예산의 출처를 말한다 — OnAIR `deployment_config`/`none`(실행) · native `argv`(재빌드 후 실행) · cFS `override`(E38 보관 로그 인용). `none`은 `null`이 아니라 *"선언 자체가 없다"*를 말하는 값이고 `budget_source_note`가 그 문장을 함께 싣는다. A5는 이제 **계산된다** — 예약 능력이 있는 호출 8종을 소스 85파일에서 세어 **0건 → `declared`**, hit가 있으면 **`enforced`로 올리지 않고 `unknown`**(호출이 있다는 것과 그 호출이 *이* 예산을 예약한다는 것은 다르다 — E28/D52와 같은 구분). 스캐너가 **소스만** 읽는 이유가 이 실험 중 실제로 나왔다: 감사가 *"`CFE_TBL` 0회"*라 했는데 grep은 15건이었고 **전부 cFS 부팅 로그**였다. 기존 값 개명 0 · 시험 pin 5곳 무수정 통과 · 비교표 diff **1 insertion / 1 deletion**.  **690/690 → 710/710** | 신규 D 없음. **생성기 실수 2건을 EVIDENCE에 기록**(§2.1 첫 glob이 한 단계 깊어 *"기록 없음"*을 냈으나 기록은 있었다 — **D51 계열** · §2.2 넓힌 glob이 정렬 순서상 먼저인 **D61(b) 결함 재현 로그**를 정상 셀처럼 인용했고 `detail`과 `cell`이 한 행 안에서 어긋났다 — **D65의 기계 판독끼리 판본**), 그리고 §2.3 재빌드가 덮은 `native/contract_gen.h`를 되돌림. 신규 가드 4건 전부 revert 시 실패 확인 | `docs/EVIDENCE_v0.48_E44.md` |
| **E42/E43** | 2026-09-11 | **순수 OnAIR 기준선 O0~O3** — *"기존 OnAIR 실행 경로에 본 연구가 추가하는 기능은 무엇인가"*를 분리한다(로드맵 §7.1). 사전 고정 기준은 `docs/plans/E42_E43_pure_onair_baseline.md`(커밋 `766ae2a`, 측정 이전). **로드맵을 좁혀 채택** — §7.4-3의 *"37개 fixture"*가 틀렸고(실측 **5행·추론 5회**), **O2·O3는 이미 측정돼 있어 인용**했다(재측정해 "새 셀 4개"라 적으면 D72가 고친 중복 증거 부풀리기) | `plugins/litert_learner/`(신규) · `harness/mk_e43_summary.py`(신규) · `harness/onair_integration_check.py`(D76) · `configs/deployments/onair_deployments.json`(O0·O1) · `results/e43_pure_onair/` · `docs/EVIDENCE_v0.47_E42_E43.md` | **Q1~Q5 PASS**. 네 경로 전부 공식 로더 구성·코어 무수정인데 **O3만 런타임을 만들지 않고 추론 0**이다 — 그것이 제안 경로가 더하는 것이다. O0 추론 5회이고 출력이 E31 TFLite oracle과 **비트 동일**(최악 abs 0.000e+00), O1은 **코드 0줄**(`budget_bytes` 생략 → `NOT_EVALUATED`). 인용 셀의 한계를 표에 함께 싣는다(O2의 nanobind 누수/D60 해제 미검증, O3의 런타임 생성 신호 부재). **메모리를 아예 재지 않았다**(로드맵 §7.5 — 회계 범위가 다르고, 재고 나서 비교하지 말라는 것은 성립하지 않는다) | D76 | `docs/EVIDENCE_v0.47_E42_E43.md` |
| **E46** | 2026-09-11 | **OPS-SAT WGAN denoiser 반입 — 네 번째 실물 공개 모델**. 사전 고정 기준은 `docs/plans/E46_wgan_import.md`(커밋 `e37a2fe`, 측정 이전). 두 문서가 미루고 제외했던 모델인데 **이연 사유가 측정으로 거짓**이었고(*"대형 출력 문제를 연다"* — `_p` 출력 37,632 B는 cFS 앱이 이미 `static`으로 처리하는 602,112 B의 16분의 1), **제외 조건(*"새 모델이 시험할 구체적인 분석 한계"*)은 D75가 충족시켰다**. 검토서가 이름 댄 `_p` 대신 **`_f`를 채택** — 공개 실입력이 224×224라 정확히 맞고(resize 0회), `_f`라야 D75를 밟는다 | `harness/tflite2onnx_ext_elementwise.py`(신규) · `harness/iree_runner.py`(`--output-layout`) · `harness/e32_native_aarch64.py`(유형 B 수정) · `native/cfs_app/fsw/src/ai_learner.c`(D75) · `results/e46_wgan/` · `docs/EVIDENCE_v0.46_E46.md` | **Q1·Q2·Q4·Q5 PASS**. 계약 `bounded` **135,666,432** = `per_call` **131,382,784** + `constants` **4,283,648**, 오버라이드 0, 한 번의 `iree-compile`. **저장소 사상 최대이고 구조가 정반대**(호출:상수 **30.7** vs DeepAE 0.006) — 같은 계약 형식이 양극단을 담는다. 의미 동치 실 noised 9장 + 경계 2 = **1,655,808원소 실패 0**(argmax 해당 없음), **OR 규칙이 네 번째 연속 판정을 좌우**(최악 `rel` 6.936e-04 = `rel_tol`의 69배, `abs` 다리만 통과). **alpha = 0.2**를 flatbuffer에서 읽었다 — ONNX 기본값 0.01로 떨어졌으면 11개 활성화가 음수 쪽에서 조용히 틀렸다. **출력이 레이아웃을 갖는 최초 모델**(D62 세 번째 얼굴) | D75 | `docs/EVIDENCE_v0.46_E46.md` |
| **E45** | 2026-09-11 | **세 모델의 합성 입력을 실데이터로 교체 (G1만)**. 사전 고정 기준은 `docs/plans/E45_real_inputs.md`(커밋 `c27b8b2`, 측정 이전). E26a가 *"조달 불가"*라고 적은 것은 그때도 지금도 참이지만(`cs.toronto.edu` 000 · `zenodo.org` 000) **다른 통로**(`git clone` · `raw.githubusercontent.com`)가 있었고 세 모델의 실입력이 전부 그 뒤에 있었다. 정본성은 미러끼리의 합치가 아니라 **torchvision이 차단된 정본 tarball에서 계산해 기록한 파일 단위 md5**와의 일치로 세웠다 | `harness/fetch_real_inputs.py`(신규, 바이트 게이트 · SKIP≠FAIL) · `harness/mk_e45_summary.py`(신규) · `harness/model_fixture.py`(`--tensors` · `--layout-defect` 결함 주입기) · `harness/e31_compare.py`(`per_kind`, 실제 계산한 headroom) · `results/e45_real_inputs/` · `docs/EVIDENCE_v0.45_E45.md` | **b2 ResNet PASS**(실 CIFAR 200장, 2,000원소 실패 0, argmax 200/200, 열 클래스 전부 — E34는 `{0,4}`뿐) · **SmartCam PASS**(pristine 썸네일 19장, 57원소 실패 0) · **b3 DeepAE FAIL**(실 log-mel 34창, 21,760원소 중 **94 실패**; 기준은 고치지 않았다). **Q3가 핵심**: 같은 레이아웃 결함에서 argmax 탐지율 **합성 0/34(0%) vs 실입력 180/200(90%)·18/19(95%)**. 부수로 **E31 보관 음성 대조를 저장소 내용만으로 재생성**(판정·105/111·argmax 3·worst_abs 0.84248263 동일). **623/623 → 650/650**(CI 실측 `3bc7efe` run 180: `full` 645/645+3 SKIP · 축소 두 레그 488/488+29 SKIP) | D74 | `docs/EVIDENCE_v0.45_E45.md` |
| **E41** | 2026-09-11 | **분석 영역 다섯 조건의 음성 시험 — 무엇이 게이트이고 무엇이 아닌가**(로드맵 §5.3-4). 사전 고정 기준은 `docs/plans/E40_E41_analysis_domain.md` §3(커밋 `9b7d5ba`, 측정 이전). **다섯 중 둘은 일부러 게이트로 만들지 않았다** — 조건 3은 출하 배포 어디서도 도달 불가(시험하려면 비행 앱에 동시성을 심어야 한다), 조건 4는 문자 그대로 강제하면 **예산의 50.0%를 쓰는 정직한 실행을 거부**한다 | `harness/e41_domain_probe.py`(셀마다 별도 프로세스 — HAL 통계가 프로세스 전역) · `harness/mk_e41_summary.py` · `harness/gen_contract_header.py`(driver 기본값 fail-open 제거) · `plugins/compiled_learner/artifact_binding.py`(`check_declared_driver`, 순수 함수) · `configs/`(배포별 `telemetry` 선언 + 템플릿 치환) · `results/e41_analysis_domain/` · `docs/EVIDENCE_v0.44_E41.md` | **R1·R3·R4·R5·R6 PASS**. 전제는 load-bearing: N=1 피크가 세 모델 모두 `per_call`과 정확히 일치하고, SmartCam은 **동시 호출 2개만으로 bounded 초과**(18,764,184 > 18,222,796). D50과 분리(N=1에서 보유/해제 피크 동일). 신설 게이트는 조건 5뿐이고 공식 OnAIR 경로에서 **admission·binding보다 먼저** 발화(`smartcam_wrong_driver`: active=false·추론 0·둘 다 null). E33 4셀 전부 보관값과 동일(과잉 거부 0). **598/598 → 617/617** | D71 | `docs/EVIDENCE_v0.44_E41.md` |
| **E40** | 2026-09-11 | **정적 상한의 분석 영역과 회계 규칙을 계약이 스스로 말하게 한다**(아홉 번째 로드맵 `ONAIR_MLIR_ADDITIONAL_RESEARCH_AND_BASELINE_PLAN.md` §5). 사전 고정 기준은 `docs/plans/E40_E41_analysis_domain.md`(커밋 `9b7d5ba`, 구현 이전). 로드맵 처방 3건 중 **둘이 새 결함을 심는다**는 것을 착수 전 조사에서 실측하고 좁혀 채택했다 | `harness/make_contract.py`(assumptions 유도 · `analysis_domain`(derived/required_premises 분리) · `accounting_rules` · `analysis_domain_drift()` 거부 가드) · `harness/static_mem_bound.py`(`SUPPORTED_RESOURCE_OPS` 공개) · `harness/contract_negative_tests.py`(SUBTREE 제외 + 전용 pin) · 보관 `contract.dynamic.{x86_64,aarch64}.json` 4 leaf 정정 · `docs/EVIDENCE_v0.43_E40.md` | **Q1~Q4 전부 PASS**. 계약 수치 diff 0 · 헤더 14/14 바이트 불변 · `dynamic`의 `derived.static_shapes=false`(이전 서술은 `static shapes`) · 계약 19개 중 **0개**가 `try_map` 분기·64바이트 정렬 전제를 언급하지 않던 공백을 `constant_policy`로 채움. revert 시 생성기가 **rc=1로 작성 자체를 거부**. **570/570 → 598/598**(FAIL 0 · SKIP 0) | D70 | `docs/EVIDENCE_v0.43_E40.md` |
| **E38** | 2026-09-10 | **조건부 opt-in 설정을 판정과 독립적으로 기록한다**(여덟 번째 외부 검토 `DECISIONS_v0_41_INTEGRATED_REVIEW.md` §4.1·§10-2). 검토가 정한 절차대로 기존 기록을 먼저 조사했고 **독립 기록이 없음을 확정**(D69) — 빌드 로그에 `CONDITIONAL` 0건, 두 트리의 `app_knobs` 완전 동일, 보관 CMakeLists는 미전개 변수 — 그래서 그 두 셀만 재실행했다 | `harness/optin_witness.py`(산출물에서 opt-in을 읽는 fail-closed witness, 양성 대조 선행) · `harness/mk_e38_summary.py` · `native/cfs_app/fsw/src/ai_learner.c`(`build_config` stage) · `native/native_learner.c`(requested vs applied) · `scripts/51_build_cfs_aarch64.sh`(knob·`-D` 목록·witness `--expect`) · `results/e38_optin_record/`(보관 E36 바이너리 2개 + 재실행 2셀 + 트리 기록) · `docs/EVIDENCE_v0.42_E38.md` | **Q1~Q5 전부 PASS**. 재실행 두 셀이 각자 89행에서 `allow_conditional_map`을 판정(91·92행)보다 **먼저** 기록. 소급 증거: 보관 두 바이너리가 `AI_LEARNER_Init`에서 **정확히 12개 명령** 차이이고 추가분이 `per_call − 1`(9,382,091)과의 비교다. E36 결정론적 값 전부 불변(peak 9,382,092 = 승인 근거 예산). native에서 **requested=true·applied=false**(이전엔 흔적 0)를 실측. 부수로 D61(b)와 **같은 결함이 `scripts/50_wire_cfs_ai_learner.sh`에도** 남아 있어(D62 계열) 같은 방식으로 고치고 세 번 빌드해 실측(`=1` 다음의 미설정 빌드가 산출물에서 **0을 증언**) | D69 | `docs/EVIDENCE_v0.42_E38.md` |
| **E37** | 2026-09-10 | **연구 최종 주장 고정 · 증거 연결 · 재현 확인 · 등급 분리**(열한 번째 외부 문서 `RESEARCH_COMPLETION_ACTIONS.md`의 필수 작업 A–D). 문서의 주장 8건을 원자료로 대조 — **정정 요구 0건**(이 저장소가 받은 검토 중 처음). 같은 대조가 저장소 결함 6건을 드러냈고 그중 **3건은 이 세션이 만든 새 도구의 결함**이다 | `harness/mk_evidence_linkage.py`(3모델 × 7항목, 원자료 참조 121건) · `harness/e37_reproduce_check.py`(최종 코드 재판정 12셀) · `harness/mk_e36b_summary.py`(E36b 요약을 로그에서 유도) · `results/evidence_linkage/{linkage.json,linkage.md,reproduce_check.json}` · `docs/EVIDENCE_v0.41_E37.md` | **연결표 21/21 present**, **재판정 12/12 동일**(판정·totals·worst 전부), 중심 주장을 E14 시점 문장에서 v0.41 문장으로 교체하고 7개 구분 확정, 네 축(모델의 실제성 / 가중치 / **입력의 실제성** / 검증한 성질) 분리. 게스트 재실행이 실제로 필요한 셀은 **1개**로 좁혀졌다 | D63(연결표 자신의 부모 객체 resolve) · D64(E36b 요약이 손조립) · D65(정정이 산문에만) · D66(원장 수치 오기) · D67(계약 키 오독) · D68(파싱 실패를 0으로) | `docs/EVIDENCE_v0.41_E37.md` |
| **E36b** | 2026-09-10 | **ResNet·DeepAE의 AArch64 확장**(§10 단계 4의 작업 항목 중 E34가 범위 밖으로 선언한 절반; E36 계획서 §3.3이 **측정 전에** 셀을 고정): 두 공개 모델이 같은 fixture·계약·AArch64 cFS 절차로 완료되는가 | 결정론적(계약 수치·출력값·판정·HAL 피크) | `results/e36b_aarch64_models/`(모델당 단일 호출 산출물·계약·헤더·native/cFS 전체 출력·게스트 raw log 6건·summary.json), `harness/e32_native_aarch64.py`·`e32_cfs_outputs.py` 일반화(D62), `harness/contract_negative_tests.py::e36b_aarch64_models_cases`(15건) | **Q3 전부 PASS → `stage_4_complete: true`**. 계약 세 수치가 **x86-64와 정확히 동일**(ResNet 618,856 = 309,416 + 309,440 · DeepAE 1,069,632 = 6,208 + 1,063,424), **오버라이드 0**, 다른 것은 커널 스택뿐(1,232 vs 439 · 16 vs 32). 의미: **native(qemu-user)와 cFS 게스트 양쪽에서** ResNet 340원소(argmax 34/34, 최악 abs 1.937e-06)·DeepAE 21,760원소(최악 abs 3.815e-05) **실패 0**, 기준은 E25 이래 무변경 승계, 기준값은 E34 보존 원본 `.tflite`. 예산: `B`→ADMIT(피크 = `per_call` 정확히)·`B−1`→NOT_ADMITTED(E36의 런타임 오버라이드 재사용, 새 배선 0). **피크가 bounded가 아니라 per_call** — E29b의 정렬 수정으로 배포가 map 분기를 잡고 있으며 E26f의 x86-64 copy 분기 측정과 대비된다. **D62**: 기존 두 하네스가 SmartCam 출력 arity를 리터럴 3으로 들고 있어 `e32_native_aarch64.py`가 **10출력 ResNet을 앞 3개로 조용히 절단**(fail-open), `e32_cfs_outputs.py`는 나눗셈 검사로 **거부**(fail-closed) — 같은 하드코딩의 두 방향. 잘린 출력의 비교기 판정이 **340/340 실패 + worst_abs 0.0**이라는 자기모순이었고 **계산은 처음부터 맞았다**. 둘 다 계약에서 arity를 읽도록 일반화(E34가 `model_fixture.py`·`e31_compare.py`에 한 것과 같은 방식). **단계 4의 재사용 기준에 대한 정직한 답: 새 하네스 0개, 일반화 2개.** **하지 않음**: 정확도(합성 전용 등급 유지)·OnAIR·지연·조건부 계층. 이 컨테이너 **494/494 → 508/508**(신규 검사 **14건** — 커밋 메시지의 "15건"은 세어 보지 않고 적은 값이라 정정한다). **CI 실측**(커밋 `b6c778f`, run 150, 3레그 success): `full` **507/507 + 1 SKIP**(PyYAML) · `without-iree` **367/367 + 26 SKIP** · `stdlib-only` **367/367 + 26 SKIP** — 컨테이너 508/508과 `full`의 차이 1건은 PyYAML 유무(D34). 축소 레그가 353→367로 **정확히 +14**라 E36b 신규 14건이 **전부 나타난다**(신규 SKIP 0): 보관 `summary.json` 판독과 소스 텍스트 검사라 툴체인 없이 실제로 돈다. | `docs/EVIDENCE_v0.40_E36b.md` | `b6c778f` |
| **E36** | 2026-09-10 | **AArch64 cFS 증거 완성**(아홉 번째 검토 §10 단계 2의 미충족 완료 기준, 열 번째 검토가 원자료로 지적한 `stage_2_complete: false`): 실제 비행 모델의 실행 전 예산 판정(승인·거부)과 계약 대상 메모리 준수를 AArch64 cFS에서 끝까지 보일 수 있는가 | 결정론적(판정·예산·HAL 피크·모드) | `docs/plans/E36_aarch64_cfs_completion.md`(**측정 전 커밋** `f3ac80c`), `native/cfs_app/fsw/src/ai_learner.c`(런타임 예산 오버라이드, D61a), `scripts/51_build_cfs_aarch64.sh`(opt-in 명시 전달·컴파일 도달 검증, D61b), `harness/mk_e36_summary.py`(신설), `results/e36_aarch64_cfs/`(7셀 게스트 raw log + 결함 재현 로그 2묶음 + summary.json) | **Q1·Q1b·Q2·Q4 전부 PASS → `stage_2_complete: true`**. **Q1**: E32에서 **빌드 자체가 불가능**했던 cFS 예산 미달 셀을 런타임 오버라이드로 열어 실행 — 예산 18,222,795 → NOT_ADMITTED·추론 0회·`CFE_ES_ExitApp` 후 **같은 cFS가 앱 8개를 계속 로드**(단계 2 완료 기준의 *"비승인 상태에서도 cFS의 다른 기능이 계속 동작"*을 로그로 충족). **Q1b**: 파싱 실패·0은 `BUDGET_INVALID`로 초기화 거부(조용한 매크로 폴백 금지). **Q2**: 조건부 계층이 **실물 8.9 MB 모델·AArch64 cFS**에서 완주 — 승인 예산 9,382,092(=`per_call`, `bounded`의 **1.94× 감소**), `module_ptr_mod64:0`·`hal_peak_after_append:0`·`arm:map`(E29b 전제조건을 **측정으로** 확인), 피크 **정확히 9,382,092**, `peak_within_admitted_budget:true`; **대조군**(같은 예산·opt-in 없음)은 NOT_ADMITTED라 통과의 원인이 느슨한 예산이 아님을 셀로 확인. **Q4**: 오버라이드 미설정이 E32 `cfs_B_plus_1`과 결정론적 값 전부 동일. **D61(이 변경이 심고 계측이 잡은 결함 2건)**: (a) 일괄 치환이 resolver의 매크로 읽기를 **자기 대입**으로 바꿔 예산 0 → 전부 NOT_ADMITTED(**정직해 보이는 거부**), `budget_source`가 드러냄 → 매크로 경로도 `<=0`이면 명시 거부; (b) 빌드 스크립트의 `${VAR:+-D}`가 **공유 CMake 캐시 잔류값**을 물려받아 무조건 트리가 조건부가 되고 **거부 셀이 ADMIT_CONDITIONAL_MAP**(유형 A) → 기본 0 명시 전달 + 컴파일 도달 검증, 조건부-먼저 순서로 재빌드해 확인. **정정**: 계획서 §3.4의 "추론 70회 재현"은 부정확(횟수는 timeout의 함수). **하지 않음**: **Q3(ResNet·DeepAE AArch64)은 미실행 — E36b로 이연, 단계 4는 잔여 셀 있음**·정확도·지연·OnAIR AArch64/SBN·계약 재생성 0회. 시험 11건, revert 시 2건 FAIL. 이 컨테이너 **483/483 → 494/494**. **CI 실측**(커밋 `78083d0`, run 146, 3레그 success): `full` **493/493 + 1 SKIP**(PyYAML) · `without-iree` **353/353 + 26 SKIP** · `stdlib-only` **353/353 + 26 SKIP** — 컨테이너 494/494와 `full`의 차이 1건은 PyYAML 유무(D34). 축소 레그가 342→353으로 **정확히 +11**이라 E36 신규 11건이 **전부 나타난다**(신규 SKIP 0): 게스트 raw log에서 만든 `summary.json` 판독과 소스 텍스트 검사라 툴체인 없이 실제로 돈다. | `docs/EVIDENCE_v0.39_E36.md` | `78083d0` |
| **E35** | 2026-09-10 | **단계 5 — 공정한 기준선과 논문 주장 확정**(아홉 번째 외부 검토 §10 단계 5, **§8.3 최대 신규성 위험**): 같은 VMFB·같은 계약 범위·**같은 조건부 지식**을 주면 MLIR 수준이 artifact-only 수준보다 무엇을 더 결정하는가 | 결정론적(판정 비교, 실행·측정 없음) | `docs/plans/E35_fair_baseline.md`(**측정 전 커밋** `3bc617f`, 결과별 서술까지 미리 고정), `harness/e35_baseline_policy_matrix.py`(신설), `results/e35_fair_baseline/summary.json` | **결론은 MLIR 우위가 아니라 주장 범위의 축소다.** 4모델(SmartCam x86-64/AArch64·ResNet·DeepAE) × 2정책 × 3예산구간 = **24셀 전부 판정 동일, 불일치 0**. 세 수치 **4/4 일치**, 그리고 **차이가 날 것으로 본 커널 스택마저 4/4 일치** — 임베디드 ELF가 vmfb 안에 있어 같은 분석기가 dump 없이 도는 구조적 이유다(*"이건 MLIR이라야 얻는다"*가 E27의 세 수치에 이어 **스택에서도 반증**). **공정성을 셀로 확인**: `band_between`에서 artifact-only 기준선도 조건부로 승인하며 **승인 근거 예산까지 동일**(기준선을 무조건 정책에만 묶어 두고 얻은 차이는 실행 정책의 차이지 정보 표현의 차이가 아니다). 같은 정책 코드(`admission_policy.py`) 하나를 두 수준에 적용했고 예산 구간은 모델과 무관한 규칙(`B`·`P`·`P−1`)으로 고정했다(§9.2 — 모델마다 유리한 예산을 고르지 않기 위해). 재컴파일 0회·모델 실행 0회. **유일한 비대칭은 one-invocation 결속인데 이점이 아니다** — 아티팩트가 하나뿐인 수준에서는 어긋날 것이 없어 질문 자체가 성립하지 않으므로 **여러 아티팩트를 쓰는 방식의 비용**이다. **부수(방법론)**: 수집기 첫 판이 스택을 0/4 불일치로 냈는데 계약 필드 이름을 잘못 읽은 **도구 결함**이었다 — 발견이 아니라 버그였고, 차이가 나오면 먼저 자기 도구를 의심해야 한다는 사례다. 계획서 §6이 **결과별 서술을 측정 전에** 정해 뒀으므로 그대로 채택: **"MLIR 기반 계약 추출·연계 방법"**(검토서 §8.3 권고 표현). CLAUDE.md 가드레일에 반증 4건을 추가했다. **하지 않음**: 정규 MLIR pass 신설(§8.3이 금지)·기준선의 인위적 약화·실행 측정·새 모델/타깃·일반화(이 4모델·이 컴파일러 버전·이 두 도구 구현의 관측이다). 이 컨테이너 **469/469 → 477/477**. **CI 실측**(커밋 `84648ea`, run 136, 3레그 success): `full` **476/476 + 1 SKIP**(PyYAML) · `without-iree` **336/336 + 26 SKIP** · `stdlib-only` **336/336 + 26 SKIP** — 컨테이너 477/477과 `full`의 차이 1건은 PyYAML 유무(D34). 축소 레그가 328→336으로 **정확히 +8**이라 E35 신규 8건이 **전부 나타난다**(신규 SKIP 0): 보관 `summary.json` 판독뿐이라 툴체인 없이 실제로 돈다. | `docs/EVIDENCE_v0.38_E35.md` | `84648ea` |
| **E34** | 2026-09-10 | **단계 4 — 두 공개 임베디드 모델로 확장**(아홉 번째 외부 검토 §10 단계 4): E31~E33이 SmartCam 하나로 세운 절차가 모델을 바꿔도 그대로 도는가 — **시험받는 것은 모델이 아니라 도구다** | 결정론적(출력값·해시·판정) | `docs/plans/E34_two_public_models.md`(**측정 전 커밋** `34b5a86`), `results/e34_two_models/`(두 원본 `.tflite` 보존 + fixture·oracle·iree·판정·음성 대조), `harness/model_fixture.py`·`e31_compare.py` 일반화, `harness/mk_e34_summary.py` 신설 | **Q1·Q1b·Q2·Q3·Q4 전부 PASS — 단계 4 완료**. **B2 ResNet** 34샘플 × 10출력 = **340원소 전부 통과**(argmax 34/34, 최악 abs 1.967e-06) · **B3 DeepAE** 34샘플 × 640출력 = **21,760원소 전부 통과**(최악 abs 4.387e-05). 기준은 E25→E31→E32→E33에서 무변경 승계, 기준값은 보존한 원본 `.tflite`(LiteRT). **새 모델별 하네스 0개·모델별 분기 0줄·재컴파일 0회** — `tflite_oracle.py`·`iree_runner.py`는 무변경이었고 `model_fixture.py`(`--layout {nhwc_to_nchw,none}`)와 `e31_compare.py`(`--argmax`, `--detail`)만 **값으로 받는 방식**으로 일반화했다. **argmax 관측이 이번의 실질**: 계획서가 예고한 과잉 거부는 **일어나지 않았고**(argmax를 요구해도 DeepAE는 34/34로 통과) 그 사실을 그대로 적었다 — 대신 **DeepAE의 argmax가 모든 입력에서 index 261 하나로 상수**임이 관측돼 (ResNet은 {0,4}로 변한다) 그 일치가 **변별력 0**임이 드러났고, 검토서 §9.3의 지시가 지시가 아니라 **측정된 사실**이 됐다. **음성 대조**(전치 대신 reshape): FAIL **258/340 원소**인데 **argmax는 34/34(100%) 통과** — E31이 SmartCam에서 잰 92%보다 강하다 — 그리고 원소 기준이 못 잡은 것은 정확히 **상수 경계 입력 2개**로 E31의 한계가 그대로 재현됐다. **원본 보존(D43)**: 지금까지 provenance만 있어 재조달해야 했던 두 `.tflite`를 in-tree로 넣고 기록된 해시와 일치 확인. **회귀**: 일반화한 comparator로 E31 보관 실행을 재판정해 판정·totals·worst 전부 동일. **의미 검증 등급을 낮춰 기록**: 두 모델은 **합성 전용**(CIFAR-10·ToyADMOS 조달 불가)이라 SmartCam(실이미지 3 포함)보다 약한 등급이고 **정확도는 주장하지 않는다** — 검토서 §10 단계 4의 "실제 데이터가 없는 셀은 의미 검증 미완료로 남긴다"를 따랐다. **하지 않음**: 정확도·실데이터 의미 검증·이 두 모델의 AArch64/cFS/OnAIR 실행(미실행)·지연·재컴파일·단계 5. 이 컨테이너 **453/453 → 469/469**. **CI 실측**(커밋 `867c384`, run 134, 3레그 success): `full` **468/468 + 1 SKIP**(PyYAML) · `without-iree` **328/328 + 26 SKIP** · `stdlib-only` **328/328 + 26 SKIP** — 컨테이너 469/469와 `full`의 차이 1건은 PyYAML 유무(D34). 축소 레그가 312→328로 **정확히 +16**이라 E34 신규 16건이 **전부 나타난다**(신규 SKIP 0): 보관 JSON 판독과 comparator 서브프로세스 단위 시험이라 툴체인 없이 실제로 돈다. | `docs/EVIDENCE_v0.37_E34.md` | `867c384` |
| **E33** | 2026-09-10 | **단계 3 — 공식 OnAIR 경로**(아홉 번째 외부 검토 §10 단계 3, §6.1): NASA OnAIR의 **공식 로더**가 이 저장소 플러그인을 구성해 실물 SmartCam을 실행하는가, 그리고 거부 경로에서 OnAIR가 살아남는가 | 결정론적(출력값·해시·판정·로더 경로) | `docs/plans/E33_official_onair_path.md`(**측정 전 커밋** `3f8aa01`), `plugins/compiled_learner/compiled_learner_plugin.py`(재작성), `harness/admission_policy.py`·`onair_integration_check.py`·`e33_onair_outputs.py`·`mk_e33_summary.py`(신설), `configs/onair_compiled_learner.ini`·`configs/deployments/onair_deployments.json`·`configs/onair_data/`(신설), `results/e33_onair_official/`(셀 4개 원자료·전체 출력·판정) | **Q1·Q2·Q3·Q4 전부 PASS — 단계 3 완료**. **E25가 v0.22.1에서 정정한 갭이 닫혔다**(그때 "OnAIR-IREE"라 부른 것은 `iree.runtime` 직접 호출이었고 공식 로더를 통과한 실행은 없었다). 하네스는 플러그인을 import하지 않고 `python driver.py <ini>`를 서브프로세스로 돌린다 — stub이 아니라는 근거. 네 셀 전부 NASA 로더가 구성했고 **OnAIR 코어 추적 파일 변경 0**(실행 전후 `git status --untracked-files=no`로 확인). **P-admit** 5추론·판정 PASS(15원소 0실패, argmax 5/5, 최악 abs 5.364e-07, 기준값은 원본 TFLite oracle, 기준은 E25→E31→E32에서 무변경 승계) · **P-deny**(bounded−1) 비활성·추론 0·rc 0 · **P-mismatch**(x86-64 계약 + AArch64 vmfb) **파일을 읽기도 전에** 크기 선검사로 거부·추론 0·rc 0 · **P-legacy** 기존 MLP가 telemetry 모드로 그대로 동작(구식 계약이라 admission은 `NOT_EVALUATED` — 없는 경계를 지어내지 않는다, 유형 B 회귀 0). **NASA 원본을 읽고서야 알 수 있었던 제약 셋**: 로더가 `(construct_name, headers)` 두 인자만 전달 → `ONAIR_MLIR_DEPLOYMENT_CONFIG`로 배포 규약 선택; `AIPlugin`이 headers 비어있지 않음을 요구하는데 이미지를 150,528 필드로 펼 수는 없음 → 입력 모드 둘(telemetry / file_replay); **CSV 파서가 모든 필드를 float화하고 실패 시 0.0으로 대체** → 문자열 샘플 ID였다면 다섯 프레임이 전부 0번 샘플을 추론하면서 "5샘플 재생"으로 보였을 것이므로 프레임은 숫자 인덱스를 싣는다. **실측 발견**: OnAIR이 데이터 소진 후 `update()` 없이 `render_reasoning()`을 한 번 더 불러 5프레임에 **6결과**(6번째는 바이트 동일 반복)가 났다 — 검증 하네스가 새 샘플로 셀 수 있는 조건이라 입력 신선도를 추적해 추론하지 않고 `stale`로 되돌리게 하고, 변환기도 같은 샘플 중복 채점을 거부한다(두 층). **부수(방법론)**: 첫 구현이 계획서 §4-3("OnAIR 프로세스가 예외로 죽지 않는다")을 스스로 어겼다 — 계약 로딩이 예외 가드 밖에 있어 파일명 하나로 OnAIR 실행 전체가 rc=1로 죽었다(실측 후 수정). **하지 않음**: SBN·cFS 연계(단독 CSV OnAIR 실행을 cFS 연계라 부르지 않는다)·정확도·지연 성능 주장·AArch64 OnAIR·다른 모델(단계 4)·공정한 기준선(단계 5)·메모리 실측(D50 조건이 그대로 적용). 이 컨테이너 **422/422 → 453/453**. **CI 실측**(커밋 `8186a72`, run 128, 3레그 success): `full` **452/452 + 1 SKIP**(PyYAML) · `without-iree` **312/312 + 26 SKIP** · `stdlib-only` **312/312 + 26 SKIP** — 컨테이너 453/453과 `full`의 차이 1건은 PyYAML 유무(D34). 축소 레그가 281→312로 **정확히 +31**이라 E33 신규 31건이 **전부 나타난다**(신규 SKIP 0): 보관 JSON 판독과 `admission_policy` 순수 단위 시험이라 툴체인 없이 실제로 돈다. | `docs/EVIDENCE_v0.36_E33.md` | `8186a72` |
| **E32** | 2026-09-10 | **단계 2 — SmartCam AArch64 cFS 실행**(아홉 번째 외부 검토 §10 단계 2): E31이 x86-64에서 확인한 원본 의미 보존이 다른 ISA로 컴파일된 다른 아티팩트와 cFS라는 다른 실행 수명주기에서도 유지되는가, 그리고 예산 판정이 계약이 지목한 할당과 일치하는가 | 결정론적(출력값·해시·판정·HAL 통계) | `docs/plans/E32_smartcam_aarch64_cfs.md`(**측정 전 커밋** `3c7295d`), `results/e32_smartcam_aarch64/`(한 번의 iree-compile 호출 산출물·계약·헤더·native 37샘플 전체 출력·cFS 5샘플 전체 출력·게스트 raw log·예산 경계), `harness/e32_native_aarch64.py`·`e32_cfs_outputs.py` 신설, `harness/elf_stack_frame.py`(D58)·`native/native_learner.c`·`native/cfs_app/fsw/src/ai_learner.c`(D59) 수정 | **Q1 PASS · Q2 PASS · Q3 조건부 계층 미달 · Q4 PASS**. 계약 세 수치가 x86-64와 **정확히 동일**(bounded 18,222,796 = per_call 9,382,092 + constants 8,840,704, dispatch 56, 오버라이드 0); 다른 것은 아티팩트와 커널 스택(1,919 B, 호출 0 vs x86-64 439 B, 호출 38)뿐이다. 의미: **S-native 37샘플 111원소 전부 통과**(argmax 37/37, 최악 abs 1.252e-06), **S-cfs 5샘플 15원소 전부 통과**(계획서가 측정 전에 고정한 셀 정의 — 실이미지 3 + 경계 2), 두 AArch64 경로 출력은 5샘플 비트 동일(일반화 안 함). 예산: `B−1`→NOT_ADMITTED(추론 0)·`B`/`B+1`→ADMIT. **D58**(과잉 거부): AArch64 과정렬 프레임의 `sub sp, x29, #K` 복귀를 인식 못 해 정직한 모델의 헤더가 거부됐다 — 도구가 올바른 상한(1919)을 계산해 놓고 쓰지 못하게 막고 있었고, x86-64의 같은 관용구는 E14부터 처리하고 있었다. **D59**(fail-open): 조건부 승인 계층에서 사후 검사가 승인 근거(`per_call`)가 아니라 `bounded`와 비교해, 승인 예산의 **106.4%**로 실행하고도 `peak_within_bounded: true`를 기록했다. 초과분은 정확히 입력 텐서 하나(602,112 B) — `per_call`의 io 항은 **동시에 살아 있는 입력을 하나로 센다**. cFS 앱은 상주 버퍼를 재사용해 피크가 `per_call`과 정확히 같았으므로, 초과는 재생 하네스 호출 패턴의 성질이다. **cFS의 `B−1` 셀은 빌드 불가**(예산이 컴파일 타임 상수라 죽은 코드 제거 → 빌드 자신의 검증이 거부; 계획서 §3.2의 "런타임 예산" 서술이 틀렸음을 실측으로 확인). **하지 않음**: 정확도·지연·공식 OnAIR 경로·다른 모델·실물 보드. 이 컨테이너 **404/404 → 411/411**. **CI 실측**(커밋 `b5d3d0e`, run 126, 3레그 success): `full` **421/421 + 1 SKIP**(PyYAML) · `without-iree` **281/281 + 26 SKIP** · `stdlib-only` **281/281 + 26 SKIP** — 컨테이너 422/422와 `full`의 차이 1건은 PyYAML 유무(D34). 축소 레그가 251→281로 **정확히 +30**이라 E32 신규 30건이 **전부 나타난다**: 합성 objdump 단위 시험과 보관 JSON 판독이라 툴체인 없이 실제로 돈다(신규 SKIP 0). | `docs/EVIDENCE_v0.35_E32.md` | `b5d3d0e` |
| **E31** | 2026-09-10 | **P2 — SmartCam 원본 의미 보존**(아홉 번째 외부 검토 `docs/reviews/ONAIR_MLIR_ARCHITECTURE_PLAN_20260910.md` §10 단계 1이 "가장 먼저 착수할 구현"으로 지목): 원본 `.tflite`를 LiteRT로 그대로 실행한 oracle과 E30의 vmfb를 **같은 전처리 결과**로 대조 | 결정론적(출력값·해시·판정) | `docs/plans/E31_smartcam_semantic_equivalence.md`(**측정 전 커밋** `8f7bcde`), `harness/model_fixture.py`·`tflite_oracle.py`·`iree_runner.py`·`e31_compare.py`(검토 §6.3의 신설 제안 4종), `results/e31_smartcam_equivalence/`(fixture manifest·실입력/경계 `.npy`·두 경로 전체 출력·판정·음성 대조), `harness/contract_negative_tests.py::e31_semantic_equivalence_cases`(14건, 라이브 재실행 포함) | **PASS**. 37입력 × 3출력 = **111원소 전부**가 사전 고정 기준(E25에서 **변경 없이 승계**: 원소별 `abs<=1e-4` **OR** `rel<=1e-5`)을 만족, **argmax 37/37** 일치, 최악 abs **5.364e-07**. 전처리는 원본 `config.ini`의 값(224×224, mean 0, std 255 → `pixel/255`)이고 모델 내부 `MUL(2.0)`·`SUB(1.0)`이 [−1,1]을 만들어 **이중 정규화 없음**을 확인. 레이아웃은 **전치**(생성기가 왕복·순열 실효성 자기검사). **두 가지가 실측으로 드러났다**: (1) **사전 고정한 OR 규칙이 판정을 좌우했다** — 실제 이미지의 최악 `rel_err` **1.573e-05**가 `rel_tol` 1e-5를 **넘고** `abs_err`만이 그 원소를 통과시켰다(rel 단독 기준이었으면 정직한 결과가 FAIL). (2) **음성 대조**(NHWC를 전치가 아니라 reshape) → **FAIL, 105/111 원소 실패**인데 **argmax만 봤다면 34/37(92%)이 통과**였고, 잘못된 레이아웃에서 통과한 샘플은 **상수 경계 입력 2개뿐**(상수 배열은 어떤 순열에도 불변 → **경계 입력만으로는 레이아웃 오류를 잡을 수 없다**, 비상수 실데이터가 필요). **부수(방법론)**: fixture 생성기의 첫 자기검사가 *샘플 값*으로 transpose≠reshape를 판정해 전부 0인 경계 입력에서 **즉시 과잉 거부** — D57을 배운 직후 같은 계열의 실수라 `arange` 라벨로 형상만 판정하도록 고쳤다. **하지 않음**: 정확도(예제 3장은 평가셋 아님)·비행 파이프라인 동치(resize는 fixture의 것)·AArch64/cFS/OnAIR(단계 2·3)·다른 모델(단계 4)·메모리 측정. 이 컨테이너 **378/378 → 392/392**. **CI 실측**(커밋 `0f25ad4`, run 122, 3레그 success): `full` **391/391 + 1 SKIP**(PyYAML) · `without-iree` **251/251 + 26 SKIP** · `stdlib-only` **251/251 + 26 SKIP** — 컨테이너 392/392와 `full`의 차이 1건은 PyYAML 유무(D34), 축소 레그에서는 신규 14건이 **10 PASS + 4 SKIP**으로 전부 나타난다(라이브 재실행 4건만 `ai_edge_litert`·`iree.runtime`·`numpy`를 요구한다) | `docs/EVIDENCE_v0.34_E31.md` | `0f25ad4` |
| **E30b** | 2026-09-10 | **E30 적대적 검증(9 반박 + finding당 2인 검증)이 찾은 SQUEEZE 변환기 확장의 잠재 fail-open(D56) 재현·수정** | 결정론적(합성 flatbuffer·ONNX·IREE 출력 비트 비교) | `harness/gen_tflite_squeeze_cases.py`(합성 SQUEEZE 사례 11건 생성기 — D43 규칙), `harness/e30b_squeeze_probe.py`(사례 1건을 확장에 통과시키고 IREE로 컴파일해 TFLite 의미와 비트 대조), `harness/tflite2onnx_ext_squeeze.py`(C5 + 옵션 부재 + 중복 축 + 항등 사례), `harness/contract_negative_tests.py::e30b_squeeze_extension_cases`(18건), 갱신된 변환 매니페스트(C5 감사 필드) | **수정**. 비행 모델은 영향 없음(kept axes {N,C}는 NHWC·NCHW에서 순서가 같다 — 그래서 합성 사례가 필요했다). C1–C4를 통과하는 TFLite-유효 **부분 squeeze**(예: [1,1,7,64] dims [1] → [1,7,64])가 NCHW로 재색인된 텐서 위에서 방출돼 **데이터가 조용히 전치**됐다: C5를 끄면 sq_H/sq_H77/sq_N이 CONVERTED → 컴파일·실행 성공 → TFLite 의미 대비 max_abs_diff **5.46 / 1.89 / 2.61**(reshape 모드 — 분석서의 문자 그대로의 제안 — 는 셋 다 침묵; squeeze 모드는 sq_H77만 침묵, 비정방 둘은 하류 shape cast가 잡음). 수정: **C5 — 남는 축이 레이아웃 순열 아래에서 TFLite 순서를 유지해야 함**(`check_kept_axis_order`, 순수 함수), 위반은 Transpose 삽입 대신 명시적 거부. 함께: SqueezeOptions 테이블 부재가 `AttributeError` 크래시였던 것(D24 부류) → 빈 dims 결정, 중복 축 collapse, 항등 사례(size-1 축 없음)는 C1 공허참(단, 레이아웃 태그가 있으면 C5가 거부). 합성 11건: CONVERTED 5건 전부 **IREE 출력 = TFLite 의미 비트 동일(max_abs_diff 0.0)**, REFUSED 6건(C5 4·C1 1·C4 1), 크래시 0. 비행 모델 ONNX 바이트 불변(`79c57abc…`). 이 컨테이너 **353/353 → 371/371**. **추가(같은 세션)**: 그 C5 자신이 과잉 거부였음이 적대적 검증에서 재현돼(D57) 축 위치 논증을 **시뮬레이션**으로 교체, 인벤토리↔변환기 판정 일치를 구조적으로 보장, **378/378**. **적대적 검증의 실행 범위(정직하게)**: 반박 9묶음은 완주했으나 **2인 검증 에이전트는 세션 한도로 한 건도 실행되지 않았다** — 수정의 확인 주체는 독립 검증자가 아니라 구현자 자신이다. **CI 실측**(커밋 `39a4692`, run 112, 3레그 success): `full` **370/370 + 1 SKIP**(PyYAML) · `without-iree` **240/240 + 21 SKIP** · `stdlib-only` **240/240 + 21 SKIP** — 컨테이너 371/371과 `full`의 차이 1건은 PyYAML 유무(D34), SKIP 20→21은 e30b의 합성 사례 시험이 변환기 패키지 없이는 정직하게 SKIP하기 때문이다. **D57까지 반영한 CI 실측**(커밋 `e4e60c3`, run 121, 3레그 success): `full` **377/377 + 1 SKIP** · `without-iree` **241/241 + 22 SKIP** · `stdlib-only` **241/241 + 22 SKIP** — 축소 레그의 총 검사 수가 컨테이너(378/378)보다 적은 것은 변환기 패키지가 없으면 `e30b_squeeze_extension_cases`가 SKIP 2건을 남기고 **조기 반환**해 사례를 하나씩 나열하지 않기 때문이다. | `docs/EVIDENCE_v0.33_E30.md` §8 | `39a4692`, `e4e60c3` |
| **E30** | 2026-09-10 | **P1 — OPS-SAT SmartCam 반입 타당성**(여덟 번째 외부 분석서 `docs/reviews/ONAIR_MLIR_P1_SEQUENCE_ANALYSIS_1.md` §9 범위 그대로): 원본 commit·sha 고정, 기계 판독 인벤토리, 원본 반입 차단 재현(SQUEEZE), 조건 검사 변환기 확장, 한 번의 `iree-compile`로 MLIR·VMFB·계약 도달 여부 | 결정론적(해시·계약 수치·HAL 통계·IR 구조) | `results/p1_smartcam_feasibility/`(원본 tflite 8,950,028 B `fd1ecbd0…` 무수정 보존 + `source_manifest.json`·`operator_inventory.json`·`import/import_log.txt`(step 1–8)·두 `transform_manifest.json`·`feasibility_summary.json`·`build/`(mlir.gz·vmfb·layout IR·ELF·계약·헤더·기준선·스모크·축소 dump)·`variant_reshape/equivalence.json`), `harness/p1_tflite_inventory.py`·`harness/p1_tflite_to_onnx.py`·`harness/tflite2onnx_ext_squeeze.py`·`harness/p1_smoke_pip_runtime.py`, `harness/contract_negative_tests.py::p1_smartcam_feasibility_cases`(31건), `requirements.txt`(변환기 패키지 고정) | **TRANSFORM_REQUIRED → GO(감사된 변환 아래)**. stock tflite2onnx 0.4.1은 파싱 단계에서 `Unsupported TFLite OP: 43 SQUEEZE!`로 거부(재현, 시험이 매번 다시 일으킴). 확장은 분석서 §5 C1–C4를 순수 함수로 검사해 전부 성립할 때만 변환(위반은 거부), NHWC→NCHW 재색인 [1,2]→[2,3] 감사. ONNX `Squeeze`·정적 `Reshape` 두 모드는 **linalg MLIR부터 바이트 동일**(`f1f659fa…`, 17,750,942 B). 한 번의 `iree-compile` → vmfb 8,915,257 B, 계약 **오버라이드 0**: `bounded` **18,222,796** = `per_call` **9,382,092**(io 602,124 + transient 8,779,968) + `constants` **8,840,704**, 디스패치 56, 커널 스택 368/439 B(38 호출 전부 해석), 상수 `confirmed`, 구조적 추출기 일치, **E27 (b′) 아티팩트 전용 기준선이 같은 세 값**, layout IR 두 분기 구조(try_map 1/scf.if 1, 1.94×). pip 스모크: `[1,3]` 유한·결정적, **피크 9,382,092 = per_call 정확히**, allocated == freed. **인터페이스 변경 기록**: 엔트리 입력 NCHW `[1,3,224,224]` vs 비행 NHWC — 원소 순서 불보존, P2는 전치 입력 필수. 부수: tflite2onnx 출력이 `set()` 순서 때문에 실행마다 다른 바이트(세 번 = 세 해시) → 래퍼가 canonical 정렬, 원본→ONNX→linalg sha256 매번 재현; pip 바인딩의 호스트 읽기가 HAL 버퍼를 붙듦(D50의 새 얼굴) → 스모크가 결과를 버리는 호출로 피크를 먼저 읽음. **하지 않음**: 수치 동치(P2)·정확도·AArch64/cFS 실행(P3)·soundness 주장. 이 컨테이너 **322/322 → 353/353**. **CI 실측**(커밋 `5267025`, run 110, 3레그 success): `full` **352/352 + 1 SKIP**(PyYAML) · `without-iree` **237/237 + 20 SKIP** · `stdlib-only` **237/237 + 20 SKIP** — 컨테이너 353/353과 `full`의 차이 1건은 PyYAML 유무(D34), SKIP 16→20은 p1-smartcam의 변환기 의존 시험 3건 + 계약 재생성 1건이 해당 패키지·IREE 도구 없이는 정직하게 SKIP하기 때문이다. | `docs/EVIDENCE_v0.33_E30.md` | `5267025` |
| **E29b** | 2026-09-10 | 일곱 번째 외부 검토 §4.1이 코드를 읽고 예측한 **조건부 admission의 fail-open**(D54)을 실물로 재현·수정 | 결정론적(HAL 통계·계약 수치) | `results/e29b_conditional_verify/`(단일 호출 fixture `bigact` + native 5셀·cFS 3셀 raw log + summary), `native/native_learner.c`·`native/cfs_app/fsw/src/ai_learner.c`, `harness/contract_negative_tests.py`(E29b 10건 신설 + **E29 assertion 2건 정정**) | **수정**. E29의 사후 검증 `hal_peak_after_append > CONTRACT_PER_CALL_BYTES`는 copy 분기가 정확히 `constants`를 할당하므로 **`constants < per_call`인 모든 모델을 통과**시킨다. 보관 계약 21개가 전부 `constants > per_call`(b2_resnet 24 B 차이)이라 E29의 양방향 실측이 이 조건을 못 밟았고, **E29 회귀 시험 자신이 그 비교식을 pin**하고 있었다(결함을 고정하는 시험). `bigact`(per_call 45,444 / constants 14,016, 오버라이드 0) 재현: 조건부 승인(예산 45,444) → 앱이 `arm=copy`라고 기록하고도 통과 → native 3/3·cFS 5/5 완주, 피크 **59,460(예산의 131%)**, `peak_within_bounded=true`. 수정 둘: (1) **append 전** `module_ptr_mod64 != 0`이면 런타임 생성 전에 거부(`MAP_PRECONDITION_UNMET`; E29 D2로 분기를 미리 확정 — 검토서 §4.2 선택지 1, copy 분기 할당 자체가 안 일어남), (2) append 후 `hal_peak_after_append != 0 → 거부`(map 분기의 append 피크는 정확히 0, E29 32/32). 수정 후 shim 셀은 native·cFS 모두 런타임 생성 전 거부·추론 0·cFS OPERATIONAL 유지, 정렬 셀은 예산 = per_call에서 **정확히 45,444**로 완주(과잉 거부 0). revert-and-confirm-fail: 비교식만 되돌리면 2건 FAIL. 이 컨테이너 **310/310 → 320/320**(v0.32.1의 D55 가드 2건을 더해 322/322). **CI 실측**(커밋 `56f02b2`, run 104, 3레그 success): `full` **321/321 + 1 SKIP**(PyYAML) · `without-iree` **213/213 + 16 SKIP** · `stdlib-only` **213/213 + 16 SKIP** — 컨테이너 322/322와 `full`의 차이 1건은 PyYAML 유무(D34), SKIP 15→16은 E29b의 단일 호출 재생성 시험이 iree 도구 없이는 정직하게 SKIP하기 때문이다. 보관 계약 diff 0. **교훈**: D53의 *"어느 숫자로 승인했는지와 어느 숫자로 검증하는지"*를 E29 자신이 어겼다 — 승인은 분기를 전제했는데 검증은 크기를 비교했다. 그리고 **시험이 무엇을 pin하는지 읽어라** — 결함의 문자열을 pin한 시험은 결함을 지킨다 | `docs/EVIDENCE_v0.32_E29b.md` | (이 커밋) |
| **E29** | 2026-09-09 | E26이 미확정으로 남긴 `stream.resource.try_map` **분기 결정 요인**을 규명하고, 배포가 그것을 제어할 수 있는지·제어했을 때 계약을 어떻게 읽어야 fail-open이 없는지 확인 | 결정론적(HAL 통계·포인터 정렬·계약 수치) | `harness/e29_align_probe.c`, `harness/e29_collect.py`, `harness/e29_no_posix_memalign.c`, `results/e29_conditional_contract/`(64셀 스윕·7모델 before/after·cFS 4셀 raw log·revert 기록), `native/native_learner.c`·`native/cfs_app/fsw/src/ai_learner.c`·`native/cfs_app/CMakeLists.txt`·`scripts/50_wire_cfs_ai_learner.sh`, `harness/contract_negative_tests.py` 20건 | **규명·제어·검증**. 결정 요인은 **모듈 이미지 포인터의 64바이트 정렬**이다 — `iree_hal_heap_buffer_wrap()`(`runtime/src/iree/hal/buffer_heap.c`)이 `IREE_HAL_HEAP_BUFFER_ALIGNMENT`(=64, `base/config.h:244`) 미정렬 span의 import를 `OUT_OF_RANGE`로 거부하고, map 분기가 정확히 그 import다. **8모델 × 8 정렬 클래스 = 64셀에서 map 32 · copy 32 · 제3의 값 0**(사전 고정 기준 D1 이분성·D2 결정 요인 둘 다 위반 0). 두 C 경로가 blob을 `malloc`하고 있었고 glibc가 16 또는 32 mod 64를 주었기 때문에 **E26·E26e·E26f의 native·cFS 셀이 전부 copy 분기**였다. `posix_memalign(…,64,…)` 한 줄로 7모델 전부 map 분기로 넘어갔고, **map 피크 = `per_call` 정확히, copy 피크 = `bounded` 정확히**(7/7 양방향) — 즉 E26이 배포 의존성으로 보고한 **1.00×~172.30× 폭 전체가 이 한 포인터의 정렬**이었고, E26f의 172.30×는 b3_deepae의 `bounded/per_call`과 같은 수다. **E26 Q1(soundness)은 그대로**(`bounded`는 두 분기의 최댓값이고 copy가 그 값에 정확히 닿음). 조건부 admission은 **계약 스키마 변경 0** — `B_map = CONTRACT_PER_CALL_BYTES`, `B_copy = CONTRACT_BOUNDED_BYTES`가 이미 헤더에 있다. opt-in 기본값 0(기존 배포 판정 무변경), 전제조건을 **구성으로 강제**하고 append 직후 **측정으로 검증**해 copy 분기면 추론 0건에서 거부. cFS 실측: 예산 6,208 B에서 `NOT_ADMITTED`이던 b3_deepae가 opt-in 시 같은 예산에서 5/5 완주, **실측 피크 정확히 6,208**. revert-and-confirm-fail: 검증 블록만 빼면 6,208 예산에서 **1,069,632(172배 초과)**로 완주하는데 `peak_within_bounded`는 `true`다 — 그 필드는 `bounded`와 비교하지 승인 근거 예산과 비교하지 않는다(D53). 이 컨테이너 **304/304**(E27 §7 정오표를 더한 최종 310/310). **CI 실측**(커밋 `58757b3`, run 96): `full` **309/309 + 1 SKIP**(PyYAML 미설치) · `without-iree` **202/202 + 15 SKIP** · `stdlib-only` **202/202 + 15 SKIP** — 이 컨테이너와 `full`의 차이 1건은 PyYAML 유무다(D34: 추정하지 않고 조건과 함께 병기). 보관 14개 계약 diff 0 | `docs/EVIDENCE_v0.31_E29.md` | (이 커밋) |
| **E28** | 2026-09-09 | 여섯 번째 외부 검토(SCI 심사) §4.3·§10 검증 중 발견한 **admission 게이트 자신의 fail-open**(D52) 재현·수정 | 결정론적(계약 매크로 산술·cFS 생존/사망) | `native/cfs_app/fsw/src/ai_learner.c`(세 버퍼 static화), `results/e28_stack_failopen/`(양방향 raw log), `results/e26_boundary_utility/x86_64/cfs/b{2,3}_*.log`, `harness/contract_negative_tests.py` 8건 | **수정**. 스택 게이트가 `base + kernel`만 계산하는데 추론 경로가 **계약이 크기를 정하는 버퍼 셋**을 같은 스택에 잡고 있었다(`feat[]` 4 B/입력원소, `out[]` 4 B/출력원소, `outs[]` **16 B/출력원소**). OPS-SAT급 계약(150,528 입력원소)에서 미계상 **602,160 B** vs 부여 262,160 B — 게이트가 `accounted=true`, admission **ADMIT**, binding **MATCH**를 낸 뒤 첫 추론에서 **SIGSEGV(EXIT=139), 추론 0건**. **지금까지의 D47·D48·D49가 전부 과잉 거부였던 것과 방향이 반대다** — 배치되면 안 되는 구성을 통과시켰고, 그 게이트가 이 연구의 주장 그 자체다. 수정은 세 버퍼를 `static`으로(같은 파일 `:322`의 `zeros[]` 전례) — **게이트 공식에 I/O 바이트를 더하는 검토서 권고는 미채택**(부여량이 정확히 base+kernel이라 오늘 도는 모델이 전부 DENY, 유형 (B)를 새로 심는다). revert-and-confirm-fail: 같은 계약·같은 스택·같은 아티팩트에서 되돌리면 **EXIT=139·추론 0건**, 고치면 **10/10 완주·정상 종료**. 과잉 거부 확인 `b2_resnet` 5/5 완주. **부수**: 검토서가 P0로 지목한 **B2·B3의 cFS x86-64 셀을 코드 수정 0건으로 완주**(둘 다 peak = bounded, `allocated` 분기) — 막고 있던 것이 없었고 실행하지 않았을 뿐이다. 이 컨테이너 **284/284**, 보관 14개 계약 diff 0 | `docs/EVIDENCE_v0.30_E28.md` | (이 커밋) |
| **E27** | 2026-09-09 | **R-3**: 네 정보 수준(소스 / 아티팩트만 / 이 저장소 MLIR 경로 / 런타임 관측)을 정상 조건과 교란 조건에서 비교 | 결정론적(정적 분석 수치·HAL 통계) | `harness/e27_baseline_source_tensors.py`(신설), `harness/e27_collect.py`(신설), `results/e27_baselines/summary.json`, `harness/contract_negative_tests.py` 9건 | **R-3의 원래 전제는 반증된 채로 남고, 그 자리에 다른 답이 있다.** 정상 조건 **8/8 셀**(합성 3 + AArch64 3 + 실물 MLPerf Tiny 2)에서 **아티팩트만 보는 (b)가 계약값과 정확히 일치** — *"이 수치는 MLIR이라야 얻는다"*는 쓰면 안 된다. 차이는 **교란에서만** 난다: 컴파일러 버전 드리프트(IREE 3.10 산출물)에서 (b)는 `alloca_sizes=[]`·`alloca_unresolved=[]`로 **문제 없다고 보고하며 5,172 = 152배 과소**, (c)는 같은 입력에서 786,476을 내고 디스어셈블 실패를 note로 기록. 동적 형상은 세 수준 전부 거부(모든 교란이 (b)를 무너뜨리진 않는다). (a)는 3.04×~7.29× **과대**로 한 번도 과소 추정하지 않으나 그 위의 게이트는 배치 가능 모델을 거부한다(유형 B). **조용한 과소 추정: a 0 · b 1 · c 0.** (d)는 배포 간 최대 **172.3×** 폭이라 기준값이 아니다. **결론**: MLIR 수준이 주는 것은 더 정확한 수치가 아니라 **독립적인 두 번째 정보원**이며, fail-closed는 의지가 아니라 **정보원 수**의 문제다. 이 컨테이너 **276/276**, CI 실측(`7fb1d4d`, run 76) full **275/275+1 SKIP** · without-iree **168/168+15** · stdlib-only **168/168+15** | `docs/EVIDENCE_v0.29_E27.md` | (이 커밋) |
| **E26f** | 2026-09-09 | **E26-ext 완결**: 상수 지배형 실물 모델(MLPerf Tiny Deep AutoEncoder, 상수:per-call **171.3:1**)에서 계약 경계 측정 | 결정론적(HAL 통계·계약 수치) | `results/e26_boundary_utility/x86_64/ext_b3_deepae/`(단일 호출 산출물 3.4 MB), `harness/contract_negative_tests.py` 7건 | **Q1·Q2·Q3 전부 성립**. bounded 1,069,632 = per_call 6,208 + constants 1,063,424, 오버라이드 0개 — 계획서 §1.1이 조사에서 인용한 값과 **정확히 일치**(독립 재현). 반입은 이 세션에서 직접 수행 (tflite2onnx → graph.name=infer → iree-import-onnx 17 → iree-opt torch→linalg → 한 번의 iree-compile), 잔여 torch op 0. **배포 의존성 최대 사례**: 같은 vmfb가 pip `iree.runtime` 6,208(`mapped`, tightness **172.30×**) vs 소스 빌드 C 런타임 1,069,632(`allocated`, 1.00×) — E26-core 상한 45.50×를 넓혔다. Q3는 `B−1`→DENY(rc=3, cleanup 0) / `B`·`B+1`·2MiB→ADMIT, 250회 호출 실패 0. **B2(1:1)와 B3(171:1)는 할당 구조가 정반대인데 결론이 같다.** 이 컨테이너 **267/267**, CI 실측(`f7be746`, run 73) full **266/266+1 SKIP** · without-iree **159/159+15** · stdlib-only **159/159+15** | `docs/EVIDENCE_v0.28_E26f.md` | (이 커밋) |
| **E26e** | 2026-09-09 | **E26-ext**: 실물 MLPerf Tiny ResNet(CIFAR-10, 16 dispatch)에서 계약 경계 측정 — E26-core의 결론이 합성 모델 밖에서도 성립하는가 | 결정론적(HAL 통계·계약 수치) | `results/e26_boundary_utility/x86_64/ext_b2_resnet/`(단일 호출 vmfb·layout IR·dump·계약·ELF·측정), `harness/contract_negative_tests.py` 8건 | **Q1·Q2·Q3 전부 재현**. bounded 618,856 = per_call 309,416 + constants 309,440, 오버라이드 0개. **같은 vmfb가 배포에 따라 두 분기를 모두 탔다** — pip `iree.runtime` peak 309,416(`mapped`, tightness 2.00×) vs 소스 빌드 C 런타임 618,856(`allocated`, 1.00×), 차이가 **정확히 상수량**. Q3는 `B−1`→DENY(rc=3, cleanup 0) / `B`·`B+1`·1MiB→ADMIT. **부수 실측 2건**: (a) `iree-compile`이 이 구성에서 **바이트 재현적이지 않다**(ResNet 스레딩 3회 = 3개 해시, `--mlir-disable-threading` 2회 = 동일) — 그러나 **계약 수치는 전부 동일**하고 `artifact.bytes`/`sha256`만 다르다. (b) **D50**: 반환 버퍼를 붙들면 HAL 피크가 부풀어(309,576 = per_call + 4×40) 저장소 분류기가 **거짓 `refutes_hypothesis`**를 냈다 — 놓아주면 정확히 per_call. 이 컨테이너 **260/260** | `docs/EVIDENCE_v0.27_E26e.md` | (이 커밋) |
| **E26d** | 2026-09-09 | 사전 고정 계획 §5-3이 지정했으나 EVIDENCE_v0.25가 보고도 미실행 기록도 하지 않은 **A5b_canonical**을 실제 게스트에서 실행 | 결정론적(손상 방식·해시·게스트 raw log) | `results/e26_boundary_utility/aarch64/a5b_canonical/`(계약·게스트 로그·판정 JSON), cFS aarch64 빌드 `e26d_canon_a5b`, `harness/contract_negative_tests.py` 8건 | **실행 완료**. `flatbuffer_root_uoffset`로 손상(크기 동일 732,760 B), 계약을 손상 파일의 실제 해시로 재생성해 **해시 게이트가 잡을 수 없는 조건**을 만든 뒤 게스트 `core-cpu1` 기동 → admission **ADMIT** → binding **MATCH** → `runtime_load_failed`(IREE FlatBuffer 검증기, E17과 **같은 오류 문자열**) → cleanup 1회 → `CFE_ES_ExitApp`, 이후 cFS는 남은 앱을 계속 로드(크래시·abort 0). `check_expect` 불일치 0건. **손상 아티팩트는 저장하지 않고** 시험이 in-tree 원본에서 재생성해 게스트가 실제로 적재한 해시와 대조한다(결정성 증명). 판정(Q1·Q3·Q2)에는 영향 없음 — A5b는 §2 기준에 들어가지 않는다. 이 컨테이너 **252/252** | `docs/EVIDENCE_v0.25_E26.md` §9 정오표 | (이 커밋) |
| **E26c** | 2026-09-09 | 벤치마크 도입 조사가 남긴 잔여 결함 후보 **C-1**의 재현·수정(D49) — 다중 출력 모델이 `stream.resource.subview` 때문에 `UNKNOWN_BOUND` | 결정론적(layout IR 구조 분석·계약 수치·HAL 통계) | `harness/static_mem_bound.py`·`harness/mlir_alloc_walk.py`(subview 봉쇄 검사), `harness/gen_model_multiout.py`, `results/e26c_multiout/`, `harness/contract_negative_tests.py` 15건 | **수정**. IREE는 결과 2개를 **하나의 external 슬랩(128 B)에 패킹**하고 `subview` 2개(32 B @0, 16 B @64)로 쪼갠다 — 즉 파서는 유일한 실제 할당을 이미 건전·보수적으로(128 ≥ 32+16) 계상하고 있었고 거부는 순전히 화이트리스트 누락이었다. 두 추출기에 op을 추가하되 **검사 없이 신뢰하지 않는다**: 세 index 피연산자가 전부 상수로 풀리고 `offset + result_size <= source_size`일 때만 통과. **revert-and-confirm-fail 2단계** — 화이트리스트만 되돌리면 7건 FAIL(과잉 거부 실재), 검사 코드만 빼면 4건 FAIL(없으면 fail-open). 실측 HAL 피크 **704 B = bounded 704 B**(tightness 1.00×), 오버라이드 0개. 이 컨테이너 **244/244**, 보관 14개 계약 diff 0 | `docs/EVIDENCE_v0.26_E26c.md` | (이 커밋) |
| **E26b** | 2026-09-09 | 벤치마크 도입 조사 중 발견된 두 번째 과잉 거부(D48) — `iree-dump-module`의 빈 백틱 라벨 상수 세그먼트가 관측에서 누락 | 결정론적(덤프 파싱·계약 수치) | `harness/static_mem_bound.py`(`_is_string_label` 신설), `harness/contract_negative_tests.py` 8건, `results/e26_boundary_utility/empty_label_rodata_fixture/` | **수정**. 첫 바이트가 NUL인 2,816 B 상수 블록이 빈 백틱 쌍으로 출력돼 관측 총합에서 버려짐 → 계약값과 모순 → D28 게이트 거부 → 오버라이드로 뚫으면 `overridden`이 되어 D39 헤더 게이트가 다시 거부. **정직한 f32 모델의 배치 경로가 닫혀 있었다.** 라벨 **길이**(`len(label)==nbytes`)로 판별하도록 수정, 저장소 전 산출물 137줄 중 136줄이 이 성질 만족·예외가 정확히 그 세그먼트. 이 컨테이너 **229/229**, 보관 14개 계약 diff 0 | `docs/EVIDENCE_v0.24_E26b.md` | (이 커밋) |
| **E26a** | 2026-09-09 | 벤치마크 지침(B0–B4)의 "출처가 추적되는 실제 워크로드" 요구를 따르다 첫 실물 모델에서 재현된 과잉 거부(D47) 수정 — ELF 내부 호출 타깃 해석 | 결정론적(ELF 정적 분석·계약 수치·해시) | `harness/elf_stack_frame.py`(호출 그래프 해석, CFG 순회로 콜리 범위 발견), `harness/make_contract.py`(`kernel_external_call_insns`를 미해석 호출 수로), `harness/contract_negative_tests.py` 12건, `results/e26_boundary_utility/mlperf_tiny_resnet_fixture/` | **수정**. MLPerf Tiny ResNet(CIFAR-10)의 softmax dispatch가 컴파일러 헬퍼를 80회 호출 → 타깃 2개 전부 `.text` 내부, `.plt` 없음, 미정의 심볼 0, 헬퍼는 프레임 0 B leaf. 수정 후 **오버라이드 0개**로 계약·헤더 완주(`bounded=618856`, `KERNEL_STACK_BYTES=439L`). **E14 14개·E25 canonical이 전부 `total_call_insns=0`이라 합성 모델셋으로는 재현 불가능했던 결함**. 거부 유지 조건 7종 시험 고정, revert 시 11/12 실패. 이 컨테이너 **221/221**, 보관 14개 계약 diff 0 | `docs/EVIDENCE_v0.23_E26a.md` | (이 커밋) |
| **E24c** | 2026-09-09 | 외부 검토(v0.20/E24b, `docs/reviews/REVIEW_v0_20_E24b.md`) F1–F5를 15개 에이전트로 재현 5 + 각 권고의 과잉 거부 위험 10(finding당 2렌즈)으로 독립 검증 후 확인된 것만 수정 | 결정론적 | `harness/gen_model_manyconst.py`(신규), `results/e24c_manyconst31/`(실물 단일-호출 번들 440 KB), `harness/make_contract.py`(subset_sum 결정 절차 선행·`constants_confirmation_state`·예산초과 거부), `harness/gen_contract_header.py`(구성요소 비음수 요구), `contracts/contract.schema.json`(`minimum: 0` + 상태 enum), `harness/contract_negative_tests.py`, 정정 7곳 | 5건 전부 재현, 4건 수정. **F1은 미채택** — 리뷰 권고(provenance 필수화)를 적용하면 삭제 경로(VERIFIED=0)가 **위조 경로(손으로 쓴 `{single_invocation:true}` → VERIFIED=1)**로 바뀌어 악화됨을 실측, threat model 결정 항목으로 이관. F2는 리뷰 서술이 부분적으로만 정확(두 변종 중 하나는 비음수 검사 부재가 원인). F3는 **리뷰 처방이 정직한 >256 MiB 모델을 거부**해 결정 절차를 예산 검사보다 앞에 두는 형태로 채택(무작위 3,000건 brute force 교차검증 0 불일치). F4는 에이전트가 "재현 안 됨"으로 보고했으나 직접 확인 결과 재현됨 — **그대로 받았으면 맞는 문서를 틀리게 정정할 뻔했다**. F5는 이 저장소 문서의 인과 서술이 틀렸음을 확인(양쪽 signed `long`; unsigned였다면 오히려 거부). D41–D44 수정, 170/170 → **191/191**(이 컨테이너 실측; CI 실측 `full` 190/190+1 SKIP · `without-iree` 103/103+11 · `stdlib-only` 103/103+11) | `docs/EVIDENCE_v0.21_E24c.md` | `3d1f640`·`c924a47`·`7bf6115`·`1758b8e` |
| **E24b** | 2026-09-09 | 외부 검토(v0.19 연구 목표 재정리본, `docs/reviews/REVIEW_v0_19_E24_RESEARCH_REFRAME.md`) §5.2 반례 5건(R1–R5)을 10개 에이전트로 재현 5 + 각 수정안의 과잉 거부 위험 5로 독립 검증 후 수정 | 결정론적 | `harness/make_contract.py`(`waive()` override 기록, 음수 스택 거부, tri-state `subset_sum_match`), `harness/gen_contract_header.py`(음수 스택·자기모순 bounded·shape 충돌·provenance 게이트, `CONTRACT_PROVENANCE_VERIFIED`), `harness/contract_negative_tests.py`, `contracts/contract.schema.json`, `native/build.sh`, `scripts/62_compile_and_check_aarch64.sh`, 보관 헤더 14개 재생성 | 5건 전부 재현. **심각도 재분류 2건, 방향 반대** — R1은 리뷰의 "매우 낮음: 수동 변조 필요"와 달리 정상 `make_contract.py` 경로로 도달하는 claim blocker(cFS 스택 게이트의 `stack_needed`가 음수가 되어 signed 비교가 모든 스택에서 참인 항등식이 됨 — 원인 서술은 E24c/F5에서 정정), R4는 **리뷰 처방이 정직한 31세그먼트 모델을 통째로 거부**해 미채택(열거기를 tri-state로 고쳐 해결). R5는 행위 시험 5건이 전부 PASS인 드리프트를 소스 수준 가드만 잡음을 실측. 부수 발견 D40(문서화된 스모크 경로 2개 파손, 1건은 E24의 N3이 만든 유형 (B) 회귀). D35–D40 수정, 143/143 → **170/170**(이 컨테이너 실측; CI 실측 `full` 169/169+1 SKIP · `without-iree` 85/85+9 · `stdlib-only` 85/85+9, 차이 1건은 PyYAML 유무), 보관 14개 헤더는 `CONTRACT_PROVENANCE_VERIFIED` 한 줄만 추가 | `docs/EVIDENCE_v0.20_E24b.md` | `cd50194`·`6b76dfd`·`a85ddf3`·`b47feb7`·`bf2be2a` |
| **E24** | 2026-09-09 | 외부 검토(v0.18-후속, `docs/reviews/REVIEW_v0_18_FOLLOWUP.md`) N1–N6·S5를 21개 에이전트로 독립 재현 검증 후 fail-closed 불변식 4건 + 과잉거부 회귀 1건 + 하네스 거짓경보 수정 | 결정론적 | `harness/make_contract.py`, `harness/gen_contract_header.py`, `harness/gen_model.py`, `harness/contract_negative_tests.py`, `contracts/contract.schema.json`, `plugins/compiled_learner/runtime/contract.json`, `.github/workflows/contract-negative-tests.yml`, `scripts/30_setup_iree.sh` | 7건 전부 confirmed·반박 0건이나 심각도는 리뷰와 다름(N1 P0→P1, N3 P0→P1, N4 P1→P2). **리뷰 제안 3건은 실측 결과 과잉 거부를 유발해 범위를 좁힘** — N1의 문자 그대로의 수정은 `dynamic` 계약 2개(A8 입력)를, N3의 strict 변종은 저장소 스키마 정본 형태를 거부. D28–D32 수정, 125/125 → **142/142+1 SKIP**(CI 실측), 보관 14개 계약 diff 0·헤더 바이트 동일 | `docs/EVIDENCE_v0.19_E24.md` | (이 커밋) |
| **E23** | 2026-09-08 | 외부 검토(v0.15) 잔여 4건(F4 표현 정정 / F8 A5a·A5b 손상 방식 코드화 / F10 OnAIR 바인딩 갭 / F11 범위 명시) + E22의 CI가 실제로 잡은 신규 크래시(D25) 수정 | 결정론적 | `harness/corrupt_vmfb.py`(신규: flip / flatbuffer_root_uoffset 두 손상 방식, ZIP64 STORED 외과적 패치 + CRC 갱신), `harness/e14_{make_scenarios,cfs_scenarios}.py`(corrupt_method 필수화·fail-closed), `plugins/compiled_learner/artifact_binding.py`(신규: 크기→sha256 게이트, stdlib 전용)와 플러그인 결선, `harness/{make_contract,static_mem_bound,contract_negative_tests}.py`(D25), `native/cfs_app/fsw/src/ai_learner.c`(admission JSON에 scope), `docs/EVIDENCE_v0.13_E18.md` §7·`docs/EVIDENCE_v0.17_E22.md` §6 정오표 | A5b 구조 손상을 보관 vmfb 8/8에서 실행(ZIP CRC 유효·타 엔트리 불변·크기 동일·결정적) 후 `iree.runtime`으로 실제 로드해 E17과 같은 오류 문자열(`FlatBuffer length prefix out of bounds, prefix 4294967295`)로 거부됨을 확인 — A5b의 네 번째 레벨. OnAIR 플러그인이 A3/A5a를 C 경로와 같은 순서(크기→해시)로 거부하게 됨. D25: 진짜 의존성 없는 환경(도구·모듈 모두 부재)에서 크래시 → 수정 후 **125/125 / 95/95+3 SKIP / 50/50+5 SKIP** | `docs/EVIDENCE_v0.18_E23.md` | (이 커밋) |
| **E22** | 2026-09-08 | 외부 검토(v0.15) F9 재현성 실제 확보 — fresh clone에서 크래시 없이 SKIP/FAIL 명확화 | 결정론적 | `harness/contract_negative_tests.py`(Result에 skip 상태, structural_available/with_structural_override 신설), `.gitignore`(dump/ 제외 해제), `results/e14_aarch64_qemu/*/dump/`(242개 파일 커밋), `requirements.txt`(신규), `.github/workflows/contract-negative-tests.yml`(신규) | iree.compiler.ir 부재 시 uncaught RuntimeError로 전체 크래시(요약 0줄)하던 3개 지점을 명확한 SKIP으로 정리; E21의 F3(구조적 검증기 미설치 기본 하드실패)과 이 시험 하네스 자신의 상호작용을 --allow-missing-structural-checker 투명 전달로 해소; dump/ 6.5MB를 gitignore에서 빼고 커밋(회귀·음성 시험이 실제로 요구하는데 빠져 있었음); 이 세션 내 실제 git clone으로 재현 확인 — with iree-base-compiler: 107/107, without: 크래시 없이 77/77+3 SKIP | EVIDENCE v0.17 | (v0.17) |
| **E21** | 2026-09-08 | 외부 검토(v0.15, `docs/reviews/REVIEW_v0_15_LATEST.md`) fail-open 결함 6건 수정(F1/F2/F3/F5/F6/F7) | 결정론적 | `harness/make_contract.py`(F1: 빈 dump-dir 하드실패, F2: ABI 반사 부재 하드실패, F3: 구조적 검증기 미설치 기본 거부), `harness/mlir_alloc_walk.py`(F5: stream.resource.pack 비상수 unresolved), `harness/gen_contract_header.py`(F6: 스택 불신뢰 분류 거부, F7: CONTRACT_DTYPES_ALL_F32 신설), `native/{ai_learner.c,native_learner.c}`(F7 C게이트), `harness/contract_negative_tests.py` 확장(96/96→107/107) | 11개 finding을 독립 에이전트 11개로 병렬 검증(9건 confirmed, 1건 partially_confirmed=F3, 1건 not-a-defect=F11), 그중 코드 fail-open 결함 6건 실제 재현 후 수정 — F1(빈 dump-dir → one-invocation 신호 전부 None인데 통과), F2(ABI 반사 부재는 불일치와 달리 미거부), F3(구조적 크로스체크가 패키지 미설치시 opportunistic로 저하, 검증 결과 "은폐"는 과장이나 정책 불일치는 타당), F5(stream.resource.pack 비상수 슬라이스가 unresolved 누락, IREE 상류 실제 문법으로 재현, 현재는 정규식 파서와의 크로스체크가 이중 방어로 완화 중), F6(스택 분석이 스스로 불신뢰로 분류해도 KNOWN=1), F7(C게이트가 "single-f32"라 주장하나 dtype 미검사, 매크로 신설). 전 6건 수정 전 코드로 되돌려 신규 시험이 실제로 실패함을 확인(revert-and-confirm-fail) | EVIDENCE v0.16 | (v0.16) |
| **E20** | 2026-09-08 | E19 구조적 크로스체크 적대적 코드 리뷰(4관점 병렬+finding당 3인 반박검증) + 과잉 거부 결함 2건 수정 | 결정론적 | `harness/make_contract.py`(비교 기준 `whole`→`p`, `dense_sum`→`const_b`), `harness/mlir_alloc_walk.py`(`diff_against_regex` 공유 헬퍼 신설), `harness/contract_negative_tests.py` 확장(85/85→96/96) | 13건 finding 중 11건 확인/2건 반박; 확인된 결함 중 2건은 실제 재현된 High severity 과잉 거부(버그 A: `whole`이 정규식 파서의 "파일 마지막 print=최다lowering" 낡은 가정에 의존, 계약이 실제 쓰는 `p`와 달라 정상 모델도 print 순서에 따라 거부될 수 있었음; 버그 B: constants(sum) 비교가 `dense_sum`이 아니라 계약이 실제 채택하는 `const_b`(패킹 크기) 기준이어야 함, 정렬 패딩 있는 모델은 영구 오탐 거부됐을 것) — 둘 다 보관된 layout IR을 surgical 텍스트 편집으로 재현(재컴파일 없음), 수정 후 회귀 시험으로 고정, 수정 전 코드로 되돌려 시험이 실제로 실패함을 확인(revert-and-confirm-fail). 3개 위치에 중복 구현됐던 diff 로직을 공유 헬퍼로 통합. 시험 갭 4건(dispatches-only 양성, allow override 예외분기, constants 진짜불일치, graceful-degradation 정식화) 추가 | EVIDENCE v0.15 | (v0.15) |
| **E19** | 2026-09-08 | 정규 MLIR pass 2단계: `mlir_alloc_walk.py`(E18) 구조적 추출기를 `make_contract.py`에 필수 크로스체크로 결선 | 결정론적 | `harness/make_contract.py` 수정(`provenance.structural_walker` 신설), `harness/contract_negative_tests.py` 확장(66/66→85/85) | 정규식 파서(`static_mem_bound.parse_alloc_ir`)와 구조적 추출기가 같은 layout IR을 각각 읽어 불일치 시 하드 실패(`--allow-structural-mismatch`로만 우회), `iree.compiler.ir` 미탑재 환경에서는 스킵(기록만, E15 이전 기준선으로 안전하게 저하); 보관된 14개 계약을 실제 프로덕션 경로(subprocess make_contract.py 호출)로 재생성해 계약 수치 diff 0 + 신규 크로스체크 필드 14/14 `available=True, agrees=True` 확인; 하드 실패 배선 자체는 monkeypatch로 불일치·파싱예외 두 조건 모두 실제 SystemExit 확인(in-process); CLAUDE.md 우선순위 3의 "컴파일러 버전 변경 시 명시적 실패" 평가지표에 실제 강제 지점 마련(다른 버전 재확인은 여전히 안 함) | EVIDENCE v0.14 | (v0.14) |
| **E18** | 2026-09-08 | 정규 MLIR pass 1단계: 구조적(비정규식) 할당 추출기, `iree.compiler.ir` API 사용 | 결정론적 | `harness/mlir_alloc_walk.py`(신규), `harness/contract_negative_tests.py` 확장(66/66) | `--mlir-print-ir-after` 청크 조각남 문제를 `util.global.load/store` 선언 합성으로 해결(유일한 텍스트 처리 단계), 이후 크기 추출은 전부 실제 Operation/Value API(op.name, def-use 체인, arith.constant 속성 직접 읽기); 보관된 14개 아티팩트 전부 정규식 파서와 값 일치(inputs/outputs/transient 원소별, constants 합계, entry_found, unresolved 존재); 화이트리스트 축소(실제 레지스트리로)로 미인식 op fail-closed 재확인; make_contract.py 통합은 범위 밖(후속) | EVIDENCE v0.13 | (v0.13) |
| **E17** | 2026-09-08 | AArch64 게스트 재현: A5b 최초 실행(3레벨), A2 경계값(mlp16k·multibranch), 재시작 2회+DELETE | 결정론적(verdict·exit code·cleanup 카운터) + 실행 확인(qemu-system-aarch64 게스트 core-cpu1) | AArch64 크로스빌드(scripts/60·61·70·71 전체 재구축), 게스트 배포 아티팩트(임시, results/ 미보관) | A5b(FlatBuffer root-uoffset 구조 손상, ZIP 재압축 후 sha256을 계약에 반영해 binding MATCH 조건 충족)를 native x86-64·cFS x86-64·**cFS AArch64 게스트** 세 레벨 전부에서 최초 실행 — 전부 admission ADMIT/binding MATCH/runtime_load_failed(FlatBuffer verifier)/cleanup 1회/cFS OPERATIONAL 유지, 크래시 0; mlp16k·multibranch A2 경계값 6/6(native)+3/3(cFS mlp16k); 재시작 2회+DELETE를 x86-64·AArch64 양쪽에서 완주(init_count 3, cleanup_calls 3, 이중해제 없음) — ES 명령 기반 정상 종료 경로로 v0.9 "정상 종료 자원회수 미검증" 문제도 해소 | EVIDENCE v0.12 | (v0.12) |
| **E16** | 2026-09-08 | C 게이트 보강: 스택 실거부(D15)·blob 크기 선검사(R8)·인터페이스 gate, x86-64 native_std 재구축 실기동 검증 | 결정론적(verdict·exit code·JSON) + 실행 확인(cFS core-cpu1 기동) | `native/cfs_app/fsw/src/ai_learner.c`, `native/native_learner.c`, `scripts/50_wire_cfs_ai_learner.sh` 수정 | 스택 확인을 Init() 최선두로 이동해 실제 거부 분기 신설(EID_STACK_REJECT), blob malloc 전 크기 선검사(계약과 다르면 해시 계산 없이 즉시 거부), 인터페이스(단일 f32 in/out) defense-in-depth 추가; x86-64 native_std 실기동으로 정상/스택거부/크기불일치/NOT_ADMITTED 4개 시나리오 검증(EVS 이벤트·JSON 필드 확인); 부수 발견: scripts/50이 WIRING.md 규칙(stack=base+kernel)을 어기고 262144로 하드코딩하던 버그를 신규 gate가 드러내 수정 | EVIDENCE v0.11 | (v0.11) |
| **E15** | 2026-09-08 | 계약 도구 fail-closed 전환 + 음성 시험(외부 검토 2건 반영, D12-D14 수정) | 결정론적 | `harness/contract_negative_tests.py`(신규), `static_mem_bound.py`/`make_contract.py`/`gen_contract_header.py` 수정 | 파서 줄바꿈 누락(D13)·미인식 op 무시·entry_found 누락(D12)·헤더 생성기의 음수/미지원 bound_method·스택 미상 수용·one-invocation의 layout IR 미결합(D14) 전부 재현 후 거부로 전환; 음성 20건+단위 5건+회귀 26건 = 51/51 PASS; 보관된 v0.9 14개 계약·헤더 재생성 시 diff 0(회귀 없음) | EVIDENCE v0.10 | (v0.10) |
| E0-env | 2026-09-07 | cFS/OnAIR/IREE 빌드·기동 | n/a | `scripts/*.sh`, `docs/STATUS.md` | cFS OPERATIONAL(LC 7.0.1, SBN 포함), OnAIR CSV/Kalman 완주, IREE 컴파일·실행 | STATUS | 278e448 |
| E0-platform | 2026-09-07 | 타이밍 적격성 게이트 | **FUNCTIONAL_ONLY** | `harness/platform_check.py`, `results_platform.json` | ratio_p99 1.67, ratio_max 2.26 | STATUS §2 | fd94420 |
| E0-integration | 2026-09-07 | OnAIR 플러그인 통합 | n/a | `plugins/compiled_learner`, `plugins/python_learner` | OnAIR core 변경 0줄, 로더 경유 완주. 함정: ServiceManager 선초기화 | EVIDENCE v0.1 §1 | ff926f7 |
| E1 | 2026-09-07 | 모델 크기 스윕 (latency) | FUNCTIONAL_ONLY | `harness/sweep.py`, `results_sweep*.json` | compiled/numpy 6.3–9.5×, 교차점 없음 | EVIDENCE v0.1 §2 | 3c82702 |
| E2 | 2026-09-07 | 모델 크기 스윕 (dispersion) | FUNCTIONAL_ONLY | 동상 | 소형에서 compiled p99/med 더 나쁨; 대형 역전은 noise 안 | EVIDENCE v0.1 §3 | 3c82702 |
| E3 | 2026-09-07 | 마샬링 floor | FUNCTIONAL_ONLY | `results_findings.json` | identity-call 4.70 µs → 격차 원인은 커널 품질 | EVIDENCE v0.1 §4 | 3c82702 |
| E4 | 2026-09-07 | lowering 설정 효과 (4종, h=16384) | FUNCTIONAL_ONLY | `results_findings.json` | median 1.87× 편차, avx2 < default | EVIDENCE v0.1 §5 | 3c82702 |
| E5 | 2026-09-07 | lowering 특성화 (10종 × 3크기, 메모리 포함) | FUNCTIONAL_ONLY (binary·RSS는 결정론적) | `harness/characterize.py`, `results_characterization_*.json` | Pareto 5개; 크기 간 ρ=+0.18; **메모리 축 반증** (B0 40 KB vs 1216 KB) | EVIDENCE v0.2 | 833edb6 |
| E6 | 2026-09-07 | 정적 메모리 상한 산출 (P2b) | 결정론적 | `harness/static_mem_bound.py`, `harness/static_bound_sweep.py`, `results_static_bound.json` | 정적 상한 = HAL 피크, 30/30 sound, tightness 1.0; 설정 불변 | EVIDENCE v0.3 §1 | 45213d6 |
| **E6c** | 2026-09-07 | 베이킹 모델 정적 상한 IR 파싱 (검토 §5 분모 해소) | 결정론적 | `results_static_bound_baked.json` | 30/30 sound·tight; 상수 720,896 B 모듈 상주로 분리 귀속 | EVIDENCE v0.3 §7 | (v0.3.1) |
| **E14-S0** | 2026-09-08 | 교차 ISA(x86-64→AArch64) 계약 건전성: 컴파일·구조 분석·qemu-user 확인 | 결정론적(계약·IR·ELF·admission) / qemu-user 시간값은 비증거 | `e14/aarch64/`, `contracts/contract.e14_aarch64.json`, `scripts/60-62_*.sh` | bounded_bytes 동일(786,476); HAL peak/per-call/경계값/모델교체/동적형상 x86-64와 일치; **AArch64에 16B 스택 프레임(x86-64엔 없음)** 발견 | EVIDENCE v0.8 | (v0.8) |
| **E14-S1** | 2026-09-08 | qemu-system-aarch64 Linux 게스트 + cFS-in-guest + Conv2D/multi-branch/동적형상 모델 | 결정론적(계약·ELF·admission·cFS 이벤트) / QEMU 시간값은 비증거 | `results/e14_aarch64_qemu/{aarch64,x86_64,native,cfs,comparison}/`, `harness/{make_contract,elf_stack_frame,e14_matrix,e14_cfs_scenarios,cfs_cmd}.py`, `scripts/51_build_cfs_aarch64.sh` | bounded_bytes 동일(모델 3종 x86-64=AArch64, MLP 1종 한정 벗어남); cFS 게스트 A1×2모델·A3·A4·A6·A7·A8 7/7 PASS; 태스크 스택 잔차 모델별 확정(16~191B)·시작스크립트 반영·자체검증; D9(스택정의 통일)·D10(one-invocation 실제검증 추가) 정정 | EVIDENCE v0.9 | af8b94a |
| **E11b** | 2026-09-08 | standalone 계측 정정 + 결합 | 결정론적(HAL·해시) | `native/native_learner.c`, `summary.json` | peak↔bounded true; 정상 상태 per-call **65,544**; 3200/3200 완료; binding MATCH | EVIDENCE v0.7 §2 | 0e887df |
| **E11c** | 2026-09-08 | 모델 교체(같은 ABI) + 계약 A | 결정론적 | 동상 | CONTRACT_ARTIFACT_MISMATCH, exit 5, 런타임 미생성 | v0.7 §1.2 | 0e887df |
| **E12b/c/d** | 2026-09-08 | cFS: 일치 / 교체 / 파일 부재 | 결정론적 | `native/results/cfs_run_*.log` | 25/25 완료 / 기동 거부+cleanup / cleanup; 3경우 cFS OPERATIONAL | v0.7 §1–3 | 0e887df |
| **E13** | 2026-09-08 | 계약 IR ↔ LLVM IR ↔ ELF 대응 (host/generic) | 결정론적 | `e13/`, `contracts/contract.e13_host.json` | 커널 alloca 0·call 0·스택 프레임 0; host AVX-512 FMA 34 vs generic 스칼라; 같은 호출 계약 MATCH | v0.7 §4 | 0e887df |
| **E11** | 2026-09-08 | Native C 변형 (standalone, Python 없음) | FUNCTIONAL_ONLY (HAL·RSS·admission은 결정론적) | `native/native_learner.c`, `native/build.sh`, `native/results/summary.json` | admission ADMIT/NOT_ADMITTED(exit 3); HAL 피크 786,476 = bounded; RSS 4.3 MB; median 32.2 µs | EVIDENCE v0.6 §2 | 271a237 |
| **E12** | 2026-09-08 | cFS 앱 `AI_LEARNER` 통합 | 동상 | `native/cfs_app/`, `native/results/cfs_run_*.log` | ADMIT: ES HK 패킷 추론 25회, 피크=bounded; NOT_ADMITTED: 앱 기동 거부, cFS OPERATIONAL 유지 | EVIDENCE v0.6 §3 | 271a237 |
| **E9** | 2026-09-08 | 할당 구조 4사례 (정렬·수명·대형·fusion) | 결정론적 | `harness/structural_cases.py`, `results_structural_cases.json` | slice 합은 2/4 과소(D3); post-layout 슬랩 4/4 sound·tight | EVIDENCE v0.5 §1 | (v0.5) |
| **E10** | 2026-09-08 | 경계값 U−1/U/U+1 | 결정론적 | `results_admission.json` (`boundary_test`) | 18/18 정확 | EVIDENCE v0.5 §5 | (v0.5) |
| **E7b** | 2026-09-08 | E7 재실행 (post-layout, 새 판정 의미, 상수 독립 확인) | 결정론적 | `results_admission.json` | 258 판정, misprediction 0, 상수 129/129 아티팩트 확인 | EVIDENCE v0.5 §6 | (v0.5) |
| **E7** | 2026-09-07 | 메모리 전용 admission checker (A5) | 결정론적 | `harness/admission_check.py`, `admission_sweep.py`, `results_admission.json` | 240 판정, optimistic/pessimistic misprediction 0, config-invariant | EVIDENCE v0.4 §1 | (v0.4) |
| **E8** | 2026-09-07 | 동적 형상 모델의 UNBOUNDED 판정 | 결정론적 | `results_admission.json` (`E8_dynamic_shape`) | all_sizes_static=false → UNBOUNDED, 예산 무관 거절 | EVIDENCE v0.4 §2 | (v0.4) |
| **E6b** | 2026-09-07 | **정정**: 가중치 베이킹 재특성화 | FUNCTIONAL_ONLY | `harness/characterize_baked.py`, `results_characterization_baked.json` | E1–E5가 매 호출 가중치 복사를 포함. 정정 후 격차 2.2–4.1×, ρ=+0.81, Pareto 붕괴 | EVIDENCE v0.3 §2 | c78c7ab |

## 가설 판정 이력

| 버전 | H1 | H2 | H3 | 근거 실험 |
|---|---|---|---|---|
| v0.0 (연구노트) | 주가설 | 보조 | 보조 | — |
| v0.1 | 부분 기각 | 강화 (근거 noise 안쪽) | 미검증 | E1–E4 |
| v0.2 | 기각 유지 | **확립** (결정론적 근거로 교체) | 미검증(입력 확보) | E5 |
| **v0.3** | 기각 유지 (격차 2.2–4.1×로 정정) | **격하: 부분 지지** (ρ 근거 철회) | **메모리 축 전제 충족** (정적 상한 sound) | E6, E6b |
| **v0.9** | 변화 없음 | 변화 없음 | H3 일반성 범위 재확대: **모델 종류 불변 확인**(MLP·Conv2D·multi-branch 3종, x86-64=AArch64); **cFS admission의 타깃 독립성 검증**(AArch64 게스트, 7/7 시나리오 PASS); 태스크 스택 잔차 통일된 정의로 확정·모델별 회계(16~191B)·시작스크립트 실반영 | E14-S1 |
| **v0.8** | 변화 없음 | 변화 없음 | H3 일반성 범위 확대: **ISA(x86-64/AArch64) 불변 확인**(단일 MLP 한정); AArch64 태스크 스택 잔차 첫 발견, 명시적 회계 필요 | E14-S0 |
| **v0.7** | 변화 없음 | 변화 없음 (설정별 지연 차이의 구조적 원인 확인: AVX-512 FMA vs 스칼라) | 검증 범위 확장: 결합·계측·실패 처리·코드 대응 | E11b/c, E12b/c/d, E13 |
| **v0.6** | 변화 없음 (Native 1.84×) | 변화 없음 | **cFS 앱 배치 형태에서도 성립** (단일 앱·모델·native_std); 경계 (b) 런타임 구성 2종에서 견고 | E11, E12 |
| **v0.5** | 변화 없음 | 변화 없음 | 시험 조건 내 성립, **근거 강화** (구조 4종, 경계값, 상수 독립 검증); 중심 문장을 검토 §8 권고로 교체 | E9, E10, E7b |
| **v0.4** | 변화 없음 | 변화 없음 (E7 config-invariance는 메모리 판정에 선택 불필요라는 반대 증거) | **메모리 축, 시험 조건 내 성립** (경계 b, 240/240, E8) | E7, E8 |
| **v0.3.1** | 현 모델·구현·플랫폼에서 성능 우위 미관측; 예측성 일반화 보류 | 설정별 비용 차이 관측; **계약 기반 선택의 이점 미입증** | 정적 per-call 버퍼 계약의 후보 근거 확보; 경계·가정·판정기 검증 필요 | E6c, 외부 검토 |

## 반증된 주장 이력

| 주장 | 출처 | 반증 실험 | 버전 |
|---|---|---|---|
| "OnAIR 공식 경로에서 매 호출 출력 버퍼를 **해제**했다" (Q4 PASS 근거) | EVIDENCE_v0.36_E33 §8 | v0.38.1: 같은 셀의 원자료 `results/e33_onair_official/p_admit/run.json` `stderr_tail`이 `nanobind: leaked 10 instances`(HalBufferView·MappedMemory) + `leaked 5 keep_alive records`를 보고한다. EVIDENCE는 이를 **한 줄도 기록하지 않았다**(저장소 grep 0건). 상태는 **메모리 해제 미검증** — 반대 방향(누수 확정)도 쓸 수 없다(경고 본문이 IREE 바인딩 참조 계수를 지목, HAL 피크 미측정). Q4의 나머지 근거는 유효하므로 판정 자체는 유지 | v0.38.1 (D60, EVIDENCE_v0.36_E33 §10) |
| "커널 스택 4/4 일치가 **독립적인 두 정보원**의 교차 확인이다" | EVIDENCE_v0.38_E35 §2.1·§4 | v0.38.1 코드 대조: 두 수준이 `harness/elf_stack_frame.py` **같은 구현**을 쓴다(`make_contract.py --elf-analysis` vs `e35_baseline_policy_matrix.py:120 --vmfb`). 게다가 `make_contract.py:822`가 "분석한 ELF == vmfb 내장 ELF"를 이미 hard fail로 강제하므로 **같은 분석기가 같은 ELF를 두 입력 경로로 읽은 결과**다. 값이 vmfb만으로 얻어진다는 반증(§0·§2 판정)은 불변, **오류 독립성 주장만 철회** | v0.38.1 (EVIDENCE_v0.38_E35 §7.2) |
| "검토서 §10 단계 1~5를 전부 완료했다" | CLAUDE.md v0.38 문단 · 결정 메모 | v0.38.1: E32 원자료 `verdicts.stage_2_complete = false`(조건부 셀이 승인 예산 9,382,092의 106.4%인 9,984,204로 실행, D59 수정은 보고만 하고 초과를 제거하지 않는다), E34는 §10 단계 4가 지정한 **"AArch64 cFS 평가 절차"** 미실행(E34 계획서 §6이 측정 전에 범위 밖으로 선언 — 은폐는 아니나 완료도 아니다). 정확한 표현은 **"단계 1·3·5 완료, 단계 2·4는 잔여 셀 있음"** | v0.38.1 |
| "아티팩트만 보는 정보 수준 (b)는 읽지 못한 것을 없다고 보고한다 — 그것이 MLIR 수준의 고유 기여다"로 읽힐 수 있는 §5 서술 | EVIDENCE_v0.29_E27 §5 | v0.31 세션: 같은 정보원에서 모든 침묵 경로를 명시적 거부로 바꾼 fail-closed 변종(`harness/e27_baseline_vmfb_only_hardened.py`)을 보관 아티팩트 8개에 직접 실행 — 정직한 7개는 계약 `bounded`와 **정확히 일치**(과잉 거부 0), 3.10 드리프트 아티팩트는 **`C4_DISASM`으로 거부**(5,172을 조용히 내지 않음). 관측된 것은 **출하한 구현**의 성질이지 정보 수준의 성질이 아니었다. 정정된 주장: 정보원 수가 정하는 것은 fail-closed 가능 여부가 아니라 **fail-closed일 때 무엇이 남는가**다(하나면 거부만, 둘이면 값+note). §0 판정과 §2의 8/8은 불변 | v0.31 (EVIDENCE_v0.29_E27 §7.2) |
| "152배 과소 추정의 기전은 VM 바이트코드 디스어셈블이 버전 경계를 넘지 못하는 것" | EVIDENCE_v0.29_E27 §3.1 | v0.31 세션 재현: 그 기전은 `alloca_sizes=[]`(전이 슬랩 65,544 누락)만 설명한다. **지배항은 두 번째 독립 실패** — `analyze()`의 `consts = sum(unlabeled[:-1])`이 쓰는 "실행 ELF는 마지막 라벨 없는 세그먼트"라는 가정이 버전 사이에서 깨진다(3.10 `[module.fb, .so, _const.bin]` vs 3.11 `[module.fb, _const.bin, .so]`, 두 vmfb의 ZIP 엔트리 직접 확인). 그래서 5,136 B 실행 ELF가 상수로, 720,896 B 상수가 실행 코드로 뒤바뀌어 `5,172 = 36 + 5,136`이 나왔다. 침묵 실패가 하나가 아니라 **둘**이었다는 점에서 §3.1의 결론은 오히려 강해진다 | v0.31 (EVIDENCE_v0.29_E27 §7.1) |
| "`sys.meta_path` 훅으로 '패키지가 아예 없는 환경'을 시뮬레이션했다" (E22 §1) | EVIDENCE_v0.17_E22 §1 | E23: 그 훅은 모듈만 숨기고 콘솔 스크립트를 남긴다 — 진짜 의존성 없는 CI 러너에서 `iree-dump-module` 부재로 크래시(D25). "모듈만 없는 환경"의 시뮬레이션이었음으로 정정 | v0.18 |
| "E25가 E14의 cross-target `both_sound: null` 갭을 해소했다" | EVIDENCE_v0.22 §6.4 · CHANGELOG · CLAUDE.md · EXPERIMENT_LOG | 외부 검토(REVIEW_v0_22_E25 §4.2) 후 코드 대조: `cross_target_compare.py:201`의 `both_sound`는 각 타깃 실행의 `peak_within_bounded`를 종합한 **메모리** 지표이고, E25 cFS 로그에는 `mem` 레코드가 아예 없다. 채워진 것은 `out0_agreement`이며 canonical 1개 한정 | v0.22.1 |
| "두 ISA의 코드생성이 같은 누산 순서를 만들어 비트까지 같았다" | EVIDENCE_v0.22 §6.1 · CHANGELOG | 외부 검토(REVIEW_v0_22_E25 §3) : 관측한 것은 출력 바이트 동일뿐. 누산 순서는 측정한 적 없음(두 ELF dispatch 미대조). 서로 다른 순서가 같은 결과를 낼 수도 있으므로 출력만으로는 근거가 되지 않음 | v0.22.1 |
| "AOT가 Python보다 빠르고 예측 가능" (H1) | 연구노트 v0.1 §5 | E1, E2 | v0.1 |
| "median과 tail 랭킹이 다르다" | 방향판단 §3.3 | E4 재분석: 차이 1.15× < noise 1.67× | v0.2 |
| "peak memory가 제안 framework의 우위 축" | 방향판단 §7 | E5: B0 대비 5.2–30.4× 더 씀 | v0.2 |
| "모델 크기가 바뀌면 순위 붕괴 (ρ=+0.18)" | EVIDENCE v0.2 §4 | E6b: 가중치 복사 인공물, ρ=+0.81 | v0.3 |
| "Pareto front 5개" | EVIDENCE v0.2 §3 | E6b: 상충 미관측 | v0.3 |
| "E5 메모리 축 반증 배수 5.2–30.4×" | EVIDENCE v0.2 §5 | E6: 배수는 런타임 컨텍스트 귀속; 프로그램 정적 65.6 KB | v0.3 |
| "A5b(`runtime_load_failed`)를 native 레벨에서 확인했다" | EVIDENCE v0.9 §4.1·§9 | 외부 검토 2건(REVIEW/OPINION v0.9) 대조: native·cFS 로그 어디에도 A5b 실행 흔적 없음(§3 표엔 A5a만), `runtime_load_failed` 7/7 `null` | v0.9.1 |
| "7/7 PASS"가 원래 계획한 cFS 시나리오 전체를 뜻한다 | EVIDENCE v0.9 §4.1 | 외부 검토 2건: 계획 31개 중 7개만 실행, expect 축소(min_completed 15→3 등), 전부 timeout(EXIT=124) 종료 | v0.9.1 |
| "커널 스택을 별도 회계해 자체 확인하도록 구현했다"가 admission gate를 뜻한다 | EVIDENCE v0.9 §5·§7-#8 | 외부 검토 2건: `accounted=false`에서도 초기화 거부 분기 없음(`ai_learner.c:258-265`), 자원 획득 이후에 계산되는 텔레메트리 | v0.9.1 |

## 방법론 결함 이력

| ID | 결함 | 영향 실험 | 발견 경로 | 조치 |
|---|---|---|---|---|
| D76 | **하네스가 배포 설정의 상대경로를 닫힌 두 키에 대해서만 다시 뿌리내렸다.** `harness/onair_integration_check.py:84`가 `("artifact_dir", "fixture_dir")` — `compiled_learner` 배포가 우연히 쓰는 두 키 — 만 처리해, `model_file`을 선언하는 LiteRT 배포에서 경로가 **`results/results/...`로 이중화**됐다 | E43의 O0 셀(그 외 배포는 두 키만 쓰므로 영향 없음) | **E43이 LiteRT 기준선을 돌리려다 발견.** 플러그인이 거부를 예외가 아니라 상태로 보고하도록 설계돼 있어 `active: false` + 사유로 드러났고 OnAIR 프로세스는 rc 0으로 정상 종료했다 — 설계된 fail-closed는 작동했고 잘못된 것은 하네스가 만든 설정이었다 | **조치 완료**: `PATH_KEYS`에 `model_file`을 더하되 **키만 추가하지 않았다** — 자기가 뿌리내릴 줄 모르는 키에 `..` 상대경로가 있으면 **명시적으로 거부**한다(새 배포 키가 조용히 잘못된 뿌리를 갖는 일을 막는다). 집합은 **명시적으로 유지** — `contract_file`은 `artifact_dir`에 붙이는 맨 파일명이라 *"`_file`로 끝나면 전부"*로 일반화하면 그 `os.path.join`이 잘못된 경로를 돌려준다(과도한 일반화 자체가 결함이다). **D62의 세 번째 얼굴** — D62 출력 arity → E45 입력 종류 → E46 출력 레이아웃 → 여기 배포 키 | 
| D75 | **D52의 수정이 한 버퍼를 빠뜨렸다.** `native/cfs_app/fsw/src/ai_learner.c:557`의 `float yv[CONTRACT_OUTPUT_ELEMS]`가 자동 변수인데, 형제 셋(`:635` `feat`, `:652` `out`, `:683` `outs`)은 D52(E28)에서 전부 `static`이 됐고 표시까지 달려 있다. 스택 게이트(`:326`)는 `base + CONTRACT_KERNEL_STACK_BYTES`만 요구하고 **이 버퍼를 어느 항에도 계상하지 않는다**. **기존 세 모델은 출력이 3·10·640 원소라 원리적으로 못 밟는다** | cFS 앱의 E25 재생 경로. 출력이 큰 계약을 배치할 때 | **E46이 WGAN(출력 150,528 원소)을 반입하려다 발견**. 코드 검토가 아니라 모델 반입 시도가 찾았다 | **조치 완료**: `static`으로 수정하고, 그것이 성립하는 근거가 계약이 선언한 `analysis_domain.required_premises.max_in_flight_calls: 1`(E40)임을 코드 주석에 명시(우연이 아니라 전제다). **양방향 실측**(`results/e46_wgan/d75_probe/`, 같은 선언·같은 사용처를 실제 컴파일): 수정 전 정적 프레임 **602,136 B**(`add $0x93018,%rsp`)·런타임 스택 변위 **602,151 B**로 게이트 허용 262,655 B의 **2.29배 초과**, 수정 후 프레임 **8 B**이고 버퍼는 `.bss`(`yv.0`). **주장하지 않음**: cFS 재빌드 후 게스트에서 SIGSEGV를 관측하지는 않았다 — 관측한 것은 프레임·변위이지 크래시가 아니다(v0.9.1 A5b 교훈) | 
| D74 | **실입력에서 Deep AutoEncoder가 사전 고정 기준을 만족하지 않는다.** 실 ToyADMOS log-mel 창 34개에서 21,760원소 중 **94개**가 `abs ≤ 1e-4 OR rel ≤ 1e-5`를 넘는다(최악 `abs` 6.189e-04 = `abs_tol`의 6.2배). 합성 `uniform[0,1)` fixture에서는 같은 모델·같은 기준이 PASS였다 — **결함이 드러난 것이 아니라 그 fixture가 모델의 실제 값 규모를 한 번도 밟지 않았다**(실제 log-mel은 dB라 `|출력|` 중앙값 28.2, OR 규칙의 유효 문턱이 `max(1e-4, 1e-5·|v|)`이라 규모가 커지면 `abs` 다리가 먼저 걸린다) | E34·E36b의 b3 의미 동치 셀의 **범위**(그 셀들의 판정 자체는 합성 입력에 대해 여전히 참이다), `CLAUDE.md` 중심 주장 | E45가 계획서 §4대로 실입력으로 재판정. 착수 전 조사가 b3를 **뒤집힐 수 있는 유일한 셀**로 미리 지목했다 | **조치: 범위 한정(정정), 기준 불변.** 계획서 §4가 측정 전에 *"FAIL이 나오면 기준이 아니라 FAIL을 보고한다"*고 정해 두었다. 도구 결함이 아님을 네 축으로 확인(두 경로가 **한 파일**을 읽음 · 전처리 항등 · IREE 3회/LiteRT 3회 **비트 동일** · 보관 셀 재판정 동일). 사후 특성화(판정 아님): 창 5개 170샘플에서 **10샘플 5.9% · 1,754/108,800 원소 1.6%** — 계통적이되 소수이고 샘플별 `max_abs` 중앙값은 `abs_tol`의 38%. 모델별 tolerance는 **채택하지 않는다**(판정이 모델의 함수가 된다). 어느 구현이 맞는지는 제3의 기준값이 없어 단정하지 않는다 | 
| D73 | **E39a가 쓸 수 있는 도구를 덜 써 놓고 그 결과를 '환경의 상한'이라고 적었다.** 선행연구 비교표가 모든 행을 `search_summary`로 등급 매기고 `fulltext 등급 칸: 0개 (이 환경의 상한)`을 **리터럴로** 적었는데, 실제로는 `mcp__Scholar_Gateway__semanticSearch`가 OnAIR 논문의 AI Magazine 공동 게재본(10.1002/aaai.70020, open access) **본문 청크를 반환한다**. 0은 세어서 얻은 값도 아니었다(D68 계열 — 세지 않고 적은 값이라, 뒤에 원문을 받아도 표는 계속 0이라고 말했을 것이다) | E39a 표의 등급 칸과 집계 문장(축별 판정 자체는 불변) | 로드맵 검증 워크플로우의 처방 위험 점검(P5)이 지적, 이 세션이 같은 도구를 직접 호출해 확인 | **조치 완료(정정)**: `fulltext_partial` 등급을 신설하고 OnAIR 행을 그 등급으로 올렸다 — 받은 청크 번호(1·3·5·10)와 **전체 청크 수 16**을 함께 적어 *부분* 원문임을 못박는다. 생성기가 등급을 **세어서** 적도록 고쳤다. 코퍼스 실측도 기록: 같은 도구에 같은 주제를 물어도 이 표의 IEEE·ACM·Elsevier·arXiv 항목은 나오지 않아 나머지 행의 등급은 그대로다. 반환된 4청크 약 27,000자에 `memory`·`admission`·`budget`·`RAM`·`resource`·`footprint`가 각각 0건이지만, **16청크 전체에 없다는 증명이 아니라 메모리를 겨냥한 질의가 고른 청크에서의 관측**이다 | 
| D72 | **E35의 "24/24 판정 동일"은 독립 측정이 아니라 따름정리다.** `harness/e35_baseline_policy_matrix.py:142`의 `if not same_numbers or b_level.get("bounded_bytes") is None: continue`가 **세 수치가 다른 모델의 셀을 하나도 만들지 않고**, 통과한 모델에서는 두 수준에 **같은 세 수치**와 계약에서 한 번 읽은 **같은 `bound_method`**를 넣으므로 두 `ap.decide` 호출이 같은 입력이다 → `verdicts_disagreeing: 0`은 **구조적으로 보장된 값**이다. 계획서 §4-2는 측정 전에 *"다른 셀이 하나라도 있으면 그 셀이 이 실험의 핵심이다"*라고 적었는데 구현은 그 셀을 **원리적으로 만들 수 없다** | E35의 `verdicts_agreeing/disagreeing` 해석(세 수치 4/4·커널 스택 4/4는 실제 측정이므로 불변) | 로드맵 검증 워크플로우의 적대적 반박이 수집기를 끝까지 읽고 지적, 이 세션이 **실행으로 재현**(같은 입력에서 3구간 × 2정책 판정 차이 0, 두 dict `==` True) | **조치 완료(정정)**: 판정은 불변이고 **근거의 무게를 옮겼다** — 반증의 근거는 24/24가 아니라 **세 수치·커널 스택의 4/4**다. `docs/EVIDENCE_v0.38_E35.md` §8 정오표, `CLAUDE.md` 중심 주장·가드레일 3곳 정정, 가드 시험 6건 신설(수집기 구조·따름정리 실증·문서 정정이 기계가 읽는 자리에 닿았는지). *"차이가 나오면 먼저 자기 도구를 의심하라"*(E35 자신의 교훈)를 **차이가 나오지 않았을 때도** 적용해야 했다 | 
| D71 | **E33의 `p_legacy` 셀은 저장소 내용만으로 재생성할 수 없었다.** `configs/onair_compiled_learner.ini` 템플릿이 telemetry 파일을 `smartcam_replay.csv`로 **하드코딩**하고 있어, 9필드 telemetry가 필요한 `legacy_mlp` 셀에 2필드가 들어가고 플러그인이 정당하게 거부한다(`active=false`·추론 0). E33의 보관 `onair.ini`는 `legacy_mlp.csv`를 가리키므로 그 셀은 **저장소에 없는 템플릿**으로 만들어졌다. 판정 수치가 아니라 **재현성** 결함이다(D55·F8 계열) | E33 `p_legacy` 셀의 재현 | E41의 R4(기존 셀 불파괴) 확인 중 발견. **대조 실행**으로 E41 변경과 무관함을 먼저 확정했다 | **조치 완료**: 배포마다 `telemetry` 키를 선언하게 하고 하네스가 치환하되, **선언이 없으면 추측하지 않고 거부**한다(잘못된 telemetry는 그 셀이 무엇을 쟀는지를 바꾼다). 고친 뒤 `legacy_mlp`이 `active=true`·추론 4회·`NOT_EVALUATED`로 **E33 값과 일치**한다 | 
| D70 | **계약이 상한을 말하지 않는다고 선언하면서 동시에 '정적 형상'이라고도 선언하고 있었다(잠재 fail-open).** `make_contract.py`의 전제 목록 첫 항목이 **리터럴** `"static shapes"`였기 때문에, `interface.all_static=false` · `bound_method=NONE` · `unresolved_sizes=['%1','%6','%5']`인 `contract.dynamic.{x86_64,aarch64}.json`이 `bound_assumptions[0]`·`validity.assumptions[0]`에 `"static shapes"`를 싣고 있었다. `bound_assumptions`를 프로그램이 읽는 곳이 저장소에 **한 곳도 없어**(쓰기 1곳, C 주석 1곳) 오늘까지는 무해한 산문 부정확이지만, 로드맵 §5.3-3이 요구한 대로 이것을 기계 판독 필드로 승격하는 순간 **이 저장소가 거부하려고 만든 바로 그 모델의 계약에 기계 판독 가능한 거짓 단언**이 실린다 | 보관 `contract.dynamic.*` 2개의 선언(수치는 무관) | 로드맵 §5.3-3 처방의 과잉 적용 위험을 착수 전 조사로 검토하다 발견 | **조치 완료**: 리터럴을 `all_static`에서 **유도**로 바꾸고, `analysis_domain.derived`의 모든 값을 원천 필드와 재대조하는 `analysis_domain_drift()`를 신설해 불일치 시 **계약을 쓰지 않는다**. 산문 `bound_assumptions[0]`과 기계 판독 `derived.static_shapes`의 일관성도 같은 가드가 검사한다. 보관 2개 계약의 4 leaf를 정정(수치 불변·헤더 바이트 불변). revert 시 `dynamic` 재생성이 **rc=1로 거부**된다 | 
| D69 | **조건부 admission의 opt-in 설정이 어디에도 독립적으로 기록되지 않았다 — 그래서 그 설정으로 얻은 판정 자신이 설정의 유일한 근거였다(순환).** E36의 두 조건부 셀은 예산 9,382,092 B로 같고 `AI_LEARNER_ALLOW_CONDITIONAL_MAP`만 다른데, 값이 남을 수 있는 자리가 **전부 비어 있었다**: `build.log`에 문자열 `CONDITIONAL` **0건**, 두 배포 트리의 `build_info.json` `app_knobs`가 **완전 동일**하고 그 키를 갖지 않으며, 보관 `ai_learner.CMakeLists.txt`는 `${AI_LEARNER_ALLOW_CONDITIONAL_MAP}` **미전개 변수**, 게스트 raw log에는 해당 stage가 없다. E36 자신이 D61에서 *"판정에 쓴 설정이 무엇이었는지를 판정 자신이 기록하게 하라"*를 세우고 `budget`·`budget_source`를 그 이유로 넣었는데 **opt-in만 그 목록에서 빠져 있었다** | E36의 `cond_positive`·`cond_denied_without_optin` 두 셀의 **기록**(판정 수치가 아니다) | 여덟 번째 외부 검토 §4.1이 지적, 이 세션이 저장소·배포 트리를 전수 조사해 확인 | **조치 완료**: 세 기록을 신설했다 — (1) 앱이 `build_config` stage를 **모든 게이트보다 먼저** 기록(거부하는 셀도 남긴다), (2) 빌드 스크립트가 knob과 `compile_commands.json`에서 뽑은 **실제 `-D` 목록**을 `build_info.json`에 기록, (3) `harness/optin_witness.py`가 **산출물에서** 값을 읽어 요청값과 다르면 **빌드를 죽인다**. witness는 양성 대조(무조건 `bounded` 비교)가 먼저 잡혀야만 `false`를 말하고 그 외에는 `undetermined`다(D25·D29 계열). 보관 E36 바이너리 2개를 in-tree로 넣어 소급 판정이 **재현 가능**하고, `native_learner.c`의 **조용한 reset**(requested→applied)도 함께 분리 기록. 검토가 정한 순서대로 **그 두 셀만** 재실행했고 E36의 결정론적 값은 전부 같다. 시험 **26건**(되돌리면 4건 실패 — 보관 원자료를 나중에 손보는 경우 포함). **같은 결함이 형제 스크립트에**: D61(b)는 `51_build_cfs_aarch64.sh`에서만 고쳐졌고 `50_wire_cfs_ai_learner.sh`가 같은 `${VAR:+-D…}` 모양을 영속 `build-native_std` 트리에 쓰고 있었다 — 같은 방식으로 고치고 3단계 빌드로 실측했다(`results/e38_optin_record/x86_64_cross_check/`) | 
| D68 | **파싱하지 못한 레코드를 "없는 레코드"로 적었다 — 그것을 금지한다고 쓴 파일 자신이.** E37이 신설한 `mk_e36b_summary.py`(그리고 같은 관용구를 쓰던 `mk_e36_summary.py`)가 게스트 로그의 JSON 레코드를 `except ValueError: continue`로 처리해, **DeepAE의 `run` 레코드 7건이 전부 766자에서 잘려** 파싱에 실패하자 `run_records: 0`을 기록했다. 레코드는 7건 실재한다(640원소 f32 출력 배열이 줄 길이를 넘겼을 뿐이다). ResNet(338–344자)·SmartCam(277–282자)은 온전해 걸리지 않았다. **D51과 같은 계열**(*"볼 수 없었다"*를 *"보았더니 없더라"*로 기록)이고, 하필 그 파일의 docstring이 *"absence is not zero (D29)"*라고 쓰고 있었다. **판정 영향 없음**(의미 동치는 `/cf/e25_outputs.bin`에서, 메모리 판정은 짧아서 온전한 `mem` 레코드에서 온다) — 그러나 두 모델의 `run_records` 비대칭(9 vs 0)은 실험 사실이 아니라 로그 절단이므로 그대로 인용하면 안 된다 | E36b DeepAE의 cFS admit 셀 요약 | **E37의 적대적 반박 단계**가 잡았다 — 검증 에이전트는 그 값을 표에 옮겨 적기만 했고, 반박 에이전트가 로그를 직접 열어 발견했다 | **조치 완료**: `stages()`가 잘린 줄에서도 stage 이름을 뽑아 `__unparsed__`에 따로 세고, `record_count()`가 *"레코드가 있었다는 사실"*을 센다. 요약이 `run_records: 7` + `run_records_unparseable: 7`을 함께 싣는다. 진짜 빈 셀은 0/0으로 남는다(수정이 "없음"을 "모름"으로 바꾸지 않았다). 가드 6건: 원자료에 절단이 실재하는지와 요약이 그것을 관측으로 세는지를 **양쪽에서** 고정 |
| D67 | **자기 도구를 먼저 의심해야 하는 두 번째 사례.** `mk_e36b_summary.py`의 첫 판이 계약의 커널 스택 키를 `kernel_stack_bytes`로 짚어(실제 이름은 `kernel_task_stack_bytes`) 두 모델의 값을 `null`로 만들었다. **이전 요약과의 전수 대조**가 잡았다 — 그 대조가 없었으면 요약이 조용히 정보를 잃은 채 커밋됐을 것이다 | E36b 요약 재생성 | E37이 재생성 결과를 기존 값과 키 단위로 비교(바뀐 곳 0·사라진 키 0을 요구) | **조치 완료**: 키를 바로잡고, 대조를 재생성 절차의 일부로 남겼다. **교훈은 E35와 같다**: *차이가 나오면 먼저 자기 도구를 의심하라* |
| D66 | **결함 원장 자신의 수치 오기.** D60 행이 누수 내역을 *"HalBufferView 6 · MappedMemory 4"*로 적었으나 원자료를 세어 보면 **5 · 5**다(총계 10은 맞다). *"수치는 실제 파일에서 읽은 값을 인용한다"*는 규율에 어긋난다 | D60 행의 서술(판정 아님) | E37이 연결표를 만들며 `run.json`을 다시 셈 | **조치 완료**: 정정하고 가드가 원자료 계수와 원장 문자열의 일치를 고정한다. 부수 실측: **누수 인스턴스 수가 추론 횟수와 정확히 대응**한다(p_admit 5회→5·5·5, p_legacy 4회→4·4·4). 이것은 후속 측정 설계의 근거이지 **"해제 미검증"을 뒤집는 근거가 아니다**(HAL 피크 미측정, 경고 본문이 원인을 바인딩 참조 계수로 지목) |
| D65 | **정정이 산문에만 남고 기계 판독 자리와 소스에는 철회된 서술이 그대로였다.** v0.38.1의 D60이 EVIDENCE §10에서 *"해제"*를 철회했으나, 그 문서가 인용하는 `results/e33_onair_official/summary.json`의 `verdicts.Q4_note`는 여전히 *"outputs are read back and **released** every call"*이었고 `compiled_learner_plugin.py:56` docstring도 *"the result buffer is released each call"*이었다. **요약만 읽는 소비자는 철회된 절반을 현재 사실로 읽고, 소스에 남은 단정은 다음 독자가 강한 주장을 복원하는 근거가 된다** | E33 요약과 OnAIR 플러그인 소스 | E37의 연결표 작성 중 원자료 재독 | **조치 완료**: 원자료를 손으로 고치지 않고 **생성기**(`mk_e33_summary.py`)를 고쳐 `Q4_note_erratum`을 덧붙였고(규율 3: 철회가 아니라 덧붙임), docstring은 코드가 실제로 하는 일(`del out` — 파이썬 참조 해제)로 좁혔다. D60 가드가 `p_admit`만 고정하고 있었으므로 같은 조건의 `p_legacy`까지 확장했다 |
| D64 | **손으로 조립한 요약이 필드를 빠뜨렸다.** E36b에는 요약 생성기가 **없었고**(E32·E33·E34·E36에는 있다), 그래서 두 DENY 셀이 키 4개뿐이라 `inferences`가 **부재**했다 — 로그에는 `run` 레코드 0건이 실재하는데도다. **키 부재는 "값이 0"이 아니다**(D29) | E36b 요약의 DENY 셀 | E37의 연결표가 하위 키 단위로 확인하자 드러남(D63과 같은 대조에서) | **조치 완료**: `harness/mk_e36b_summary.py` 신설 — 게스트 로그·계약·비교 JSON에서 전부 유도한다. **기존 값이 바뀐 곳 0 · 사라진 키 0**, 추가된 필드 42개 |
| D63 | **연결표 자신의 fail-open — 부모 객체 단위 resolve.** E37이 만든 `mk_evidence_linkage.py`의 첫 판이 admission 셀을 `cells.<name>` **객체 하나로** 확인해서, 하위 키가 빠져 있어도(D64) 셀 전체가 `present`로 통과했다. *"증거가 추적 가능한가"*를 검사하는 도구가 **정확히 그 갭을 못 보는** 형태였다 | E37 연결표의 항목 4 | 같은 세션의 **적대적 검증 에이전트**가 21셀을 독립 재해결하다 발견, 구현자가 결손 주입으로 재확인 | **조치 완료**: **하위 키마다** resolve한다. 동시에 **null을 갭과 구분**한다 — 거부된 셀에 승인 근거 예산이 없는 것, `BUDGET_INVALID` 셀에 admission 레코드가 없는 것은 같은 레코드의 **양성 신호**(`verdict`, `budget_invalid_event: true`)가 허가하는 값이고, 허가 없는 null은 `null_value`로 남아 시험이 거부한다. 결손 주입 2건으로 실제로 잡히는 것을 확인 |
| D62 | **같은 하드코딩이 한 도구에서는 fail-open, 다른 도구에서는 fail-closed였다.** 두 AArch64 하네스가 SmartCam의 출력 arity를 리터럴 `3`으로 들고 있었다. `e32_native_aarch64.py`는 `per = 3`으로 **10출력 ResNet을 앞 3개로 조용히 잘라** 기록했고(fail-open), `e32_cfs_outputs.py`는 `OUT_ELEMS = 3`이지만 `raw.size % OUT_ELEMS` 검사가 있어 **거부**했을 것이다(fail-closed). 절단된 출력을 비교기에 넣으면 **340/340 원소 실패 + `worst_abs 0.0`** — 판정과 최악 오차가 서로 모순되는, 도구를 먼저 의심해야 하는 신호다. **계산은 처음부터 맞았다**(잘린 3개가 oracle의 앞 3개와 1e-6 수준 일치) | E36b의 ResNet·DeepAE AArch64 셀 | 단계 4의 *"새 모델별 하네스 0개"* 기준을 실제로 확인하려다 첫 비교에서 드러남 | **조치 완료**: 두 도구가 `--contract`의 `interface.outputs[0].shape`에서 arity를 읽고, 출력이 하나가 아니거나 dtype이 f32가 아니면 거부하며, 바이트 수가 나누어떨어지지 않으면 *"모델이 갖지 않은 모양으로 자르기를 거부한다"*고 명시 실패한다. E34가 `model_fixture.py`·`e31_compare.py`를 일반화한 것과 **같은 방식**(분기가 아니라 값). 회귀 3건이 리터럴 복귀와 절단 허용을 함께 막는다. **교훈**: *한 모델로 세운 절차를 다른 모델에 적용하는 것이 그 절차의 시험이다 — 통과하지 못하는 것은 모델이 아니라 도구다* |
| D61 | **한 변경이 심은 두 결함, 둘 다 계측이 잡았다.** (a) **자기 대입**: `(long)AI_LEARNER_BUDGET_BYTES` → `g.budget_bytes` 일괄 치환이 resolver 자신의 초기화 줄까지 바꿔 `g.budget_bytes = g.budget_bytes;`가 됐다. 오버라이드 미설정 시 예산 0 → 모든 실행이 `NOT_ADMITTED`. **bounded > 0이므로 그 거부는 논리적으로 맞고, 따라서 정직해 보인다** — 원인을 감춘 거부다. (b) **공유 CMake 캐시 잔류**: `${ALLOW_CONDITIONAL_MAP:+-D...}`는 *"미설정 = 이전 동작"*처럼 보이지만 `build-aarch64_std`는 호출 간 공유되는 영속 트리라 이전 조건부 빌드의 `1`이 살아남았다 → 무조건 트리가 조용히 조건부가 되고 **거부해야 할 셀이 `ADMIT_CONDITIONAL_MAP`**(유형 A) | E36의 cFS 셀 전부, `native/cfs_app/fsw/src/ai_learner.c`, `scripts/51_build_cfs_aarch64.sh` | **시험이 아니라 계측**: 계획서 §3.1이 요구한 `budget`·`budget_source`를 admission 레코드가 항상 싣기 때문에 (a)는 `"budget":0,"budget_source":"macro"`로, (b)는 거부 셀의 `verdict`로 즉시 드러났다 | **조치 완료**: (a) 매크로를 로컬 변수로 읽고 **매크로 경로에서도 `budget <= 0`이면 `BUDGET_INVALID`로 거부**(원인을 감추는 거부를 만들지 않는다). (b) 기본값 `0`으로 **항상 명시 전달** + `AI_LEARNER_BUDGET_BYTES`와 **같은 방식으로 컴파일 도달 검증**(실패 시 셀이 아니라 **빌드가 죽는다**). 재빌드를 **조건부 트리 먼저** 순서로 해 결함 조건을 실제로 만든 뒤 통과 확인. 재현 로그 보존: `results/e36_aarch64_cfs/{prefix_bug,cache_leak_bug}/`. **교훈**: ***"판정에 쓴 설정이 무엇이었는지를 판정 자신이 기록하게 하라"*** — 그 기록이 없었다면 (a)는 정당한 거부로, (b)는 정당한 승인으로 보였을 것이다 |
| D60 | **문서–원자료 모순(기록 결함)**: `EVIDENCE_v0.36_E33.md` §8이 OnAIR Q4를 *"매 호출 출력 소비·**해제**"*로 적었으나, 같은 셀의 원자료 `p_admit/run.json` `stderr_tail`은 종료 시점에 `nanobind: leaked 10 instances`(HalBufferView 5 · MappedMemory 5 — 세어서 확정. 초기 기록의 "6 · 4"는 오기였고 E37이 원자료를 다시 세어 정정했다) + `leaked 5 keep_alive records`를 보고한다. **원자료는 저장소 안에 커밋돼 있었고 문서만 반대로 적혔다** — D55(요약만 커밋되고 원자료 누락)·D45(`both_sound` 오귀속)와 같은 계열이며, 이번에는 원자료가 있는데도 **읽히지 않았다**는 점이 다르다. 자동 시험이 원자료의 그 필드를 한 번도 보지 않았기 때문에 스위트는 내내 green이었다 | E33(v0.36)의 Q4 판정 문구, 그 문구를 인용한 CLAUDE.md·CHANGELOG | 열 번째 외부 검토 문서가 `run.json`을 직접 읽어 지적, 이 세션이 저장소 원자료로 재확인(그 검토 문서는 근거 위치를 "§9"로 인용했으나 §9에는 없다 — **없다는 것이 결함**이었다) | **조치 완료**: EVIDENCE §10 정오표로 "해제" 철회 → **메모리 해제 미검증**(양방향: 누수 확정도 금지). Q4 판정은 유지(호출 규약 수명주기는 확인됨, 런타임 객체 해제만 미검증). CLAUDE.md 가드레일에 *"OnAIR 경로에서 출력 버퍼 해제를 검증했다"* 금지 추가. 가드 시험 신설 — 원자료에 그 경고가 **실재하는지**와 EVIDENCE가 그 사실을 **기록하고 있는지**를 함께 고정해, 어느 쪽이 바뀌어도 시험이 알린다 |
| D59 | **fail-open(유형 A)**: 조건부 승인 계층의 **사후** 메모리 검사가 승인 근거가 아닌 값과 비교한다. `peak_within_bounded`는 *무조건 계약이 건전한가*를 답하지 *이 배포가 승인받은 예산 안에 있었는가*를 답하지 않으며, 두 값은 무조건 계층에서만 같다. SmartCam/aarch64 실측: `ADMIT_CONDITIONAL_MAP` budget 9,382,092 → 피크 **9,984,204(106.4%)** → `peak_within_bounded: true`. 초과분은 정확히 입력 텐서 하나(602,112 B)이며 `peak = per_call + 입력`이 산술로 맞아떨어진다 — `per_call`의 io 항은 **동시에 살아 있는 입력을 하나로 센다**는 전제를 재생 패턴이 어겼다. D53이 세운 규칙을 **추론 이후에 도는 검사에서 다시 어긴 것**이고, E29b는 그 규칙을 분기 판정에만 적용했다 | E29·E29b의 조건부 계층, `native_learner.c`·`ai_learner.c` | E32 A-cond 셀 실측(E29·E29b는 입력이 수십 바이트인 모델만 조건부로 돌려 같은 구조가 수치로 드러나지 않았다) | **조치 완료**: 두 C 경로가 승인 근거 예산을 스스로 유도해 `admitted_budget_bytes`·`peak_within_admitted_budget`·`admission_mode`를 함께 보고하고, cFS 앱은 초과 시 EVS **ERROR**를 올린다. `peak_within_bounded`는 제거하지 않는다(계약 건전성과 배포 준수는 다른 질문이며 둘 다 필요하다). 대조: cFS 앱의 두 모드는 상주 입력 버퍼를 재사용해 같은 조건에서 피크가 `per_call`과 정확히 같다 |
| D58 | **과잉 거부(유형 B)**: AArch64 과정렬 프레임(`sub x9, sp, #imm` + `and sp, x9, #mask`)의 복귀 철자 `sub sp, x29, #K`를 인식하지 못해 `realign_restored`가 거짓으로 남고 `dynamic_stack_alloc`이 서면서 정직한 정적 프레임이 `bucket_4_unaccounted_dynamic_stack`으로 분류됐다. **도구는 프롤로그를 E14부터 인식했고 올바른 상한(160+1696+63=1919)까지 계산해 두고도 자기 수치를 쓰지 못하게 막고 있었다**(D47과 같은 구조). x86-64의 같은 관용구(`lea -N(%rbp),%rsp`)는 E14부터 처리하고 있었고 주석에 "AArch64 `mov sp, x29`의 대응물"이라고 적혀 있었다 — 빠진 것은 AArch64의 오프셋 철자뿐 | `harness/elf_stack_frame.py`, AArch64 헤더 생성 경로 | E32의 AArch64 SmartCam 컴파일에서 헤더 생성기가 거부(40 dispatch 중 5개) | **조치 완료**: 에필로그 `sub sp, x29, #K`를 프롤로그의 `add x29, sp, #K`와 **K가 같을 때만** 복귀로 인정(한 방향 완화). 독립 산술 검증으로 `all_dispatch_frames_balanced`가 false→true(차변·대변을 따로 누적해 대조하는 항등식). 회귀 10건 — 수용 2건과 **거부 4건**(오프셋 불일치·진짜 `sub sp,sp,x8` alloca·짝 없는 `sub x9,sp,#N`·프레임 포인터 미설정)을 함께 고정, revert 시 수용 2건 FAIL. 보관 14개 계약 diff 0 |
| D57 | **과잉 거부(유형 B) — 그리고 그 수정이 곧 fail-open**: D56을 고치며 도입한 C5가 *"남는 축이 레이아웃 순열 아래 상대 순서를 유지해야 한다"*는 **축 위치 논증**이었다. 재정렬되는 남는 축이 전부 extent 1이면 데이터는 움직이지 않으므로 `[2,1,1,1]` dims `[1]`처럼 **방출이 정확한 입력을 "would reorder data"라는 사실이 아닌 이유로 거부**했다(C1–C4 통과 레이아웃 rank-4 81형상 중 7건). 더 나쁜 것은 **명백한 수정("extent>1 축만 순서 유지")이 fail-open**이라는 점이다 — `[1,2,1,1]` dims `[0]`이 그 규칙을 만족하면서 선언 형상 `[2,1,1]`과 ONNX 추론 형상 `[1,2,1]`이 어긋난다. 같은 자리에서 논증이 두 번 틀렸다. 부수로 형제 도구 `p1_tflite_inventory.py`가 **D56과 같은 조건(SqueezeOptions 부재)에서 크래시**하고 음수 dims를 C1·C4 위반으로 오보고 — 감사 도구가 자기가 감사하는 변환을 반박 | E30b의 C5(v0.33.1), `harness/p1_tflite_inventory.py` | E30 적대적 검증의 `extension-overrejection` 반박 에이전트가 오라클 스윕으로 재현(refuted=true), 이 세션이 저장소 생성기로 직접 재현 | **조치 완료**: C5를 논증이 아니라 **시뮬레이션**으로 교체 — 모델 자신의 형상으로 라벨 배열을 만들어 레이아웃 순열 + 재색인 squeeze를 방출될 ONNX 그대로 계산하고 TFLite 기준(`np.squeeze`)과 **형상·값 둘 다** 대조. `e30b_squeeze_probe.py`가 `--iree`로 보는 등식을 컴파일러 없이 계산하므로 맞는 답을 틀린 이유로 낼 여지가 없다. 두 경계 사례를 생성기·회귀에 신설(과잉 거부 사례는 통과 + IREE `max_abs_diff 0.0`, fail-open 사례는 거부 유지). 인벤토리는 변환기의 순수 함수를 직접 import해 구조적으로 갈라질 수 없게 하고 다중 subgraph를 `inventory_complete` 상태로 승격. 비행 모델 ONNX 바이트 불변, **371/371 → 378/378**. **교훈**: *경계 조건을 논증으로 좁히면 논증이 틀린 만큼 틀린다 — 계산할 수 있는 것은 계산하라* |
| D56 | **fail-open(유형 A, 잠재)**: SQUEEZE 변환기 확장이 C1–C4(제거 축 크기 1·원소 수·dtype·정적 출력 형상)만 검사해, 남는 축의 **순서가 레이아웃 순열(NHWC→NCHW) 아래에서 바뀌는** 부분 squeeze를 통과시켰다 — 형상·원소 수·dtype이 전부 보존되면서 **데이터만 조용히 전치**되는 변환(reshape 모드 max_abs_diff 5.46/1.89/2.61, 컴파일러·런타임 무경고). 비행 모델은 kept axes {N,C}라 무관 | E30 확장(v0.33) | E30 적대적 검증의 반박 에이전트가 `tflite` 빌더 API로 합성 flatbuffer를 만들어 end-to-end 재현; 이 세션이 `harness/gen_tflite_squeeze_cases.py`로 재현 코드화하고 C5를 끈 revert-and-confirm-fail로 확인 | **조치 완료(E30b)**: C5(kept-axis order) 순수 함수 + 명시적 거부, 옵션 부재 크래시 제거, 중복 축 collapse, 항등 사례 정의. 합성 11건 회귀(CONVERTED 5건은 IREE 출력을 TFLite 의미와 비트 대조). **교훈**: *"형상·원소 수·dtype이 보존된다"는 데이터 순서가 보존된다는 뜻이 아니다* — 구조 조건은 레이아웃 변환을 통과한 뒤에도 성립해야 한다 |
| D55 | **원자료 부재 — 세 실험 연속**: `.gitignore`가 `*.log`를 무시하고 실험별 allowlist(e14·e25·e26)로만 되살리고 있어, E28(2건)·E29(4건)·E29b(3건)의 cFS raw log가 **저장소에 들어간 적이 없다**. 세 EVIDENCE 문서가 그 파일을 "원자료"로 인용했고 summary.json은 커밋돼 있어 **시험은 내내 green**이었다 — E22의 `dump/` 함정(시험이 조용히 요구하지 않는 증거)이 한 단계 위에서 재발 | `.gitignore` 8–12행 · E28~E29b 커밋 절차 | v0.32.1 세션에서 `git status --ignored results`로 발견(E29b 커밋 직후). 9건 전부 디스크에는 있었다(E14·E26 로그는 allowlist 덕에 처음부터 추적됨 — 별개) | **조치 완료(v0.32.1)**: allowlist를 `!results/**/*.log`로 교체, 9건 커밋, `cited_raw_logs_tracked_cases()` 신설 — 모든 `results/**/summary.json`의 `"log"` 인용이 존재하고 **git에 추적되는지** 검사(스테이징 전 FAIL·후 PASS 실측, 21건 인용). **교훈**: *"summary가 아니라 원자료가 추적되는지 시험이 직접 물어라"* — 요약만 커밋되면 요약이 곧 증거로 둔갑한다 |
| D54 | **fail-open(유형 A)**: E29 조건부 admission의 사후 검증이 `hal_peak_after_append > CONTRACT_PER_CALL_BYTES`였다. copy 분기의 append 직후 피크는 정확히 `constants`이므로 **`constants < per_call`인 모든 모델**에서 이 식이 거짓 → 앱이 `arm=copy`라고 스스로 기록한 뒤에도 실행 → 종료 피크 `per_call + constants`가 승인 근거 예산 `per_call`을 초과. 보관 계약 21개가 전부 반대쪽이라 E29의 7모델 양방향 실측이 못 봤고, **E29 회귀 시험이 그 비교식의 존재를 pin**하고 있었다 | E29 조건부 계층(v0.31) | 일곱 번째 외부 검토 §4.1이 코드에서 예측. `bigact`(per_call 45,444 / constants 14,016) 단일 호출 fixture로 native·cFS 양방향 재현: ADMIT_CONDITIONAL_MAP → append 피크 14,016(통과) → copy → 3/3·5/5 완주, 피크 **59,460 = 예산의 131%**, `peak_within_bounded=true` (`results/e29b_conditional_verify/`) | **조치 완료(E29b)**: append 전 정렬 검사(런타임 생성 전 거부, copy 할당 자체 없음) + append 후 `!= 0`(분기 판정). 정렬 셀 과잉 거부 0. E29 assertion 2건을 `!= 0`으로 정정(삭제 아님, 주석에 사유). **교훈**: *"시험이 무엇을 pin하는지 읽어라"* — 결함의 문자열을 pin한 시험은 결함을 지킨다 |
| D53 | **fail-open(유형 A), 설계 단계에서 차단**: 조건부 admission이 `per_call`을 근거로 승인해 놓고 **실제로 어느 분기를 탔는지 검증하지 않으면**, copy 분기에서 승인 예산의 172배를 할당하고도 계속 실행한다. 게다가 기존 텔레메트리는 이것을 잡지 못한다 — `peak_within_bounded`는 피크를 `CONTRACT_BOUNDED_BYTES`와 비교하지 **승인 근거가 된 예산**과 비교하지 않기 때문에 이 상황에서도 `true`다 | E29 조건부 계층(신규) | 검증 블록만 제거한 바이너리로 x86-64 native 실측: 예산 6,208 B, `ADMIT_CONDITIONAL_MAP` → `arm=copy` → `hal_device_bytes_peak=1069632`, `peak_within_bounded=true`, rc=0, 추론 3건 완주 (`results/e29_conditional_contract/revert_confirm_fail.txt`) | **출하 전 차단(E29)**: append 직후 HAL 피크를 읽어 `> CONTRACT_PER_CALL_BYTES`이면 추론 한 건 전에 거부(`MAP_PRECONDITION_FAILED`), 두 C 경로 모두. **교훈**: D52가 *"계약이 준 숫자를 게이트가 실제로 쓰는지 확인하라"*였다면 이것은 ***"어느 숫자로 승인했는지와 어느 숫자로 검증하는지가 같은지 확인하라"***다 |
| D52 | **fail-open(유형 A) — 그리고 이 저장소 핵심 논증 안에서**: cFS 태스크 스택 게이트(`ai_learner.c:203`)가 `stack_needed = AI_LEARNER_STACK_BASE_BYTES + CONTRACT_KERNEL_STACK_BYTES`만 계산했는데, 추론 경로가 **계약이 크기를 정하는 버퍼 셋**을 같은 스택에 잡았다 — `feat[]`(4 B×입력원소), `out[]`(4 B×출력원소), `outs[]`(**16 B**×출력원소+8). 게이트가 스택 충분성을 certify하고 admission이 ADMIT을 낸 뒤 **첫 추론에서 프로세스가 죽는다** | ai_learner.c(E12~) | OPS-SAT 입력 형상(224×224×3, 150,528 원소) 계약으로 실제 cFS 기동 → `accounted=true` → ADMIT → MATCH → **SIGSEGV(EXIT=139)**, 미계상 602,160 B vs 부여 262,160 B | **조치 완료(E28)**: 세 버퍼를 `static`으로(같은 파일 `:322`의 `zeros[]`가 이미 그러함) — 게이트를 가르치는 대신 **게이트의 기존 공식을 참으로** 만든다. 검토서 권고대로 게이트 공식에 I/O 바이트를 더하면 부여 공식이 정확히 base+kernel이라 **오늘 도는 모델이 전부 DENY**(유형 (B) 신규 생성)이므로 미채택. 버킷은 스택→BSS로 이동하며 둘 다 HAL 계약 밖이라 `bounded_bytes` 불변. revert-and-confirm-fail 양방향 실측. **교훈**: 지금까지가 *"신호의 부재를 신호로 읽지 말라"*였다면 이것은 ***"계약이 준 숫자를 게이트가 실제로 쓰는지 확인하라"***다 — 헤더에 `CONTRACT_INPUT_ELEMS`가 있고 코드가 그것으로 배열을 잡는데 게이트만 그것을 몰랐다 |
| D51 | **측정 결함(방법론)**: `e27_collect.py`의 첫 실행이 모델 파일명을 잘못 적어(`mlp16k_baked.mlir` — 실제는 `m16k_baked.mlir`) 하위 프로세스가 `FileNotFoundError`로 죽었는데, 수집기는 그것을 **`explicit_refusal`(정직한 거부)로 집계**했다. 즉 **측정 도구가 "볼 수 없었다"를 "보았더니 없더라"로 바꿔 기록**했고, 하필 **그 구분을 측정하는 실험 안에서** 일어났다 — 그대로 뒀다면 (a)가 정상 조건에서 정직하게 거부한 것처럼 보였을 것이다 | e27_collect.py(E27 세션) | 결과가 §1의 사전 관측과 어긋나 경로를 직접 확인 | **조치 완료(E27)**: `tool_error` 상태 신설, 입력 파일 존재 선확인, 하위 프로세스 traceback을 거부와 구분, 요약에 `collection_clean` 불리언과 별도 경고. 같은 부류로 (d) 열이 `device_bytes_peak`/`hal_device_bytes_peak` 두 키 중 하나만 읽어 **배포 하나가 조용히 누락**되던 것도 함께 수정. 회귀 시험이 `collection_clean`을 고정한다 |
| D50 | **측정 결함(방법론)**: pip `iree.runtime`으로 HAL 피크를 잴 때 **반환된 출력 버퍼를 붙들고 있으면** 이전 호출의 출력이 해제되지 않아 `device_bytes_peak`가 부풀어 오른다. ResNet에서 출력 4개를 유지하니 309,416 → **309,576**(= per_call + 4×40 B)이 되었고, `e26_collect.py::branch_of`가 그것을 **`refutes_hypothesis`**로 판정했다 — B0 37셀에서 한 번도 없던 값이라 하마터면 *"실물 CNN이 분기 가설을 반증했다"*는 틀린 헤드라인이 나갈 뻔했다 | 측정 하네스(E26e 세션) | 통계를 단계별로 분리해 직접 재측정 (모듈 적재 후 0 → 1회 호출 후 309,416) | **조치 완료(E26e)**: 호출마다 결과를 놓아주고(= `native_learner.c`가 하는 것) `allocated == freed`인 상태에서 피크를 읽는다. 회귀 시험이 pip 셀의 피크가 per_call과 정확히 같은지 고정한다. **교훈**: HAL 통계는 *관측자가 무엇을 붙들고 있는지*에 반응한다 — E26의 "HAL 통계는 프로세스 전역"과 같은 계열의 두 번째 얼굴이다 |
| D49 | **과잉 거부(유형 B)**: `stream.resource.subview`가 `static_mem_bound.py`의 `_KNOWN_ENTRY_OPS`와 `mlir_alloc_walk.py`의 `KNOWN_ENTRY_OPS` 어디에도 없어, D13의 fail-closed 규칙이 그것을 `unresolved`로 밀어 넣고 `bound_method`가 `UNKNOWN_BOUND`가 됨. **IREE는 다중 결과를 하나의 external 슬랩에 패킹한 뒤 subview로 쪼개므로, 사실상 모든 다중 출력 모델이 계약을 만들 수 없었다** — 정보가 없어서가 아니라 있는 정보를 쓰지 못해서 | static_mem_bound.py(E15~), mlir_alloc_walk.py(E18~) | 스톡 2출력 모델을 한 번의 `iree-compile`로 컴파일해 직접 재현. 지침 도입 조사(C-1)와 별도 반박 검증이 **독립적으로 같은 결론**에 도달했으나 어느 쪽도 액면 그대로 받지 않고 직접 확인 | **조치 완료(E26c)**: 두 화이트리스트에 추가하되 `tensor.export`·`dealloca`와 달리 **검사를 붙였다** — 세 index 피연산자가 전부 상수로 풀리고 `offset + result_size <= source_size`여야 통과, 아니면 `unresolved`. 구조적 추출기의 피연산자 순서 `[source, source_size, offset, result_size]`는 실제 op에 MLIR API를 걸어 읽었다. 실물 근거 `harness/gen_model_multiout.py` + `results/e26c_multiout/`(D43 규칙), HAL 피크 704 = bounded 704. **남는 제약**: C 헤더 생성기는 여전히 단일 f32 in/out만 허용하므로 다중 출력은 *계약은 되고 C 배치는 거부* — 두 C 실행기가 실제로 그렇게 가정하므로 정확한 진술이다 |
| D48 | **과잉 거부(유형 B)**: `static_mem_bound.py:229`가 embedded `.rodata` 세그먼트를 "백틱이 있으면 문자열 라벨"로 판별했는데, `iree-dump-module`은 내용이 인쇄 가능해 보이면 백틱 사이에 렌더링하므로 **첫 바이트가 NUL인 진짜 상수 블록이 빈 백틱 쌍으로 출력돼 관측에서 누락**. 연쇄: 계약값과 모순 → D28 상수 게이트 거부 → `--allow-unconfirmed-constants`로 뚫으면 `verification_grade: overridden` → D39 헤더 게이트가 다시 거부. 정직한 f32 모델의 배치 경로가 닫힘 | static_mem_bound.py(E23~) | 벤치마크 도입 조사 중 f32 모델에서 재현 → `iree-dump-module` 출력 직접 대조 | **조치 완료(E26b)**: 라벨의 **존재**가 아니라 **길이**로 판별(`len(label)==nbytes`). 저장소 전 산출물의 백틱 rodata 줄 137개 중 136개가 이 성질을 만족하고 유일한 예외가 오분류되던 그 세그먼트. 모호하면 데이터로 분류(과다 계상은 크로스체크가 거부해 보이고, 과소 계상은 조용히 통과하므로). 시험 8건, 실물 vmfb 보존. **부수 교훈**: 이 시험의 첫 작성본은 revert 시 `AttributeError`로 스위트 전체를 죽였다 — D24/D32와 같은 부류이며, **결함을 재현하려고 되돌리는 순간이 정확히 그 조건이 발생하는 때**다 |
| D47 | **과잉 거부(유형 B)**: `elf_stack_frame.py`의 `classify()`가 *"resolve targets (imports/runtime) before classifying"*이라고 스스로 지시해 놓고 **그 해석 단계가 구현된 적이 없어**, 호출 명령이 하나라도 있으면 bucket (3)/(4)로 분류 → `gen_contract_header.py`(E21/D22)가 배치 가능한 헤더를 거부. 정직한 공개 CNN이 통째로 막힘. 부수로 `make_contract.py`의 `kernel_external_call_insns`가 이름과 달리 **내부 호출 포함 총 호출 수**로 채워지고 있었음(헤더 게이트는 그것을 "콜리 스택을 볼 수 없는 호출"로 읽음) | elf_stack_frame.py(E14~), make_contract.py | 벤치마크 지침의 실제 워크로드 요구를 따르다 MLPerf Tiny ResNet에서 재현 → objdump/readelf로 80개 call의 타깃 2개가 전부 `.text` 내부·`.plt` 없음·미정의 심볼 0·헬퍼는 프레임 0 B leaf임을 직접 확인 | **조치 완료(E26a)**: 한 방향 해석(미해석→해석만 가능)을 구현하고, 간접 호출·범위 밖·꼬리 호출·동적 alloca·간접 분기·재귀·해석 부재는 전부 거부 유지. 콜리 범위는 CFG 순회로 발견(첫 구현의 "첫 ret까지" 절단은 이 ResNet 헬퍼에서 거짓 거부를 냄 — 자기 첫 ret을 건너뛰는 전방 분기 보유). 시험 12건, revert 시 11건 실패. **교훈**: E14 14개와 E25 canonical이 전부 `total_call_insns=0`이라 **합성 모델셋으로는 원리적으로 재현 불가능**했다 — 도구의 한계는 측정한 모델 집합의 함수이며, 워크로드의 외적 타당성은 결과의 문제일 뿐 아니라 **도구 검증 범위의 문제**다 |
| D46 | **재현성**: `.gitignore:7`의 포괄 규칙 `*.log`에 대한 예외가 `results/e14_aarch64_qemu/**/*.log`에만 있어 **E25의 AArch64 게스트 원시 로그(시리얼·드라이버·qemu stderr)가 저장소에 커밋되지 않고 있었다.** 그런데 EVIDENCE_v0.22 §7은 그 디렉터리를 원시 근거로 가리킨다 — **문서가 가리키는 증거가 클론에 없는 상태**. E22의 F9(회귀 시험이 요구하는 `dump/`가 같은 규칙으로 빠져 있었음)와 같은 계열 | .gitignore(E25 시점) | 외부 검토 §5의 "README 동기화" 항목을 처리하다 발견 → `git ls-files`·`git check-ignore -v`로 확인 | **조치 완료(v0.22.1)**: `!results/e25_equivalence/**/*.log`(+ 선제적으로 `!results/e26_boundary_utility/**/*.log`) 추가, 로그 5개 커밋. 부수 확인: 시도 2의 시리얼 로그는 `scripts/71:24`의 `: > serial.log`로 시도 3이 덮어써 **복구 불가** — §7의 "emergency 2회" 중 두 번째는 산출물로 검증되지 않음을 EVIDENCE §11.8과 BOOT_LOG에 증거 등급으로 명시(철회 아님). **교훈**: 새 results 디렉터리를 만들 때 `.gitignore`의 포괄 규칙에 대한 예외를 함께 넣지 않으면, 그 실험의 원시 근거는 문서에만 존재하게 된다 |
| D45 | **방법론**: 서로 **다른 종류의 관측**(출력 동치 vs 메모리 soundness)을 같은 지표 이름(`both_sound`)에 귀속. E25 마무리 시 `cross_target_compare.py`의 정의를 다시 읽지 않고 E14 §11.4의 요약("cross-target 비교는 실행 대조가 아님")만 기억해 "실행 대조를 했으니 채워졌다"로 건너뛰었다. CLAUDE.md "가장 중요한 교훈"의 변종 — *두 관측이 모순되지 않는다*가 *다른 종류의 관측까지 닫혔다*로 번진 형태 | EVIDENCE_v0.22 §6.4 · CHANGELOG · CLAUDE.md · EXPERIMENT_LOG(E25) | 외부 검토(REVIEW_v0_22_E25 §4.2) → `cross_target_compare.py:201` 코드와 E25 cFS 로그의 stage 목록 대조로 확인 | **조치 완료(v0.22.1/E25b)**: EVIDENCE §11.3 정오표 + 살아있는 문서 4곳 정정 + 반증 원장 등재. 실제 `both_sound`를 채우는 것은 E26에서 `--native-summaries`로 수행한다. **부수 교훈**: 지표 이름이 무엇을 종합하는지는 그 지표를 만든 코드가 정본이며, 다른 문서의 요약을 근거로 채워졌다고 판단하지 않는다 |
| D44 | **방법론**: D35의 원인을 "`CFE_ES_AppInfo_t.StackSize`가 unsigned라 비교가 항상 참"이라고 서술했으나 실제 코드는 `es_stack`·`stack_needed` 둘 다 signed `long`이고 `info.StackSize`는 명시적 `(long)` 캐스트다. **인과가 반대로 틀렸다** — 실제 unsigned 비교였다면 `-37856`이 1.8e19로 wrap돼 현실적인 모든 스택에서 오히려 거부됐을 것이다. 그런데 그 서술에 "실제 C 식으로 컴파일해 확인"이라는 표시가 붙어 있었다(작업 규율 4가 경계하는 형태). 7곳에 전파 | CLAUDE.md·CHANGELOG·EXPERIMENT_LOG·EVIDENCE_v0.20·코드 주석 2곳(E24b) | 외부 검토(REVIEW_v0_20_E24b F5) → 실제 C 식과 그 반사실을 각각 컴파일해 확인 | **조치 완료(E24c)**: 살아있는 문서 4곳 직접 정정, EVIDENCE_v0.20 §12 정오표, 코드 주석 2곳. **가장 위험했던 것은 대소문자 무시 grep으로만 잡히는 `gen_contract_header.py`의 음수 스택 가드 바로 위 주석** — 살아있는 fail-closed 가드의 존재 이유를 설명하는 자리다. "`(long)` 캐스트를 unsigned 해석을 근거로 제거하지 말 것" 경고 추가(제거 시 정상값 262335 포함 전부 거부됨을 실측). 판정(항등식·claim blocker)과 코드 수정은 유지되며, `es_stack = -1`(GetAppInfo 실패)에서도 참이라는 사실이 추가로 확인됨 |
| D43 | D38(24세그먼트 절단 제거)을 정당화한 ">24 세그먼트가 정상 컴파일에서 나온다"는 주장의 **실물 IREE 산출물이 저장소에 없어** 저장소 내용만으로 재현 불가. 수치도 부정확(31은 모델의 상수 개수이고, 이 저장소 추출기 기준 세그먼트는 33개) | EVIDENCE_v0.20·EXPERIMENT_LOG·CLAUDE.md·코드 주석 2곳(E24b) | 외부 검토(F4) → **재현 에이전트는 "3~5개, 재현 안 됨"으로 보고했으나 상수 수가 적은 기존 모델을 쓴 결과였고, 직접 확인 시 재현됨** | **조치 완료(E24c)**: `harness/gen_model_manyconst.py` 신설(16x32 tail로 두 번째 dispatch 강제 — 없으면 dump 파일명에 mlir basename이 없어 one-invocation 검사가 정당하게 거부), `results/e24c_manyconst31/`에 한 번의 호출 산출물 전체 보존(440 KB, dump는 축소본이며 계약이 모든 수치 동일하게 재생성됨을 확인). **실물 입력으로 현재 True / 옛 절단 False 실증**, 6건 회귀 시험 |
| D42 | `subset_sum_match`의 예산 초과 `None`이 기본 거부되지 않아, D25("관측 불가", 거부)와 N1("관측했더니 모순", 거부) 사이의 **같은 증거 상태**가 통과. 네 가지 `None` 사유를 구분하는 기계 판독 가능한 필드도 없어 자유 텍스트 note로만 존재 | make_contract.py(E24b) | 외부 검토(F3) → 코드 대조 + in-process 재현 | **조치 완료(E24c)**: **리뷰 처방(예산 초과 기본 거부)은 총량이 정확히 한 세그먼트인 정직한 >256 MiB 모델을 거부**해 미채택 — 결정 절차(정확 일치 O(n), 후보 20개 이하 전수 열거)를 예산 검사보다 **앞에** 두고 둘 다 실패할 때만 `None`. 무작위 3,000건 brute force 교차검증 0 불일치. `constants_confirmation_state` 5-상태 신설(스키마 선택 필드), `unevaluable_budget_exceeded`는 `--allow-unverified-invocation`으로만 우회 |
| D41 | D36(E24b/R2)의 합 항등식이 `is_int(_pc) and is_int(_cb) and ...` 형태라 **두 값이 우연히 정수일 때만** 실행 — `null` 구성요소는 검사를 무력화하고, 비음수 검사가 없어 `per_call = bounded+1, constants = -1`이 항등식을 **공허하게 만족**시킨다. 셋 다 `BOUND_KNOWN=1` 헤더. 이 저장소가 이미 세 번 고친 그 모양(D25·D28·D29)이 게이트 안에 있었음 | gen_contract_header.py(E24b) | 외부 검토(F2, **서술은 부분적으로만 정확** — "둘 다 정수일 때만"은 첫 변종만 설명하고 두 번째는 비음수 검사 부재가 원인) → 두 변종 실제 재현 | **조치 완료(E24c)**: `bound_known`일 때 두 구성요소가 비음수 정수임을 **요구**. 실측으로 좁힌 세 성질 — 스코프 유지(풀면 A8 입력 `dynamic` 2개 거부), `>= 0`이지 `> 0` 아님(constants==0은 정당), `is_int`를 요구 조건으로. 스키마 바이트 필드 `minimum: 0` |
| D40 | `CLAUDE.md`가 스모크 테스트로 문서화한 `cd native && bash build.sh`와 `scripts/62_compile_and_check_aarch64.sh`가 둘 다 `gen_contract_header.py`에서 rc=1로 죽고 있었음. 원인 3개 중 2개는 E15부터의 기존 문제(예시 계약에 ELF 스택 분석 없음 / 인라인 계약이 `bound_method` 미선언), 1개는 **E24의 N3 dtype 게이트가 만든 유형 (B) 회귀** | native/build.sh, scripts/62(E15~), gen_contract_header.py(E24) | R5 검증 중 별개로 발견 → E24b 이전 생성기로도 rc=1임을 확인해 원인 분리 | **조치 완료(E24b)**: 두 스크립트가 `--allow-unknown-stack` 전달, `scripts/62` 인라인 계약이 `bound_method`·dtype 선언. `documented_smoke_path_cases()` 신설(스크립트 미실행, 툴체인 없는 환경에서도 동작). **교훈**: "과잉 거부 위험을 측정했다"는 어떤 입력 집합에서 측정했는지에 전적으로 의존 — E24는 커밋된 계약 파일만 봤고 스크립트가 런타임에 만드는 계약을 보지 않았다 |
| D39 | 적용된 `--allow-*`/`--no-validate` override가 계약에 아무 흔적을 남기지 않아, ABI·triple·ELF·one-invocation 검사를 **전부 우회한 계약**과 전부 통과한 계약이 free-text `provenance.notes`를 직접 diff하지 않는 한 구별 불가. 게다가 그 계약의 유일한 소비자 `gen_contract_header.py`가 provenance를 **한 글자도 읽지 않아** 둘이 같은 배치 가능 헤더를 만듦 | make_contract.py, gen_contract_header.py(E15~) | 외부 검토(REVIEW_v0_19_E24_RESEARCH_REFRAME §5.2 R5) → 실제 override 계약으로 헤더 생성해 재현 | **조치 완료(E24b)**: `waive()`가 **거부 지점이 아니라 플래그 접근**을 감싼다(기계적 패치는 결합형 게이트 2곳을 놓치고, 그러면 override가 적용됐는데 `verification_grade="verified"`라 주장하는 신규 fail-open이 됨). `provenance.overrides_applied`/`verification_grade` 신설(스키마 **선택** — 필수화하면 보관 14개가 무효), 헤더 생성기가 override 계약을 기본 거부(`--allow-override-contract`) + `CONTRACT_PROVENANCE_VERIFIED`. 드리프트 주입 시 행위 시험 5건은 전부 PASS이고 소스 수준 가드만 FAIL함을 실측 |
| D38 | `subset_sum_match`가 세그먼트 24개에서 절단 — (a) 정상 단일 컴파일로 31개 세그먼트가 나오는 모델(스톡 플래그 `--iree-stream-resource-max-allocation-size=1024` 하나면 충분)에 대해 **계약 안에 "NOT matched"라는 거짓 진술**을 쓰고 `cross_target_compare.py`가 퍼뜨림, (b) 26개 이상에서 모순을 통과(경계 정확히 25개로 실측). 두 상태(`False`=모순 / 열거 포기)가 같은 값으로 붕괴 | make_contract.py(E15~) | 외부 검토(§5.2 R4, 리뷰가 유일하게 "가능"으로 표시) → 스톡 플래그로 31세그먼트 실제 생성 | **조치 완료(E24b)**: **리뷰의 문자 그대로의 처방("절단은 unevaluated로 분리하고 기본 거부")은 정직한 31세그먼트 모델을 통째로 거부해 미채택.** 열거기를 진짜 tri-state로(개수 절단 대신 `total`로 가지치기한 비트셋 DP, 예산은 총합 256 MiB). N1 게이트의 carve-out 2개 소멸. `dynamic` 계약 2개의 6개 스칼라 의도적 재생성(EVIDENCE_v0.19 §7이 예고한 비용) |
| D37 | `gen_contract_header.py`의 `shape_from()`이 fallback 체인이라 `validity`와 `interface`의 shape 표현을 **비교한 적이 없음** — 두 표현이 충돌해도 `KNOWN` 헤더 생성 | gen_contract_header.py(E14~) | 외부 검토(§5.2 R3) | **조치 완료(E24b)**: `check_shape_agreement()`를 `shape_from()` 전에 실행, **존재하는 statement끼리만** 대조(부재는 불일치가 아님 — `contract.e14_aarch64.json`은 `interface` 블록이 없는 정직한 계약이고 양쪽 존재를 요구하면 유형 (B)). dtype 절반은 **반박** — E24/N3이 이미 두 블록 합집합을 읽으므로 이미 거부됨 |
| D36 | `bounded_bytes`가 **같은 계약이 명시한 구성요소 합**과 대조되지 않음 — `bounded_bytes=1`이 `per_call=1352`+`constants=2176` 옆에 있어도 `BOUND_KNOWN=1`이고 C 게이트는 셋 중 첫 번째만 읽음 | gen_contract_header.py(E15 D13) | 외부 검토(§5.2 R2) | **조치 완료(E24b)**: `bound_known` 범위에서 `bounded == per_call + constants` 강제. **`>=`가 아니라 `==`** — `make_contract.py`가 셋을 같은 dict 리터럴·같은 `all_static` 게이트에서 계산하므로 등식이 항등이고(저장소 정수 계약 전수 확인), `>=`는 거부 집합이 같으면서 부풀린 자기모순 값만 통과시켜 정당한 ADMIT을 NOT_ADMITTED로 만듦(유형 B) |
| D35 | `--elf-analysis` JSON의 **음수 스택**이 부호 검사 없이 계약에 복사되고 `elif stack_b:`의 truthiness가 음수를 "신뢰" 버킷으로 승격 → `KNOWN` 헤더 생성. 실제 C 식으로 컴파일해 확인: `stack_needed = 262144 + (-300000) = -37856`이고 `es_stack`·`stack_needed`가 모두 signed `long`인데 `stack_needed`가 음수라 `es_stack >= stack_needed`가 **스택 0에서도, 그리고 `GetAppInfo` 실패 시의 `es_stack = -1`에서도 참** — E16/D15의 거부 분기가 조용히 멈추는데 텔레메트리는 `accounted=true`를 계속 보고. 스키마에 `minimum` 없음 | make_contract.py, gen_contract_header.py, contract.schema.json(E15~E16) | 외부 검토(§5.2 R1, "매우 낮음: 수동 변조 필요"로 분류) → **검증자가 계약을 손대지 않고 `--elf-analysis` 입력만 바꿔 정상 파이프라인으로 재현, claim blocker로 상향** | **조치 완료(E24b)**: 생성기가 음수 스택 거부(우회 플래그 없음), `make_contract.py`가 ELF 분석의 음수를 애초에 거부, `stack_b is not None and stack_b > 0`. **`<= 0`은 쓰지 않음** — `stack == 0`은 정당하고(dispatch 없는 ELF의 `max(..., default=0)`) `<= 0`은 `dynamic` 계약 2개(A8 입력)도 거부. **정정(E24c/F5)**: 원인 서술이 틀렸다 — unsigned 승격이 아니라 `stack_needed` 음수화다(양쪽 signed `long`, `info.StackSize`는 명시적 `(long)` 캐스트). 인과가 반대로 틀렸음도 실측 확인 — 실제 unsigned 비교였다면 `-37856`이 1.8e19로 wrap돼 오히려 **거부**됐을 것이다. 판정(항등식·claim blocker)과 수정은 유지 |
| D34 | E24의 EVIDENCE·README·CLAUDE.md가 `full` 레그 수치를 **143/143**으로 기록 — 그것은 이 개발 컨테이너의 값이고(PyYAML이 시스템 패키지로 존재), `pip install -r requirements.txt`만 한 fresh clone의 값은 **142/142 + 1 SKIP**이다. 또 `without-iree` 레그를 E23 시점 값(48/48+6)으로 인용했으나 실제로는 58/58+9이며 `stdlib-only`와 동일하다(IREE 도구가 없으면 `jsonschema`를 쓰는 경로가 이미 전부 SKIP되므로 두 레그가 같은 것을 측정한다). **F9(E22)·D25(E23)에 이은 같은 부류의 세 번째 반복** — "내 환경에서 돌려봤다"를 fresh checkout의 근거로 쓴 것 | docs/EVIDENCE_v0.19_E24.md, README.md, CLAUDE.md, CHANGELOG.md, EXPERIMENT_LOG.md(E24) | E24가 신설한 `stdlib-only` CI 레그의 첫 성공 실행 로그를 읽다가 발견 — **그 레그가 잡아낸 첫 대상이 E24 자신의 문서 수치였다** | **조치 완료(E24)**: 다섯 문서의 수치를 전부 CI 실측값으로 정정하고, 세 조건을 조건과 함께 병기. PyYAML은 `requirements.txt`에 추가하지 않음 — 워크플로우 YAML 검사는 본질적으로 푸시 전 로컬 가드이고(파일이 깨지면 CI는 애초에 실행되지 않는다) 그 하나를 위해 배포 의존성을 늘리는 것은 균형이 맞지 않는다. EVIDENCE_v0.19 §6.2에 방법론 교훈으로 기록 |
| D33 | E24가 CI 워크플로우에 추가한 `stdlib-only` 레그의 `run:` 스칼라에 `": "`(콜론+공백)이 들어간 Python 문자열이 있어 **워크플로우 파일 전체가 YAML 파싱 불가** — 푸시 즉시 실패했고, 실행 이름이 워크플로우 이름 대신 파일 경로로 표시됐다(단계 하나도 시작되지 않은 파싱 오류의 신호). **CI가 구조적으로 시험 실패로 보고할 수 없는 유일한 결함 부류** — 어떤 시험이 실패했는지 찾으면 아무것도 나오지 않는다 | .github/workflows/contract-negative-tests.yml(E24) | 자기 커밋의 CI 실행이 즉시 실패 → 로컬 `yaml.safe_load`로 line 53 col 60 확인 | **조치 완료(E24)**: 해당 스칼라에서 콜론+공백 제거. `workflow_yaml_cases()` 신설 — `.github/workflows/*.yml`이 파싱되고 `name`·`jobs`를 선언하는지 검사(PyYAML 부재 시 SKIP, 저장소 의존성 아님). 깨진 YAML로 되돌려 시험이 실제로 그 오류를 잡음을 확인. D24·D25와 같은 부류(하네스가 자신이 덮어야 할 조건을 볼 수 없었음) |
| D32 | 시험 하네스가 `jsonschema` 부재 환경에서 **15건의 거짓 FAIL**을 냄(98/113) — `make_contract.py`가 E15/D13에 따라 rc=3으로 하드 실패하는데, 그 15건은 ABI 불일치·낡은 dump-dir 등 **다른 것**을 검사하는 케이스다. CI의 `without-deps` 레그가 `jsonschema`를 설치해 이 조건을 한 번도 시험하지 않았고, `scripts/99_bootstrap_all.sh` 경로는 그것을 설치하지 않아 도달 가능했다 | contract_negative_tests.py, scripts/30_setup_iree.sh, CI(E22~E23) | 외부 검토(REVIEW_v0_18_FOLLOWUP N6) → 스텁 모듈로 import 차단해 재현, pip 없는 venv로 진짜 무의존성도 실측 | **조치 완료(E24)**: `schema_validator_available()` 신설 + `with_structural_override()`가 `--no-validate`를 투명 부착(블록 통째 SKIP은 jsonschema가 불필요한 43건까지 버려 채택 안 함). E15/D13 분기 자체의 시험 신설(그 분기는 어디에서도 시험되지 않았음), 부트스트랩에 jsonschema 추가, CI에 `stdlib-only` 레그 신설. CI 실측 142/142+1 · 58/58+9 · 58/58+9 |
| D31 | E23이 D27로 추가한 OnAIR 바인딩 게이트가 **저장소 기본 fixture를 거부** — `plugins/compiled_learner/runtime/contract.json`에 게이트 필수 필드 `artifact.bytes`가 없어 기본 실행 경로가 즉시 `ArtifactBindingError`. E23의 단위 시험이 E14 계약을 써서 이 통합 경로를 건드리지 않아 125/125가 통과했다. **결함 유형 (B) 과잉 거부** | plugins/compiled_learner, gen_model.py, contract.schema.json(E23) | 외부 검토(REVIEW_v0_18_FOLLOWUP N5) → 커밋된 fixture를 그대로 게이트에 넣어 재현 | **조치 완료(E24)**: `gen_model.py`를 **먼저** 수정(`sweep.py`가 그 디렉터리를 매 스텝 재작성하므로 데이터만 고치면 휘발성) → fixture에 `bytes: 10642`(재컴파일 없음, sha256은 이미 실물과 일치) → 스키마 `artifact.required`에 추가(17개 계약 전수 통과). stdlib 전용 독립 시험 함수 신설 |
| D30 | `harness/gen_contract_header.py`가 dtype을 **선택적 확장 필드**인 `interface.inputs/outputs`에서만 읽어, 그것이 없으면 dtype 집합이 빈 집합이 되고 게이트·매크로가 둘 다 `dtypes - {"f32"}`를 검사하므로 **빈 집합과 `{"f32"}`가 구분되지 않음** — 스키마 필수 필드인 단수형이 f16이라 말하는 계약이 `CONTRACT_DTYPES_ALL_F32=1`로 통과(공허참). 복수형이 f32여도 단수형이 f16이면 통과하는 변종도 있음 | gen_contract_header.py(E21 D23) | 외부 검토(REVIEW_v0_18_FOLLOWUP N3) → 보관 계약 변형 3종으로 재현 | **조치 완료(E24)**: 스키마 필수 단수형 dtype을 집합에 합치고 매크로에 `dtypes and` 요구. **strict 변종(복수형 부재 시 거부)은 실측 과잉 거부로 채택 안 함** — 이 저장소 스키마가 선언하는 정본 형태를 거부한다. 단수형을 `validity`에서도 읽도록 함(`shape_from()`과 같은 규칙 — 이걸 빠뜨리면 `contract.e14_aarch64.json`이 거부됨) |
| D29 | `harness/gen_contract_header.py`의 스택 신뢰 판정이 세 신호를 `bool()`/`not in (None,...)`으로 읽어 **필드 부재와 '신뢰함'이 구분되지 않음** — `kernel_stack_classification` 키만 지워도(또는 `null`로 두어도, 세 키를 전부 지워도, `kernel_external_call_insns`만 지워도) `KERNEL_STACK_BYTES_KNOWN=1` 헤더가 바이트 동일하게 생성. E21/D22가 물은 것은 "분석이 불신을 표명했는가"인데, 부재는 그 질문에 답할 수 없으므로 UNKNOWN이어야 한다 | gen_contract_header.py(E21 D22) | 외부 검토(REVIEW_v0_18_FOLLOWUP N4) → 보관 conv2d 계약에서 키만 제거해 재현 | **조치 완료(E24)**: 세 신호를 `is not False`/`!= 0`/`not in (...)`로 바꿔 `None`을 명시적 불신뢰로. `bound_known` 조건부 범위 제한은 넣지 않음(주변 게이트가 이미 그 범위를 제공하고, 넣으면 dynamic 경로 fail-open이 되살아남 — 반박 검증 2인이 각각 실측). `and is_int(stack)` 가드가 no-ELF 경로의 기존 오진도 함께 수정 |
| D28 | `harness/make_contract.py`가 (a) 비어 있지 않은 dump-dir에서 링크된 ELF만 없으면 `dump_elf_in_vmfb=None`이 되는데 어느 분기도 None을 보지 않아 exit 0 + notes 빈 리스트 + **배치 가능한 헤더** 생성(E21/F1은 *완전히 빈* dump-dir만 막았음), (b) 상수량의 아티팩트 측 관측이 IR과 **모순**돼도(`consts_confirmed=False`) 거부하지 않음 — E23/D25가 더 약한 증거인 "관측 불가"는 거부하는데 더 강한 "관측했더니 모순"은 통과시키는 비대칭. (a)는 다른 모델의 vmfb를 짝지어도 계약이 나와 **D10 구멍이 None 경로로 재개방**됨 | make_contract.py(E21 F1, E23 D25) | 외부 검토(REVIEW_v0_18_FOLLOWUP N1·N2) → 보관 dump-dir에서 `.so`만 제거 / 가짜 `iree-dump-module`로 재현. 도달 경로 확인: 메타 플래그 대신 구성요소 dump 플래그만 주면 이 모양이 나옴 | **조치 완료(E24)**: None 전용 catch-all 신설(False는 기존 우회 불가 분기 유지 — 섞으면 검증된 불일치를 우회 가능한 것처럼 오도), `single_invocation`을 `is True`로, 계산만 하고 버리던 두 신호를 provenance에 기록. 상수 모순은 carve-out 3개(const==0 / 세그먼트 24개 초과 / D17 정렬 패딩)로 좁혀 거부 + 별도 `--allow-unconfirmed-constants`. **리뷰의 문자 그대로의 수정은 보관 14개 중 2개를 거부**함을 실측 |
| D27 | OnAIR `CompiledLearner._load_artifact()`가 계약이 지목한 `.vmfb`를 sha256·크기 검사 없이 그대로 IREE에 넘김 — 같은 저장소의 C 경로(`native_learner.c`, cFS 앱)는 크기 선검사→sha256→자원 획득 전 거부를 이미 하고 있어, A3(모델 교체)·A5a(손상)를 C는 잡고 OnAIR는 통과시키는 비대칭 | plugins/compiled_learner(E0~) | 외부 검토(REVIEW_v0_15_LATEST F10) → 수정 전 코드 5줄 확인 + 실제 손상 파일로 재현 | **조치 완료(E23)**: `plugins/compiled_learner/artifact_binding.py` 신설(stdlib 전용), `verify_artifact_hash=True` 기본, 계약에 `artifact.bytes`/`sha256`이 없으면 검사 생략이 아니라 거부. 해시한 바로 그 바이트를 IREE에 전달. 시험 6건(EVIDENCE_v0.18 §2) |
| D26 | A5b 시나리오가 `docs/EVIDENCE_v0.12_E17.md` §2.1이 서술한 구조 손상(module.fb FlatBuffer 길이 접두사)이 아니라 A5a와 **동일한** offset 4096 임의 bit flip을 사용 — 두 방식을 구분하는 필드 자체가 없어 저장소 코드만으로는 E17의 A5b를 재현할 수 없었음(E17 결과 자체는 수작업 구조 손상으로 얻은 것이라 반증되지 않음) | e14_make_scenarios.py, e14_cfs_scenarios.py(E14~E17) | 외부 검토(REVIEW_v0_15_LATEST F8) → 두 파일 대조 + 수정 전 코드로 revert-and-confirm-fail | **조치 완료(E23)**: `harness/corrupt_vmfb.py` 신설(두 방식 실제 구현, ZIP64 STORED 외과적 패치), `corrupt_method` 필수화(누락·미인식은 `ValueError`), 보관 vmfb 8/8 + `iree.runtime` 실제 로드 거부 확인(EVIDENCE_v0.18 §1) |
| D25 | `iree-dump-module` 바이너리 부재 시 `make_contract.iree_dump_module()`·`static_mem_bound.artifact_rodata_segments()`가 uncaught `FileNotFoundError`로 전체 크래시 — D24와 같은 부류의 두 번째 지점. **E22가 D24를 확인할 때 쓴 `sys.meta_path` import 차단은 Python 모듈만 숨기고 콘솔 스크립트는 PATH에 남기므로 이 조건을 원리적으로 재현하지 못했다** | make_contract.py, static_mem_bound.py, contract_negative_tests.py(E15~E22) | **E22가 만든 CI의 `without-deps` 레그가 첫 실행에서 실제로 실패**(사람이 아니라 CI가 발견) → `env -i PATH=/usr/bin:/bin` + import 차단으로 충실히 재현 | **조치 완료(E23)**: 두 함수가 `OSError`를 잡고, 후자는 `([],[])`가 아닌 `(None,None)` 반환(“관측 못 함”≠“관측했고 없음”). 상수 독립 확인 불가는 `null`로 기록 + 기본 거부(`--allow-unverified-invocation`로만 우회), `constants_check_note` 3-상태화. 하네스에 `iree_tools_available()` 신설(SKIP 처리). 세 환경 실측 125/125·95/95+3·50/50+5(EVIDENCE_v0.18 §5) |
| D24 | `harness/contract_negative_tests.py`가 `iree.compiler.ir` 부재 환경에서 uncaught `RuntimeError`로 전체 크래시(PASS/FAIL 요약 0줄 출력 없이 죽음) — `structural_walker_checks()`의 화이트리스트 축소 시험에 `except`가 없었음(다른 두 곳은 있었음). "96/96, 환경 구축 불필요" 주장을 fresh clone에서 재현 불가능하게 만든 핵심 원인 중 하나 | contract_negative_tests.py(E15~E21) | 외부 검토(REVIEW_v0_15_LATEST F9) → sys.meta_path 훅으로 패키지 부재를 가역적으로 시뮬레이션해 재현 | **조치 완료(E22)**: `Result`에 `skip` 상태 신설, 3개 지점을 명확한 SKIP으로 정리(FAIL도 크래시도 아님). 실제 git clone + 시뮬레이션으로 77/77+3 SKIP 확인(EVIDENCE_v0.17) |
| D23 | `native/cfs_app/fsw/src/ai_learner.c`·`native/native_learner.c`의 C 인터페이스 게이트가 이벤트 메시지·주석에서 "single-f32-in/out"을 검사한다고 서술하지만 실제 조건은 `CONTRACT_NUM_INPUTS/OUTPUTS` 개수만 검사 — dtype 매크로 자체가 헤더에 없어 "개수는 맞지만 dtype만 stale"한 손수정 헤더를 C가 잡을 수 없음(주석이 명시한 위협 모델 자체를 놓침) | native/cfs_app, native_learner.c | 외부 검토(REVIEW_v0_15_LATEST F7) → 코드 대조 + 직접 실행으로 확인 | **조치 완료(E21)**: `gen_contract_header.py`가 `CONTRACT_DTYPES_ALL_F32` 매크로 신설, 양쪽 C 게이트의 필수 매크로 가드·인터페이스 검사에 반영. 새 회귀 시험 4건, revert-confirm-fail로 실효성 확인(EVIDENCE_v0.16) |
| D22 | `harness/gen_contract_header.py`가 스택 값의 정수 여부만 확인 — `elf_stack_frame.py`의 `classify()`가 스스로 "신뢰 불가"로 표시하는 조건(동적 스택 할당, 미해결 외부 호출, bucket_3/4 분류)이어도 `CONTRACT_KERNEL_STACK_BYTES_KNOWN=1`로 헤더가 생성됨 | gen_contract_header.py | 외부 검토(REVIEW_v0_15_LATEST F6) → conv2d 계약을 적대적으로 편집해 직접 실행으로 확인 | **조치 완료(E21)**: `kernel_dynamic_stack_alloc`/`kernel_external_call_insns`/`kernel_stack_classification`이 불신뢰를 나타내면 `stack_known`을 강제 False(`--allow-unknown-stack`으로 통합 우회). 새 회귀 시험 4건, revert-confirm-fail(EVIDENCE_v0.16) |
| D21 | `harness/mlir_alloc_walk.py`의 `stream.resource.pack` 처리가 비상수 index operand를 `unresolved`에 넣지 않고 조용히 버림 — 형제 분기(`stream.tensor.import`/`stream.resource.alloca`)와 다른 비대칭 패턴, 코드 자신의 주석("unresolved ones are reported, not skipped")과 실제 동작 불일치 | mlir_alloc_walk.py(E18/E19) | 외부 검토(REVIEW_v0_15_LATEST F5) → IREE 상류 실제 문법으로 합성한 MLIR을 `ir.Module.parse()`로 파싱해 재현 | **조치 완료(E21)**: 형제 분기와 동일한 `(bucket if ok else unresolved)` 패턴으로 통일. 현재 배선(E19 크로스체크)에서는 정규식 파서와의 불일치로 이미 이중 방어되고 있었음이 검증 과정에서 확인됨(severity P1로 재평가). 재현을 고정 회귀 시험으로 등록, revert-confirm-fail(EVIDENCE_v0.16) |
| D20 | E19가 "MANDATORY cross-check"라 부르는 구조적 크로스체크가 `iree.compiler.ir` 미설치 시 조용히 정규식 단독으로 저하 — "mandatory"라는 표현과 실제 opportunistic 동작이 불일치 | make_contract.py(E19) | 외부 검토(REVIEW_v0_15_LATEST F3) → 몽키패치로 재현 | **조치 완료(E21)**: 패키지 미설치도 기본 하드 실패로 변경(`--allow-missing-structural-checker`로만 우회). 검증 결과 이 동작 자체는 E19 세션이 이미 문서화·시험했던 의도적 설계였음이 확인됨("은폐"라는 리뷰의 프레이밍은 과장, severity P1로 재평가) — 다만 "mandatory" 표현과의 정책적 불일치는 타당해 기본값을 변경. F9(재현성)와 상충하는 트레이드오프가 있어 E22에서 이어 다룸 |
| D19 | `make_contract.py`의 ABI 반사(`iree.abi.declaration`) 검사가 "있는데 다름"은 하드 실패, "아예 없음"은 note만 남기는 비대칭 구조 — 컴파일러 자신의 반사 크로스체크가 통째로 생략된 채 소스 시그니처만 신뢰 | make_contract.py | 외부 검토(REVIEW_v0_15_LATEST F2) → conv2d layout IR에서 abi.declaration 절만 제거해 실제 실행으로 재현 | **조치 완료(E21)**: `abi is None`도 `--allow-missing-abi-declaration`(신규)으로만 우회 가능한 하드 실패로 승격. 새 회귀 시험 2건, revert-confirm-fail(EVIDENCE_v0.16) |
| D18 | `make_contract.py`의 one-invocation 신호(`dump_elf_in_vmfb`/`stem_in_dump`/`layout_dispatches_in_dump`)가 3값 논리인데 거부 조건이 `is False`뿐 — `--dump-dir`가 완전히 비면 세 신호 전부 `None`이 되어 "불일치를 못 찾음"이 "동일 호출임을 확인함"과 동일하게 취급됨, 설명하는 note도 전무 | make_contract.py | 외부 검토(REVIEW_v0_15_LATEST F1) → 빈 dump-dir로 실제 실행해 재현 | **조치 완료(E21)**: `dump_files`가 비면 하드 실패(`--allow-unverified-invocation`으로만 우회). 새 회귀 시험 2건, revert-confirm-fail(EVIDENCE_v0.16) |
| D17 | E19의 구조적 크로스체크(constants(sum))가 계약이 실제 채택하는 `const_b`(packed_sum, 정렬 패딩/중복제거 포함)가 아니라 `dense_sum`(패킹 이전 per-tensor 합)과 비교됨 — 구조적 추출기의 constants 합은 항상 packed_sum을 재는데(stream.resource.alloc의 폴백 경로), 잘못된 기준과 비교해 정렬 패딩이 있는 완전히 정상적인 모델이 영구적으로 하드 거부됐을 것 | E19(`make_contract.py` 구조적 크로스체크) | 이 세션 내 적대적 다중 리뷰(4관점+반박검증) → mlp16k layout IR의 패킹 크기만 편집해 실제 재현 | **조치 완료(E20)**: 비교 기준을 `const_b`로 수정, `mlir_alloc_walk.diff_against_regex` 공유 헬퍼로 3곳(make_contract.py/CLI/시험 스위트) 중복 제거, 재현 시나리오를 고정 회귀 시험으로 등록, 수정 전 코드로 되돌려 시험이 실제로 실패함을 확인(EVIDENCE_v0.15) |
| D16 | E19의 구조적 크로스체크가 계약이 실제로 서명하는 `p`(lowering_score로 고른 값)가 아니라 `whole`(정규식 파서의 "파일 마지막 print=최다lowering" 낡은 가정에 의존하는 값, make_contract.py 자신의 docstring이 스레드 스케줄링에 좌우된다고 경고하는 바로 그 가정)과 비교됨 — 14개 보관 모델은 우연히 chosen==last여서 드러나지 않았을 뿐, print 순서가 바뀌면 완전히 정상인 모델도 하드 거부될 수 있었음 | E19(`make_contract.py` 구조적 크로스체크) | 이 세션 내 적대적 다중 리뷰(4관점 중 2관점이 독립적으로 수렴) → conv2d layout IR의 두 print 청크 순서만 교환해 실제 재현 | **조치 완료(E20)**: 비교 기준을 `p`로 수정, 재현 시나리오를 고정 회귀 시험으로 등록, 수정 전 코드로 되돌려 시험이 실제로 실패함을 확인(EVIDENCE_v0.15) |
| D15 | 스택 회계(`kernel_stack_accounted`)가 admission gate가 아니라 텔레메트리 — `accounted=false`에서도 거부 분기가 없고, 위치도 IREE 세션·입력버퍼·SB 파이프 생성 이후. 게다가 빌드 스크립트가 startup에 써넣은 `base+kernel`을 앱이 그대로 되읽어 `>=`로 비교하므로 구조상 항상 참(항등식). base 262,144 B는 "E12부터 써온" 관례 상수이며 저장소 어디에도 실측 스택 사용량 근거 없음 | E14-S1(§4.1·§5, cFS `AI_LEARNER`) | 외부 검토 2건(REVIEW R4 / OPINION §8.4) → 코드 대조로 확인 | EVIDENCE_v0.9 §11.5(§11.3 합격기준 #8 재판정)로 표현 축소. **조치 완료(E16)**: 스택 확인을 Init() 최선두(자원 획득 전)로 이동하고 `accounted=false`에서 실제 거부하는 분기 신설(EID_STACK_REJECT), x86-64 native_std 실기동으로 검증(EVIDENCE_v0.11 §3). base 262,144 B가 여전히 관례 상수라는 한계는 남음 |
| D14 | one-invocation 검증(D10 수정분)이 layout IR을 검사에 결합하지 않음 — bound를 만드는 모든 수치가 layout IR에서 나오는데 `single_invocation` 판정식(`make_contract.py:389`)에 layout IR 변수가 없음. `layout_ir_sha256`은 기록만 되고 무엇과도 대조 안 됨 → 다른 컴파일 호출의 layout IR + 올바른 vmfb/dump-dir 조합이 검사를 통과할 수 있음 | E14-S1 계약 도구화 | 외부 검토 2건(REVIEW R5 / OPINION §8.5) → 코드 대조로 확인 | EVIDENCE_v0.9 §11.7로 표현 축소("대표적 산출물 혼입 탐지"); **조치 완료(E15)**: layout IR이 참조하는 dispatch 심볼이 `--dump-dir`에 실제 파일로 존재하는지 검사, 위반 시 hard fail(EVIDENCE_v0.10 §2.3, §3) |
| D13 | 계약 생성기 체인이 fail-open — (a) `static_mem_bound.py`의 정규식 파서가 한 줄(`[^\n]*`) 한정이라 미인식 자원 할당 연산이 `unresolved`가 아니라 조용히 무시됨(줄바꿈만 넣어 재현: outputs/transient_slabs가 사라져도 `unresolved=[]` 유지), (b) `gen_contract_header.py`가 `bounded_bytes`의 부호를 검사하지 않아 음수가 `CONTRACT_BOUND_KNOWN=1`로 헤더에 실리고 C 게이트(`bounded<=budget`)가 무조건 ADMIT, (c) `bound_method`가 `"NONE"` 하나만 막는 블랙리스트(소문자 `none` 등 통과) | 계약 도구 전반(`static_mem_bound.py`, `make_contract.py`, `gen_contract_header.py`) | 외부 검토 2건(REVIEW R2·R3 / OPINION §8.2·§8.3) → 직접 재현으로 확인 | EVIDENCE_v0.9 §11.9로 기록; 보관된 14개 계약 값 자체는 vmfb 재대조로 14/14 일치(반증 아님). **조치 완료(E15)**: 파서에 줄바꿈 안전 정규식 + 미인식 op 화이트리스트 승격, 헤더 생성기에 bound_method 화이트리스트·비음수 검사·입출력 개수/dtype 검사 추가, 음성 시험 51/51 PASS, 14/14 회귀 없음(EVIDENCE_v0.10) |
| D12 | `static_mem_bound.py:239`의 독립 실행 경로에서 `all_static = len(unresolved)==0`이 `entry_found`를 누락 — entry 함수를 못 찾아 IR 전체를 파싱한 경우에도 `bound_method=static_from_stream_layout`을 출력, `admission_check.py:57`이 이를 받아 ADMIT 가능 | `static_mem_bound.py` 단독 실행 경로(`make_contract.py`의 `all_static`은 `entry_found`를 포함해 영향 없음) | 외부 검토 코드 대조 중 추가 발견 | EVIDENCE_v0.9 §11.9; **조치 완료(E15)**: `all_static = entry_found and not unresolved`로 수정, 단위 시험으로 확인(EVIDENCE_v0.10 §3) |
| D11 | EVIDENCE_v0.9 §4.1·§9와 CLAUDE.md가 A5b(계약 해시가 손상 파일을 가리키는 경우, `runtime_load_failed` 경로)를 "native 레벨에서 확인했다"고 서술했으나 실제로는 native·cFS 어느 레벨에서도 실행되지 않음(§3 표에는 A5a만 존재, 문서 자체가 자기모순) | E14-S1 문서화(§4.1·§9) | 외부 검토 2건(REVIEW R1 / OPINION §8.1) → native/cfs 로그·summary.json 전수 대조로 확인 | EVIDENCE_v0.9 §11.1로 정정(서술 철회), CLAUDE.md 갱신. **실제 재현 완료(E17)**: FlatBuffer root-uoffset 구조 손상으로 native x86-64·cFS x86-64·cFS AArch64 게스트 3레벨 전부에서 실행 — admission ADMIT/binding MATCH/runtime_load_failed로 안전 거부, 크래시 0(EVIDENCE_v0.12 §2) |
| D10 | 계약 생성기의 `provenance.single_invocation`이 하드코딩 `true` — one-invocation 규칙을 검증해야 할 도구가 실제로는 검증을 안 해 서로 다른 컴파일 호출의 산출물을 섞어도 통과시킴 | E14-S1 계약 도구화 | 적대적 리뷰(워크플로우) | dump-dir 실행파일이 vmfb에 임베디드돼있는지(sha256) + dump-dir 파일명에 입력 basename이 포함되는지, 두 신호로 실제 검증 추가 |
| D9 | v0.7/v0.8이 x86-64·AArch64 스택 프레임에 서로 다른 정의(x86=`sub`만, AArch64=callee-save만)를 적용 — "AArch64만 프레임이 있다"는 서술이 정의 불일치의 인공물이었음 | E14-S1(§5, `elf_stack_frame.py`) | 두 ISA에 같은 정의 적용해 재분석 | 통일된 정의(callee-save+지역=frame_bytes, +복귀주소=invocation_stack_bytes)로 EVIDENCE_v0.9 §5.1 정정 |
| D7 | 호출당 할당 = 초기화 비용 포함 상각값 | 검토 v0.6 §5 | 지표 의미 | 정상 상태 카운터 차이(65,544) |
| D6 | 계약이 아티팩트를 식별하지 않음 | 검토 v0.6 §3 | gate 보장 조건부 | sha256·크기 결합, 헤더 생성 |
| D5 | 전체 HAL 피크를 per-call 계약과 비교 | 검토 v0.6 §4 | summary 누락 | bounded 비교 |
| D4 | 세션 해제 전 모듈 데이터 free → 해제 시 segfault | E11 하네스 종료 | 측정값 무영향 | 해제 순서 수정 |
| D3 | 상한 = slice 합 → 정렬 패딩 무시로 과소 추정 (E9-A 48<128, E9-B 84<128) | v0.3–v0.4 상한 방법 | 검토 §7 예측 → E9 실증 | post-layout transient alloca 크기로 교체; MLP 60/60 재검증 |
| D2 | 정적 상한 파서가 initializer 내 상수 임포트를 입력으로 오귀속; 런타임 컨텍스트를 721 KB 과대 산정 | E6b 해석, contract 예시 | 외부 검토 §5·§6 + E6c | 엔트리 함수 스코프 파싱, 상수 별도 집계, 정오표 |
| D1 | 가중치를 호출 인자로 전달 → 매 호출 전체 가중치 디바이스 임포트 | E1–E5 | E6 HAL 통계 `bytes_per_call == 전체 입력 크기` | E6b 재측정; 이후 모든 하네스는 `bytes_per_call` 기록 의무 |

| "slice 합은 보수적 상한" | EVIDENCE v0.3 §1.1 / v0.4 §0 | E9: 정렬 패딩으로 과소 | v0.5 |
| "cFS 배치 전 admission 수행" | REPORT v0.4 | cFS 통합 미완; 검토 §8 문장으로 교체 | v0.5 |
