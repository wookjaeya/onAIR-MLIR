# EXPERIMENT LOG — onair-mlir-bench

규칙
- 실험 ID는 불변이다. 재실행은 새 ID(예: E5b)를 받는다.
- 각 항목은 `platform_grade`를 반드시 기록한다. FUNCTIONAL_ONLY 결과는 방향성 관측이며 논문 증거가 아니다.
- 판정 변경은 여기서 하지 않는다. 판정은 `docs/EVIDENCE_v*.md`에서 버전으로 관리하고, 여기에는 어느 문서가 판정했는지만 링크한다.
- 모든 항목은 git 커밋 해시로 산출물에 연결된다 (`git log --oneline`).

| ID | 날짜 | 실험 | 플랫폼 grade | 산출물 | 결과 요약 | 판정 문서 | 커밋 |
|---|---|---|---|---|---|---|---|
| **E25** | 2026-09-09 | OnAIR↔cFS 동일 모델·의미 동치 — 하나의 canonical 가중치에서 baked MLIR+weights.npz를 함께 생성하고 다섯 경로(NumPy reference / IREE Python 바인딩 / native C / cFS x86-64 / cFS AArch64 게스트)의 출력을 사전 고정 기준으로 대조 | 결정론적(출력 비트값·해시·계약 수치) | `harness/gen_model_canonical.py`·`harness/e25_compare.py`(신규), `native/native_learner.c`·`native/cfs_app/fsw/src/ai_learner.c`(게이트 뒤 파일 기반 동치 모드), `results/e25_equivalence/` | **PASS**. reference 대비 네 경로 합계 512/512 원소(경로당 128 = 입력 64 × 출력 2), argmax 경로당 64/64 (v0.22.1 정정: 이전 서술 "256/256"은 argmax 집계와 섞인 오기). **IREE 경로 6쌍 전부 비트 동일**(x86-64 셋은 같은 vmfb라 기대된 결과, **AArch64는 다른 vmfb인데도 비트 동일 — 계획이 예상하지 않은 결과이며 일반화하지 않음**). AArch64 반복 실행 비트 동일(결정적). 계약 값은 두 ISA 동일(786476/65580/720896/16), vmfb만 다름. **출발점**: 이전까지 OnAIR(`mlp_9x65536x2`, 외부 npz)와 cFS(`mlp16k`, baked)는 서로 다른 모델이었고 같은 것은 인터페이스뿐. **사전 고정 기준의 가치 실증**: telemetry regime은 abs 단독 1/64, normalized는 rel 단독 59/64 — 어느 한 기준만 요구했어도 정직한 결과가 FAIL. E14 cross-target 지표의 `out0_agreement` 자리를 **canonical 모델에 한해** 실제 출력 대조로 채움 (v0.22.1 정정: 이전의 "`both_sound` 갭 해소"는 오귀속 — `both_sound`는 메모리 관측 종합값이고 E25는 메모리를 계측하지 않았다, D45). 환경 실패 2건(게스트 emergency mode)은 §7로 분리 기록 | `docs/EVIDENCE_v0.22_E25.md` | (이 커밋) |
| **E25b** | 2026-09-09 | 일곱 번째 외부 검토(REVIEW_v0_22_E25) 반영 — E25 주장 범위 5건 정정 + 판정 도구를 사전 고정 기준(계획 §3.2)에 맞춤 | 결정론적(판정·수치·바이트 비교) | `harness/e25_compare.py`(쌍별 규칙: same_vmfb=비트 동일 / cross_vmfb=tolerance+argmax, `--vmfb` 필수·누락 시 거부), `harness/contract_negative_tests.py::e25_compare_rule_cases()` 8건, `results/e25_equivalence/build/comparison_all.pairrule.json`(신규, 원본 보존) | **확인·수정**. 검토 지적 8건 전부 저장소 대조로 사실 확인. **과잉 거부(유형 B) 직접 재현**: 한 원소 1 ulp 차(모든 원소가 abs·rel 기준을 각각 단독 16/16 만족)에서 옛 도구 `VERDICT: FAIL rc=1` → 새 도구 PASS. 보관 출력 재판정 판정·`vs_reference`·`argmax` 전부 불변, 규칙 배정 same 3/cross 3. 이 컨테이너 `--skip-regression` **156/156**. 정정 5건: 512 원소 · 누산 순서 철회 · `both_sound` 오귀속(D45) · "OnAIR-IREE" 명칭 · 계약 ISA 동일성 범위 | `docs/EVIDENCE_v0.22_E25.md` §11·§12 | (이 커밋) |
| **E26** | 2026-09-09 | 부분 메모리 계약 경계의 유용성 — 사전 고정 기준(`docs/plans/E26_boundary_utility.md`, 측정 전 커밋 `c8e8980`)으로 Q1 soundness · Q2 보수성 · Q3 admission 유용성 측정 | 결정론적(HAL allocator 통계·계약 수치·admission 판정). 지연값 미인용, RSS는 버킷 보고용 | `harness/e26_collect.py`(신규), `results/e26_boundary_utility/`(x86_64 native·cFS·pip_runtime, aarch64 native, comparison, summary.json) | **Q1 PASS**(21개 실행 셀 전부 `peak ≤ bounded`, 위반 0) · **Q3 PASS**(unsafe admit 0, `B−1`→DENY·`B`/`B+1`→ADMIT) · **Q2 정량화**(tightness 1.00×~45.50×). **기전 규명**: IREE가 상수를 `stream.resource.try_map`+`scf.if(%did_map)`로 감싸므로 `bounded`는 두 분기의 최댓값이고 soundness는 구조적으로 성립, tightness만 분기의 함수다. 같은 vmfb의 peak가 **런타임 배포에 따라 최대 45.5배** 달라짐을 네 배포에서 실측 — 런타임 계측 상한은 배포 종속, **정적 계약은 배포 독립**. 분기 결정 요인 4가지 실측 배제, 최종 요인은 미확정. **E14의 `both_sound: null`을 실제 실행으로 채움**(3모델 true, D45가 철회했던 항목) | `docs/EVIDENCE_v0.25_E26.md` | (이 커밋) |
| **E26c** | 2026-09-09 | 벤치마크 도입 조사가 남긴 잔여 결함 후보 **C-1**의 재현·수정(D49) — 다중 출력 모델이 `stream.resource.subview` 때문에 `UNKNOWN_BOUND` | 결정론적(layout IR 구조 분석·계약 수치·HAL 통계) | `harness/static_mem_bound.py`·`harness/mlir_alloc_walk.py`(subview 봉쇄 검사), `harness/gen_model_multiout.py`, `results/e26c_multiout/`, `harness/contract_negative_tests.py` 15건 | **수정**. IREE는 결과 2개를 **하나의 external 슬랩(128 B)에 패킹**하고 `subview` 2개(32 B @0, 16 B @64)로 쪼갠다 — 즉 파서는 유일한 실제 할당을 이미 건전·보수적으로(128 ≥ 32+16) 계상하고 있었고 거부는 순전히 화이트리스트 누락이었다. 두 추출기에 op을 추가하되 **검사 없이 신뢰하지 않는다**: 세 index 피연산자가 전부 상수로 풀리고 `offset + result_size <= source_size`일 때만 통과. **revert-and-confirm-fail 2단계** — 화이트리스트만 되돌리면 7건 FAIL(과잉 거부 실재), 검사 코드만 빼면 4건 FAIL(없으면 fail-open). 실측 HAL 피크 **704 B = bounded 704 B**(tightness 1.00×), 오버라이드 0개. 이 컨테이너 **244/244**, 보관 14개 계약 diff 0 | `docs/EVIDENCE_v0.26_E26c.md` | (이 커밋) |
| **E26b** | 2026-09-09 | 벤치마크 도입 조사 중 발견된 두 번째 과잉 거부(D48) — `iree-dump-module`의 빈 백틱 라벨 상수 세그먼트가 관측에서 누락 | 결정론적(덤프 파싱·계약 수치) | `harness/static_mem_bound.py`(`_is_string_label` 신설), `harness/contract_negative_tests.py` 8건, `results/e26_boundary_utility/empty_label_rodata_fixture/` | **수정**. 첫 바이트가 NUL인 2,816 B 상수 블록이 빈 백틱 쌍으로 출력돼 관측 총합에서 버려짐 → 계약값과 모순 → D28 게이트 거부 → 오버라이드로 뚫으면 `overridden`이 되어 D39 헤더 게이트가 다시 거부. **정직한 f32 모델의 배치 경로가 닫혀 있었다.** 라벨 **길이**(`len(label)==nbytes`)로 판별하도록 수정, 저장소 전 산출물 137줄 중 136줄이 이 성질 만족·예외가 정확히 그 세그먼트. 이 컨테이너 **229/229**, 보관 14개 계약 diff 0 | `docs/EVIDENCE_v0.24_E26b.md` | (이 커밋) |
| **E26a** | 2026-09-09 | 벤치마크 지침(B0–B4)의 "출처가 추적되는 실제 워크로드" 요구를 따르다 첫 실물 모델에서 재현된 과잉 거부(D47) 수정 — ELF 내부 호출 타깃 해석 | 결정론적(ELF 정적 분석·계약 수치·해시) | `harness/elf_stack_frame.py`(호출 그래프 해석, CFG 순회로 콜리 범위 발견), `harness/make_contract.py`(`kernel_external_call_insns`를 미해석 호출 수로), `harness/contract_negative_tests.py` 12건, `results/e26_boundary_utility/mlperf_tiny_resnet_fixture/` | **수정**. MLPerf Tiny ResNet(CIFAR-10)의 softmax dispatch가 컴파일러 헬퍼를 80회 호출 → 타깃 2개 전부 `.text` 내부, `.plt` 없음, 미정의 심볼 0, 헬퍼는 프레임 0 B leaf. 수정 후 **오버라이드 0개**로 계약·헤더 완주(`bounded=618856`, `KERNEL_STACK_BYTES=439L`). **E14 14개·E25 canonical이 전부 `total_call_insns=0`이라 합성 모델셋으로는 재현 불가능했던 결함**. 거부 유지 조건 7종 시험 고정, revert 시 11/12 실패. 이 컨테이너 **221/221**, 보관 14개 계약 diff 0 | `docs/EVIDENCE_v0.23_E26a.md` | (이 커밋) |
| **E24c** | 2026-09-09 | 외부 검토(v0.20/E24b, `docs/reviews/REVIEW_v0_20_E24b.md`) F1–F5를 15개 에이전트로 재현 5 + 각 권고의 과잉 거부 위험 10(finding당 2렌즈)으로 독립 검증 후 확인된 것만 수정 | 결정론적 | `harness/gen_model_manyconst.py`(신규), `results/e24c_manyconst31/`(실물 단일-호출 번들 440 KB), `harness/make_contract.py`(subset_sum 결정 절차 선행·`constants_confirmation_state`·예산초과 거부), `harness/gen_contract_header.py`(구성요소 비음수 요구), `contracts/contract.schema.json`(`minimum: 0` + 상태 enum), `harness/contract_negative_tests.py`, 정정 7곳 | 5건 전부 재현, 4건 수정. **F1은 미채택** — 리뷰 권고(provenance 필수화)를 적용하면 삭제 경로(VERIFIED=0)가 **위조 경로(손으로 쓴 `{single_invocation:true}` → VERIFIED=1)**로 바뀌어 악화됨을 실측, threat model 결정 항목으로 이관. F2는 리뷰 서술이 부분적으로만 정확(두 변종 중 하나는 비음수 검사 부재가 원인). F3는 **리뷰 처방이 정직한 >256 MiB 모델을 거부**해 결정 절차를 예산 검사보다 앞에 두는 형태로 채택(무작위 3,000건 brute force 교차검증 0 불일치). F4는 에이전트가 "재현 안 됨"으로 보고했으나 직접 확인 결과 재현됨 — **그대로 받았으면 맞는 문서를 틀리게 정정할 뻔했다**. F5는 이 저장소 문서의 인과 서술이 틀렸음을 확인(양쪽 signed `long`; unsigned였다면 오히려 거부). D41–D44 수정, 170/170 → **191/191**(이 컨테이너 실측; CI 실측 `full` 190/190+1 SKIP · `without-iree` 103/103+11 · `stdlib-only` 103/103+11) | `docs/EVIDENCE_v0.21_E24c.md` | `3d1f640`·`c924a47`·`7bf6115`·`1758b8e` |
| **E24b** | 2026-09-09 | 외부 검토(v0.19 연구 목표 재정리본, `docs/reviews/REVIEW_v0_19_E24_RESEARCH_REFRAME.md`) §5.2 반례 5건(R1–R5)을 10개 에이전트로 재현 5 + 각 수정안의 과잉 거부 위험 5로 독립 검증 후 수정 | 결정론적 | `harness/make_contract.py`(`waive()` override 기록, 음수 스택 거부, tri-state `subset_sum_match`), `harness/gen_contract_header.py`(음수 스택·자기모순 bounded·shape 충돌·provenance 게이트, `CONTRACT_PROVENANCE_VERIFIED`), `harness/contract_negative_tests.py`, `contracts/contract.schema.json`, `native/build.sh`, `scripts/62_compile_and_check_aarch64.sh`, 보관 헤더 14개 재생성 | 5건 전부 재현. **심각도 재분류 2건, 방향 반대** — R1은 리뷰의 "매우 낮음: 수동 변조 필요"와 달리 정상 `make_contract.py` 경로로 도달하는 claim blocker(cFS 스택 게이트의 `stack_needed`가 음수가 되어 signed 비교가 모든 스택에서 참인 항등식이 됨 — 원인 서술은 E24c/F5에서 정정), R4는 **리뷰 처방이 정직한 31세그먼트 모델을 통째로 거부**해 미채택(열거기를 tri-state로 고쳐 해결). R5는 행위 시험 5건이 전부 PASS인 드리프트를 소스 수준 가드만 잡음을 실측. 부수 발견 D40(문서화된 스모크 경로 2개 파손, 1건은 E24의 N3이 만든 유형 (B) 회귀). D35–D40 수정, 143/143 → **170/170**(이 컨테이너 실측; CI 실측 `full` 169/169+1 SKIP · `without-iree` 85/85+9 · `stdlib-only` 85/85+9, 차이 1건은 PyYAML 유무), 보관 14개 헤더는 `CONTRACT_PROVENANCE_VERIFIED` 한 줄만 추가 | `docs/EVIDENCE_v0.20_E24b.md` | `cd50194`·`6b76dfd`·`a85ddf3`·`b47feb7`·`bf2be2a` |
| **E24** | 2026-09-09 | 외부 검토(v0.18-후속, `docs/reviews/REVIEW_v0_18_FOLLOWUP.md`) N1–N6·S5를 21개 에이전트로 독립 재현 검증 후 fail-closed 불변식 4건 + 과잉거부 회귀 1건 + 하네스 거짓경보 수정 | 결정론적 | `harness/make_contract.py`, `harness/gen_contract_header.py`, `harness/gen_model.py`, `harness/contract_negative_tests.py`, `contracts/contract.schema.json`, `plugins/compiled_learner/runtime/contract.json`, `.github/workflows/contract-negative-tests.yml`, `scripts/30_setup_iree.sh` | 7건 전부 confirmed·반박 0건이나 심각도는 리뷰와 다름(N1 P0→P1, N3 P0→P1, N4 P1→P2). **리뷰 제안 3건은 실측 결과 과잉 거부를 유발해 범위를 좁힘** — N1의 문자 그대로의 수정은 `dynamic` 계약 2개(A8 입력)를, N3의 strict 변종은 저장소 스키마 정본 형태를 거부. D28–D32 수정, 125/125 → **142/142+1 SKIP**(CI 실측), 보관 14개 계약 diff 0·헤더 바이트 동일 | `docs/EVIDENCE_v0.19_E24.md` | (이 커밋) |
| **E23** | 2026-09-08 | 외부 검토(v0.15) 잔여 4건(F4 표현 정정 / F8 A5a·A5b 손상 방식 코드화 / F10 OnAIR 바인딩 갭 / F11 범위 명시) + E22의 CI가 실제로 잡은 신규 크래시(D25) 수정 | 결정론적 | `harness/corrupt_vmfb.py`(신규: flip / flatbuffer_root_uoffset 두 손상 방식, ZIP64 STORED 외과적 패치 + CRC 갱신), `harness/e14_{make_scenarios,cfs_scenarios}.py`(corrupt_method 필수화·fail-closed), `plugins/compiled_learner/artifact_binding.py`(신규: 크기→sha256 게이트, stdlib 전용)와 플러그인 결선, `harness/{make_contract,static_mem_bound,contract_negative_tests}.py`(D25), `native/cfs_app/fsw/src/ai_learner.c`(admission JSON에 scope), `docs/EVIDENCE_v0.13_E18.md` §7·`docs/EVIDENCE_v0.17_E22.md` §6 정오표 | A5b 구조 손상을 보관 vmfb 8/8에서 실행(ZIP CRC 유효·타 엔트리 불변·크기 동일·결정적) 후 `iree.runtime`으로 실제 로드해 E17과 같은 오류 문자열(`FlatBuffer length prefix out of bounds, prefix 4294967295`)로 거부됨을 확인 — A5b의 네 번째 레벨. OnAIR 플러그인이 A3/A5a를 C 경로와 같은 순서(크기→해시)로 거부하게 됨. D25: 진짜 의존성 없는 환경(도구·모듈 모두 부재)에서 크래시 → 수정 후 **125/125 / 95/95+3 SKIP / 50/50+5 SKIP** | `docs/EVIDENCE_v0.18_E23.md` | (이 커밋) |
| **E22** | 2026-09-08 | 외부 검토(v0.15) F9 재현성 실제 확보 — fresh clone에서 크래시 없이 SKIP/FAIL 명확화 | 결정론적 | `harness/contract_negative_tests.py`(Result에 skip 상태, structural_available/with_structural_override 신설), `.gitignore`(dump/ 제외 해제), `results/e14_aarch64_qemu/*/dump/`(242개 파일 커밋), `requirements.txt`(신규), `.github/workflows/contract-negative-tests.yml`(신규) | iree.compiler.ir 부재 시 uncaught RuntimeError로 전체 크래시(요약 0줄)하던 3개 지점을 명확한 SKIP으로 정리; E21의 F3(구조적 검증기 미설치 기본 하드실패)과 이 시험 하네스 자신의 상호작용을 --allow-missing-structural-checker 투명 전달로 해소; dump/ 6.5MB를 gitignore에서 빼고 커밋(회귀·음성 시험이 실제로 요구하는데 빠져 있었음); 이 세션 내 실제 git clone으로 재현 확인 — with iree-base-compiler: 107/107, without: 크래시 없이 77/77+3 SKIP | EVIDENCE v0.17 | (v0.17) |
| **E21** | 2026-09-08 | 외부 검토(v0.15, `docs/reviews/REVIEW_v0_15_LATEST.md`) fail-open 결함 6건 수정(F1/F2/F3/F5/F6/F7) | 결정론적 | `harness/make_contract.py`(F1: 빈 dump-dir 하드실패, F2: ABI 반사 부재 하드실패, F3: 구조적 검증기 미설치 기본 거부), `harness/mlir_alloc_walk.py`(F5: stream.resource.pack 비상수 unresolved), `harness/gen_contract_header.py`(F6: 스택 불신뢰 분류 거부, F7: CONTRACT_DTYPES_ALL_F32 신설), `native/{ai_learner.c,native_learner.c}`(F7 C게이트), `harness/contract_negative_tests.py` 확장(96/96→107/107) | 11개 finding을 독립 에이전트 11개로 병렬 검증(9건 confirmed, 1건 partially_confirmed=F3, 1건 not-a-defect=F11), 그중 코드 fail-open 결함 6건 실제 재현 후 수정 — F1(빈 dump-dir → one-invocation 신호 전부 None인데 통과), F2(ABI 반사 부재는 불일치와 달리 미거부), F3(구조적 크로스체크가 패키지 미설치시 opportunistic로 저하, 검증 결과 "은폐"는 과장이나 정책 불일치는 타당), F5(stream.resource.pack 비상수 슬라이스가 unresolved 누락, IREE 상류 실제 문법으로 재현, 현재는 정규식 파서와의 크로스체크가 이중 방어로 완화 중), F6(스택 분석이 스스로 불신뢰로 분류해도 KNOWN=1), F7(C게이트가 "single-f32"라 주장하나 dtype 미검사, 매크로 신설). 전 6건 수정 전 코드로 되돌려 신규 시험이 실제로 실패함을 확인(revert-and-confirm-fail) | EVIDENCE v0.16 | (v0.16) |
| **E20** | 2026-09-08 | E19 구조적 크로스체크 적대적 코드 리뷰(4관점 병렬+finding당 3인 반박검증) + 과잉 거부 결함 2건 수정 | 결정론적 | `harness/make_contract.py`(비교 기준 `whole`→`p`, `dense_sum`→`const_b`), `harness/mlir_alloc_walk.py`(`diff_against_regex` 공유 헬퍼 신설), `harness/contract_negative_tests.py` 확장(85/85→96/96) | 13건 finding 중 11건 확인/2건 반박; 확인된 결함 중 2건은 실제 재현된 High severity 과잉 거부(버그 A: `whole`이 정규식 파서의 "파일 마지막 print=최다lowering" 낡은 가정에 의존, 계약이 실제 쓰는 `p`와 달라 정상 모델도 print 순서에 따라 거부될 수 있었음; 버그 B: constants(sum) 비교가 `dense_sum`이 아니라 계약이 실제 채택하는 `const_b`(패킹 크기) 기준이어야 함, 정렬 패딩 있는 모델은 영구 오탐 거부됐을 것) — 둘 다 보관된 layout IR을 surgical 텍스트 편집으로 재현(재컴파일 없음), 수정 후 회귀 시험으로 고정, 수정 전 코드로 되돌려 시험이 실제로 실패함을 확인(revert-and-confirm-fail). 3개 위치에 중복 구현됐던 diff 로직을 공유 헬퍼로 통합. 시험 갭 4건(dispatches-only 양성, allow override 예외분기, constants 진짜불일치, graceful-degradation 정식화) 추가 | EVIDENCE v0.15 | (v0.15) |
| **E19** | 2026-09-08 | 정규 MLIR pass 2단계: `mlir_alloc_walk.py`(E18) 구조적 추출기를 `make_contract.py`에 필수 크로스체크로 결선 | 결정론적 | `harness/make_contract.py` 수정(`provenance.structural_walker` 신설), `harness/contract_negative_tests.py` 확장(66/66→85/85) | 정규식 파서(`static_mem_bound.parse_alloc_ir`)와 구조적 추출기가 같은 layout IR을 각각 읽어 불일치 시 하드 실패(`--allow-structural-mismatch`로만 우회), `iree.compiler.ir` 미탑재 환경에서는 스킵(기록만, E15 이전 기준선으로 안전하게 저하); 보관된 14개 계약을 실제 프로덕션 경로(subprocess make_contract.py 호출)로 재생성해 계약 수치 diff 0 + 신규 크로스체크 필드 14/14 `available=True, agrees=True` 확인; 하드 실패 배선 자체는 monkeypatch로 불일치·파싱예외 두 조건 모두 실제 SystemExit 확인(in-process); CLAUDE.md 우선순위 3의 "컴파일러 버전 변경 시 명시적 실패" 평가지표에 실제 강제 지점 마련(다른 버전 재확인은 여전히 안 함) | EVIDENCE v0.14 | (v0.14) |
| **E18** | 2026-09-08 | 정규 MLIR pass 1단계: 구조적(비정규식) 할당 추출기, `iree.compiler.ir` API 사용 | 결정론적 | `harness/mlir_alloc_walk.py`(신규), `harness/contract_negative_tests.py` 확장(66/66) | `--mlir-print-ir-after` 청크 조각남 문제를 `util.global.load/store` 선언 합성으로 해결(유일한 텍스트 처리 단계), 이후 크기 추출은 전부 실제 Operation/Value API(op.name, def-use 체인, arith.constant 속성 직접 읽기); 보관된 14개 아티팩트 전부 정규식 파서와 값 일치(inputs/outputs/transient 원소별, constants 합계, entry_found, unresolved 존재); 화이트리스트 축소(실제 레지스트리로)로 미인식 op fail-closed 재확인; make_contract.py 통합은 범위 밖(후속) | EVIDENCE v0.13 | (v0.13) |
| **E17** | 2026-09-08 | AArch64 게스트 재현: A5b 최초 실행(3레벨), A2 경계값(mlp16k·multibranch), 재시작 2회+DELETE | 결정론적(verdict·exit code·cleanup 카운터) + 실행 확인(qemu-system-aarch64 게스트 core-cpu1) | AArch64 크로스빌드(scripts/60·61·70·71 전체 재구축), 게스트 배포 아티팩트(임시, results/ 미보관) | A5b(FlatBuffer root-uoffset 구조 손상, ZIP 재압축 후 sha256을 계약에 반영해 binding MATCH 조건 충족)를 native x86-64·cFS x86-64·**cFS AArch64 게스트** 세 레벨 전부에서 최초 실행 — 전부 admission ADMIT/binding MATCH/runtime_load_failed(FlatBuffer verifier)/cleanup 1회/cFS OPERATIONAL 유지, 크래시 0; mlp16k·multibranch A2 경계값 6/6(native)+3/3(cFS mlp16k); 재시작 2회+DELETE를 x86-64·AArch64 양쪽에서 완주(init_count 3, cleanup_calls 3, 이중해제 없음) — ES 명령 기반 정상 종료 경로로 v0.9 "정상 종료 자원회수 미검증" 문제도 해소 | EVIDENCE v0.12 | (v0.12) |
| **E16** | 2026-09-08 | C 게이트 보강: 스택 실거부(D15)·blob 크기 선검사(R8)·인터페이스 gate, x86-64 native_std 재구축 실기동 검증 | 결정론적(verdict·exit code·JSON) + 실행 확인(cFS core-cpu1 기동) | `native/cfs_app/fsw/src/ai_learner.c`, `native/native_learner.c`, `scripts/50_wire_cfs_ai_learner.sh` 수정 | 스택 확인을 Init() 최선두로 이동해 실제 거부 분기 신설(EID_STACK_REJECT), blob malloc 전 크기 선검사(계약과 다르면 해시 계산 없이 즉시 거부), 인터페이스(단일 f32 in/out) defense-in-depth 추가; x86-64 native_std 실기동으로 정상/스택거부/크기불일치/NOT_ADMITTED 4개 시나리오 검증(EVS 이벤트·JSON 필드 확인); 부수 발견: scripts/50이 WIRING.md 규칙(stack=base+kernel)을 어기고 262144로 하드코딩하던 버그를 신규 gate가 드러내 수정 | EVIDENCE v0.11 | (v0.11) |
| **E15** | 2026-09-08 | 계약 도구 fail-closed 전환 + 음성 시험(외부 검토 2건 반영, D12-D14 수정) | 결정론적 | `harness/contract_negative_tests.py`(신규), `static_mem_bound.py`/`make_contract.py`/`gen_contract_header.py` 수정 | 파서 줄바꿈 누락(D13)·미인식 op 무시·entry_found 누락(D12)·헤더 생성기의 음수/미지원 bound_method·스택 미상 수용·one-invocation의 layout IR 미결합(D14) 전부 재현 후 거부로 전환; 음성 20건+단위 5건+회귀 26건 = 51/51 PASS; 보관된 v0.9 14개 계약·헤더 재생성 시 diff 0(회귀 없음) | EVIDENCE v0.10 | (v0.10) |
| E0-env | 2026-09-07 | cFS/OnAIR/IREE 빌드·기동 | n/a | `scripts/*.sh`, `docs/STATUS.md` | cFS OPERATIONAL(LC 7.0.1, SBN 포함), OnAIR CSV/Kalman 완주, IREE 컴파일·실행 | STATUS | 278e448 |
| E0-platform | 2026-09-07 | 타이밍 적격성 게이트 | **FUNCTIONAL_ONLY** | `harness/platform_check.py`, `results_platform.json` | ratio_p99 1.67, ratio_max 2.26 | STATUS §2 | fd94420 |
| E0-integration | 2026-09-07 | OnAIR 플러그인 통합 | n/a | `plugins/compiled_learner`, `plugins/python_learner` | OnAIR core 변경 0줄, 로더 경유 완주. 함정: ServiceManager 선초기화 | EVIDENCE v0.1 §1 | ff926f7 |
| E1 | 2026-09-07 | 모델 크기 스윕 (latency) | FUNCTIONAL_ONLY | `harness/sweep.py`, `results_sweep*.json` | compiled/numpy 6.3–9.5×, 교차점 없음 | EVIDENCE v0.1 §2 | 3c82702 |
| E2 | 2026-09-07 | 모델 크기 스윕 (dispersion) | FUNCTIONAL_ONLY | 동상 | 소형에서 compiled p99/med 더 나쁨; 대형 역전은 noise 안 | EVIDENCE v0.1 §3 | 3c82702 |
| E3 | 2026-09-07 | 마샬링 floor | FUNCTIONAL_ONLY | `results_findings.json` | identity-call 4.70 µs → 격차 원인은 커널 품질 | EVIDENCE v0.1 §4 | 3c82702 |
| E4 | 2026-09-07 | lowering 설정 효과 (4종, h=16384) | FUNCTIONAL_ONLY | `results_findings.json` | median 1.87× 편차, avx2 < default | EVIDENCE v0.1 §5 | 3c82702 |
| E5 | 2026-09-07 | lowering 특성화 (10종 × 3크기, 메모리 포함) | FUNCTIONAL_ONLY (binary·RSS는 결정론적) | `harness/characterize.py`, `results_characterization_*.json` | Pareto 5개; 크기 간 ρ=+0.18; **메모리 축 반증** (B0 40 KB vs 1216 KB) | EVIDENCE v0.2 | 833edb6 |
| E6 | 2026-09-07 | 정적 메모리 상한 산출 (P2b) | 결정론적 | `harness/static_mem_bound.py`, `harness/static_bound_sweep.py`, `results_static_bound.json` | 정적 상한 = HAL 피크, 30/30 sound, tightness 1.0; 설정 불변 | EVIDENCE v0.3 §1 | 45213d6 |
| **E6c** | 2026-09-07 | 베이킹 모델 정적 상한 IR 파싱 (검토 §5 분모 해소) | 결정론적 | `results_static_bound_baked.json` | 30/30 sound·tight; 상수 720,896 B 모듈 상주로 분리 귀속 | EVIDENCE v0.3 §7 | (v0.3.1) |
| **E14-S0** | 2026-09-08 | 교차 ISA(x86-64→AArch64) 계약 건전성: 컴파일·구조 분석·qemu-user 확인 | 결정론적(계약·IR·ELF·admission) / qemu-user 시간값은 비증거 | `e14/aarch64/`, `contracts/contract.e14_aarch64.json`, `scripts/60-62_*.sh` | bounded_bytes 동일(786,476); HAL peak/per-call/경계값/모델교체/동적형상 x86-64와 일치; **AArch64에 16B 스택 프레임(x86-64엔 없음)** 발견 | EVIDENCE v0.8 | (v0.8) |
| **E14-S1** | 2026-09-08 | qemu-system-aarch64 Linux 게스트 + cFS-in-guest + Conv2D/multi-branch/동적형상 모델 | 결정론적(계약·ELF·admission·cFS 이벤트) / QEMU 시간값은 비증거 | `results/e14_aarch64_qemu/{aarch64,x86_64,native,cfs,comparison}/`, `harness/{make_contract,elf_stack_frame,e14_matrix,e14_cfs_scenarios,cfs_cmd}.py`, `scripts/51_build_cfs_aarch64.sh` | bounded_bytes 동일(모델 3종 x86-64=AArch64, MLP 1종 한정 벗어남); cFS 게스트 A1×2모델·A3·A4·A6·A7·A8 7/7 PASS; 태스크 스택 잔차 모델별 확정(16~191B)·시작스크립트 반영·자체검증; D9(스택정의 통일)·D10(one-invocation 실제검증 추가) 정정 | EVIDENCE v0.9 | af8b94a |
| **E11b** | 2026-09-08 | standalone 계측 정정 + 결합 | 결정론적(HAL·해시) | `native/native_learner.c`, `summary.json` | peak↔bounded true; 정상 상태 per-call **65,544**; 3200/3200 완료; binding MATCH | EVIDENCE v0.7 §2 | 0e887df |
| **E11c** | 2026-09-08 | 모델 교체(같은 ABI) + 계약 A | 결정론적 | 동상 | CONTRACT_ARTIFACT_MISMATCH, exit 5, 런타임 미생성 | v0.7 §1.2 | 0e887df |
| **E12b/c/d** | 2026-09-08 | cFS: 일치 / 교체 / 파일 부재 | 결정론적 | `native/results/cfs_run_*.log` | 25/25 완료 / 기동 거부+cleanup / cleanup; 3경우 cFS OPERATIONAL | v0.7 §1–3 | 0e887df |
| **E13** | 2026-09-08 | 계약 IR ↔ LLVM IR ↔ ELF 대응 (host/generic) | 결정론적 | `e13/`, `contracts/contract.e13_host.json` | 커널 alloca 0·call 0·스택 프레임 0; host AVX-512 FMA 34 vs generic 스칼라; 같은 호출 계약 MATCH | v0.7 §4 | 0e887df |
| **E11** | 2026-09-08 | Native C 변형 (standalone, Python 없음) | FUNCTIONAL_ONLY (HAL·RSS·admission은 결정론적) | `native/native_learner.c`, `native/build.sh`, `native/results/summary.json` | admission ADMIT/NOT_ADMITTED(exit 3); HAL 피크 786,476 = bounded; RSS 4.3 MB; median 32.2 µs | EVIDENCE v0.6 §2 | 271a237 |
| **E12** | 2026-09-08 | cFS 앱 `AI_LEARNER` 통합 | 동상 | `native/cfs_app/`, `native/results/cfs_run_*.log` | ADMIT: ES HK 패킷 추론 25회, 피크=bounded; NOT_ADMITTED: 앱 기동 거부, cFS OPERATIONAL 유지 | EVIDENCE v0.6 §3 | 271a237 |
| **E9** | 2026-09-08 | 할당 구조 4사례 (정렬·수명·대형·fusion) | 결정론적 | `harness/structural_cases.py`, `results_structural_cases.json` | slice 합은 2/4 과소(D3); post-layout 슬랩 4/4 sound·tight | EVIDENCE v0.5 §1 | (v0.5) |
| **E10** | 2026-09-08 | 경계값 U−1/U/U+1 | 결정론적 | `results_admission.json` (`boundary_test`) | 18/18 정확 | EVIDENCE v0.5 §5 | (v0.5) |
| **E7b** | 2026-09-08 | E7 재실행 (post-layout, 새 판정 의미, 상수 독립 확인) | 결정론적 | `results_admission.json` | 258 판정, misprediction 0, 상수 129/129 아티팩트 확인 | EVIDENCE v0.5 §6 | (v0.5) |
| **E7** | 2026-09-07 | 메모리 전용 admission checker (A5) | 결정론적 | `harness/admission_check.py`, `admission_sweep.py`, `results_admission.json` | 240 판정, optimistic/pessimistic misprediction 0, config-invariant | EVIDENCE v0.4 §1 | (v0.4) |
| **E8** | 2026-09-07 | 동적 형상 모델의 UNBOUNDED 판정 | 결정론적 | `results_admission.json` (`E8_dynamic_shape`) | all_sizes_static=false → UNBOUNDED, 예산 무관 거절 | EVIDENCE v0.4 §2 | (v0.4) |
| **E6b** | 2026-09-07 | **정정**: 가중치 베이킹 재특성화 | FUNCTIONAL_ONLY | `harness/characterize_baked.py`, `results_characterization_baked.json` | E1–E5가 매 호출 가중치 복사를 포함. 정정 후 격차 2.2–4.1×, ρ=+0.81, Pareto 붕괴 | EVIDENCE v0.3 §2 | c78c7ab |

## 가설 판정 이력

| 버전 | H1 | H2 | H3 | 근거 실험 |
|---|---|---|---|---|
| v0.0 (연구노트) | 주가설 | 보조 | 보조 | — |
| v0.1 | 부분 기각 | 강화 (근거 noise 안쪽) | 미검증 | E1–E4 |
| v0.2 | 기각 유지 | **확립** (결정론적 근거로 교체) | 미검증(입력 확보) | E5 |
| **v0.3** | 기각 유지 (격차 2.2–4.1×로 정정) | **격하: 부분 지지** (ρ 근거 철회) | **메모리 축 전제 충족** (정적 상한 sound) | E6, E6b |
| **v0.9** | 변화 없음 | 변화 없음 | H3 일반성 범위 재확대: **모델 종류 불변 확인**(MLP·Conv2D·multi-branch 3종, x86-64=AArch64); **cFS admission의 타깃 독립성 검증**(AArch64 게스트, 7/7 시나리오 PASS); 태스크 스택 잔차 통일된 정의로 확정·모델별 회계(16~191B)·시작스크립트 실반영 | E14-S1 |
| **v0.8** | 변화 없음 | 변화 없음 | H3 일반성 범위 확대: **ISA(x86-64/AArch64) 불변 확인**(단일 MLP 한정); AArch64 태스크 스택 잔차 첫 발견, 명시적 회계 필요 | E14-S0 |
| **v0.7** | 변화 없음 | 변화 없음 (설정별 지연 차이의 구조적 원인 확인: AVX-512 FMA vs 스칼라) | 검증 범위 확장: 결합·계측·실패 처리·코드 대응 | E11b/c, E12b/c/d, E13 |
| **v0.6** | 변화 없음 (Native 1.84×) | 변화 없음 | **cFS 앱 배치 형태에서도 성립** (단일 앱·모델·native_std); 경계 (b) 런타임 구성 2종에서 견고 | E11, E12 |
| **v0.5** | 변화 없음 | 변화 없음 | 시험 조건 내 성립, **근거 강화** (구조 4종, 경계값, 상수 독립 검증); 중심 문장을 검토 §8 권고로 교체 | E9, E10, E7b |
| **v0.4** | 변화 없음 | 변화 없음 (E7 config-invariance는 메모리 판정에 선택 불필요라는 반대 증거) | **메모리 축, 시험 조건 내 성립** (경계 b, 240/240, E8) | E7, E8 |
| **v0.3.1** | 현 모델·구현·플랫폼에서 성능 우위 미관측; 예측성 일반화 보류 | 설정별 비용 차이 관측; **계약 기반 선택의 이점 미입증** | 정적 per-call 버퍼 계약의 후보 근거 확보; 경계·가정·판정기 검증 필요 | E6c, 외부 검토 |

## 반증된 주장 이력

| 주장 | 출처 | 반증 실험 | 버전 |
|---|---|---|---|
| "`sys.meta_path` 훅으로 '패키지가 아예 없는 환경'을 시뮬레이션했다" (E22 §1) | EVIDENCE_v0.17_E22 §1 | E23: 그 훅은 모듈만 숨기고 콘솔 스크립트를 남긴다 — 진짜 의존성 없는 CI 러너에서 `iree-dump-module` 부재로 크래시(D25). "모듈만 없는 환경"의 시뮬레이션이었음으로 정정 | v0.18 |
| "E25가 E14의 cross-target `both_sound: null` 갭을 해소했다" | EVIDENCE_v0.22 §6.4 · CHANGELOG · CLAUDE.md · EXPERIMENT_LOG | 외부 검토(REVIEW_v0_22_E25 §4.2) 후 코드 대조: `cross_target_compare.py:201`의 `both_sound`는 각 타깃 실행의 `peak_within_bounded`를 종합한 **메모리** 지표이고, E25 cFS 로그에는 `mem` 레코드가 아예 없다. 채워진 것은 `out0_agreement`이며 canonical 1개 한정 | v0.22.1 |
| "두 ISA의 코드생성이 같은 누산 순서를 만들어 비트까지 같았다" | EVIDENCE_v0.22 §6.1 · CHANGELOG | 외부 검토(REVIEW_v0_22_E25 §3) : 관측한 것은 출력 바이트 동일뿐. 누산 순서는 측정한 적 없음(두 ELF dispatch 미대조). 서로 다른 순서가 같은 결과를 낼 수도 있으므로 출력만으로는 근거가 되지 않음 | v0.22.1 |
| "AOT가 Python보다 빠르고 예측 가능" (H1) | 연구노트 v0.1 §5 | E1, E2 | v0.1 |
| "median과 tail 랭킹이 다르다" | 방향판단 §3.3 | E4 재분석: 차이 1.15× < noise 1.67× | v0.2 |
| "peak memory가 제안 framework의 우위 축" | 방향판단 §7 | E5: B0 대비 5.2–30.4× 더 씀 | v0.2 |
| "모델 크기가 바뀌면 순위 붕괴 (ρ=+0.18)" | EVIDENCE v0.2 §4 | E6b: 가중치 복사 인공물, ρ=+0.81 | v0.3 |
| "Pareto front 5개" | EVIDENCE v0.2 §3 | E6b: 상충 미관측 | v0.3 |
| "E5 메모리 축 반증 배수 5.2–30.4×" | EVIDENCE v0.2 §5 | E6: 배수는 런타임 컨텍스트 귀속; 프로그램 정적 65.6 KB | v0.3 |
| "A5b(`runtime_load_failed`)를 native 레벨에서 확인했다" | EVIDENCE v0.9 §4.1·§9 | 외부 검토 2건(REVIEW/OPINION v0.9) 대조: native·cFS 로그 어디에도 A5b 실행 흔적 없음(§3 표엔 A5a만), `runtime_load_failed` 7/7 `null` | v0.9.1 |
| "7/7 PASS"가 원래 계획한 cFS 시나리오 전체를 뜻한다 | EVIDENCE v0.9 §4.1 | 외부 검토 2건: 계획 31개 중 7개만 실행, expect 축소(min_completed 15→3 등), 전부 timeout(EXIT=124) 종료 | v0.9.1 |
| "커널 스택을 별도 회계해 자체 확인하도록 구현했다"가 admission gate를 뜻한다 | EVIDENCE v0.9 §5·§7-#8 | 외부 검토 2건: `accounted=false`에서도 초기화 거부 분기 없음(`ai_learner.c:258-265`), 자원 획득 이후에 계산되는 텔레메트리 | v0.9.1 |

## 방법론 결함 이력

| ID | 결함 | 영향 실험 | 발견 경로 | 조치 |
|---|---|---|---|---|
| D49 | **과잉 거부(유형 B)**: `stream.resource.subview`가 `static_mem_bound.py`의 `_KNOWN_ENTRY_OPS`와 `mlir_alloc_walk.py`의 `KNOWN_ENTRY_OPS` 어디에도 없어, D13의 fail-closed 규칙이 그것을 `unresolved`로 밀어 넣고 `bound_method`가 `UNKNOWN_BOUND`가 됨. **IREE는 다중 결과를 하나의 external 슬랩에 패킹한 뒤 subview로 쪼개므로, 사실상 모든 다중 출력 모델이 계약을 만들 수 없었다** — 정보가 없어서가 아니라 있는 정보를 쓰지 못해서 | static_mem_bound.py(E15~), mlir_alloc_walk.py(E18~) | 스톡 2출력 모델을 한 번의 `iree-compile`로 컴파일해 직접 재현. 지침 도입 조사(C-1)와 별도 반박 검증이 **독립적으로 같은 결론**에 도달했으나 어느 쪽도 액면 그대로 받지 않고 직접 확인 | **조치 완료(E26c)**: 두 화이트리스트에 추가하되 `tensor.export`·`dealloca`와 달리 **검사를 붙였다** — 세 index 피연산자가 전부 상수로 풀리고 `offset + result_size <= source_size`여야 통과, 아니면 `unresolved`. 구조적 추출기의 피연산자 순서 `[source, source_size, offset, result_size]`는 실제 op에 MLIR API를 걸어 읽었다. 실물 근거 `harness/gen_model_multiout.py` + `results/e26c_multiout/`(D43 규칙), HAL 피크 704 = bounded 704. **남는 제약**: C 헤더 생성기는 여전히 단일 f32 in/out만 허용하므로 다중 출력은 *계약은 되고 C 배치는 거부* — 두 C 실행기가 실제로 그렇게 가정하므로 정확한 진술이다 |
| D48 | **과잉 거부(유형 B)**: `static_mem_bound.py:229`가 embedded `.rodata` 세그먼트를 "백틱이 있으면 문자열 라벨"로 판별했는데, `iree-dump-module`은 내용이 인쇄 가능해 보이면 백틱 사이에 렌더링하므로 **첫 바이트가 NUL인 진짜 상수 블록이 빈 백틱 쌍으로 출력돼 관측에서 누락**. 연쇄: 계약값과 모순 → D28 상수 게이트 거부 → `--allow-unconfirmed-constants`로 뚫으면 `verification_grade: overridden` → D39 헤더 게이트가 다시 거부. 정직한 f32 모델의 배치 경로가 닫힘 | static_mem_bound.py(E23~) | 벤치마크 도입 조사 중 f32 모델에서 재현 → `iree-dump-module` 출력 직접 대조 | **조치 완료(E26b)**: 라벨의 **존재**가 아니라 **길이**로 판별(`len(label)==nbytes`). 저장소 전 산출물의 백틱 rodata 줄 137개 중 136개가 이 성질을 만족하고 유일한 예외가 오분류되던 그 세그먼트. 모호하면 데이터로 분류(과다 계상은 크로스체크가 거부해 보이고, 과소 계상은 조용히 통과하므로). 시험 8건, 실물 vmfb 보존. **부수 교훈**: 이 시험의 첫 작성본은 revert 시 `AttributeError`로 스위트 전체를 죽였다 — D24/D32와 같은 부류이며, **결함을 재현하려고 되돌리는 순간이 정확히 그 조건이 발생하는 때**다 |
| D47 | **과잉 거부(유형 B)**: `elf_stack_frame.py`의 `classify()`가 *"resolve targets (imports/runtime) before classifying"*이라고 스스로 지시해 놓고 **그 해석 단계가 구현된 적이 없어**, 호출 명령이 하나라도 있으면 bucket (3)/(4)로 분류 → `gen_contract_header.py`(E21/D22)가 배치 가능한 헤더를 거부. 정직한 공개 CNN이 통째로 막힘. 부수로 `make_contract.py`의 `kernel_external_call_insns`가 이름과 달리 **내부 호출 포함 총 호출 수**로 채워지고 있었음(헤더 게이트는 그것을 "콜리 스택을 볼 수 없는 호출"로 읽음) | elf_stack_frame.py(E14~), make_contract.py | 벤치마크 지침의 실제 워크로드 요구를 따르다 MLPerf Tiny ResNet에서 재현 → objdump/readelf로 80개 call의 타깃 2개가 전부 `.text` 내부·`.plt` 없음·미정의 심볼 0·헬퍼는 프레임 0 B leaf임을 직접 확인 | **조치 완료(E26a)**: 한 방향 해석(미해석→해석만 가능)을 구현하고, 간접 호출·범위 밖·꼬리 호출·동적 alloca·간접 분기·재귀·해석 부재는 전부 거부 유지. 콜리 범위는 CFG 순회로 발견(첫 구현의 "첫 ret까지" 절단은 이 ResNet 헬퍼에서 거짓 거부를 냄 — 자기 첫 ret을 건너뛰는 전방 분기 보유). 시험 12건, revert 시 11건 실패. **교훈**: E14 14개와 E25 canonical이 전부 `total_call_insns=0`이라 **합성 모델셋으로는 원리적으로 재현 불가능**했다 — 도구의 한계는 측정한 모델 집합의 함수이며, 워크로드의 외적 타당성은 결과의 문제일 뿐 아니라 **도구 검증 범위의 문제**다 |
| D46 | **재현성**: `.gitignore:7`의 포괄 규칙 `*.log`에 대한 예외가 `results/e14_aarch64_qemu/**/*.log`에만 있어 **E25의 AArch64 게스트 원시 로그(시리얼·드라이버·qemu stderr)가 저장소에 커밋되지 않고 있었다.** 그런데 EVIDENCE_v0.22 §7은 그 디렉터리를 원시 근거로 가리킨다 — **문서가 가리키는 증거가 클론에 없는 상태**. E22의 F9(회귀 시험이 요구하는 `dump/`가 같은 규칙으로 빠져 있었음)와 같은 계열 | .gitignore(E25 시점) | 외부 검토 §5의 "README 동기화" 항목을 처리하다 발견 → `git ls-files`·`git check-ignore -v`로 확인 | **조치 완료(v0.22.1)**: `!results/e25_equivalence/**/*.log`(+ 선제적으로 `!results/e26_boundary_utility/**/*.log`) 추가, 로그 5개 커밋. 부수 확인: 시도 2의 시리얼 로그는 `scripts/71:24`의 `: > serial.log`로 시도 3이 덮어써 **복구 불가** — §7의 "emergency 2회" 중 두 번째는 산출물로 검증되지 않음을 EVIDENCE §11.8과 BOOT_LOG에 증거 등급으로 명시(철회 아님). **교훈**: 새 results 디렉터리를 만들 때 `.gitignore`의 포괄 규칙에 대한 예외를 함께 넣지 않으면, 그 실험의 원시 근거는 문서에만 존재하게 된다 |
| D45 | **방법론**: 서로 **다른 종류의 관측**(출력 동치 vs 메모리 soundness)을 같은 지표 이름(`both_sound`)에 귀속. E25 마무리 시 `cross_target_compare.py`의 정의를 다시 읽지 않고 E14 §11.4의 요약("cross-target 비교는 실행 대조가 아님")만 기억해 "실행 대조를 했으니 채워졌다"로 건너뛰었다. CLAUDE.md "가장 중요한 교훈"의 변종 — *두 관측이 모순되지 않는다*가 *다른 종류의 관측까지 닫혔다*로 번진 형태 | EVIDENCE_v0.22 §6.4 · CHANGELOG · CLAUDE.md · EXPERIMENT_LOG(E25) | 외부 검토(REVIEW_v0_22_E25 §4.2) → `cross_target_compare.py:201` 코드와 E25 cFS 로그의 stage 목록 대조로 확인 | **조치 완료(v0.22.1/E25b)**: EVIDENCE §11.3 정오표 + 살아있는 문서 4곳 정정 + 반증 원장 등재. 실제 `both_sound`를 채우는 것은 E26에서 `--native-summaries`로 수행한다. **부수 교훈**: 지표 이름이 무엇을 종합하는지는 그 지표를 만든 코드가 정본이며, 다른 문서의 요약을 근거로 채워졌다고 판단하지 않는다 |
| D44 | **방법론**: D35의 원인을 "`CFE_ES_AppInfo_t.StackSize`가 unsigned라 비교가 항상 참"이라고 서술했으나 실제 코드는 `es_stack`·`stack_needed` 둘 다 signed `long`이고 `info.StackSize`는 명시적 `(long)` 캐스트다. **인과가 반대로 틀렸다** — 실제 unsigned 비교였다면 `-37856`이 1.8e19로 wrap돼 현실적인 모든 스택에서 오히려 거부됐을 것이다. 그런데 그 서술에 "실제 C 식으로 컴파일해 확인"이라는 표시가 붙어 있었다(작업 규율 4가 경계하는 형태). 7곳에 전파 | CLAUDE.md·CHANGELOG·EXPERIMENT_LOG·EVIDENCE_v0.20·코드 주석 2곳(E24b) | 외부 검토(REVIEW_v0_20_E24b F5) → 실제 C 식과 그 반사실을 각각 컴파일해 확인 | **조치 완료(E24c)**: 살아있는 문서 4곳 직접 정정, EVIDENCE_v0.20 §12 정오표, 코드 주석 2곳. **가장 위험했던 것은 대소문자 무시 grep으로만 잡히는 `gen_contract_header.py`의 음수 스택 가드 바로 위 주석** — 살아있는 fail-closed 가드의 존재 이유를 설명하는 자리다. "`(long)` 캐스트를 unsigned 해석을 근거로 제거하지 말 것" 경고 추가(제거 시 정상값 262335 포함 전부 거부됨을 실측). 판정(항등식·claim blocker)과 코드 수정은 유지되며, `es_stack = -1`(GetAppInfo 실패)에서도 참이라는 사실이 추가로 확인됨 |
| D43 | D38(24세그먼트 절단 제거)을 정당화한 ">24 세그먼트가 정상 컴파일에서 나온다"는 주장의 **실물 IREE 산출물이 저장소에 없어** 저장소 내용만으로 재현 불가. 수치도 부정확(31은 모델의 상수 개수이고, 이 저장소 추출기 기준 세그먼트는 33개) | EVIDENCE_v0.20·EXPERIMENT_LOG·CLAUDE.md·코드 주석 2곳(E24b) | 외부 검토(F4) → **재현 에이전트는 "3~5개, 재현 안 됨"으로 보고했으나 상수 수가 적은 기존 모델을 쓴 결과였고, 직접 확인 시 재현됨** | **조치 완료(E24c)**: `harness/gen_model_manyconst.py` 신설(16x32 tail로 두 번째 dispatch 강제 — 없으면 dump 파일명에 mlir basename이 없어 one-invocation 검사가 정당하게 거부), `results/e24c_manyconst31/`에 한 번의 호출 산출물 전체 보존(440 KB, dump는 축소본이며 계약이 모든 수치 동일하게 재생성됨을 확인). **실물 입력으로 현재 True / 옛 절단 False 실증**, 6건 회귀 시험 |
| D42 | `subset_sum_match`의 예산 초과 `None`이 기본 거부되지 않아, D25("관측 불가", 거부)와 N1("관측했더니 모순", 거부) 사이의 **같은 증거 상태**가 통과. 네 가지 `None` 사유를 구분하는 기계 판독 가능한 필드도 없어 자유 텍스트 note로만 존재 | make_contract.py(E24b) | 외부 검토(F3) → 코드 대조 + in-process 재현 | **조치 완료(E24c)**: **리뷰 처방(예산 초과 기본 거부)은 총량이 정확히 한 세그먼트인 정직한 >256 MiB 모델을 거부**해 미채택 — 결정 절차(정확 일치 O(n), 후보 20개 이하 전수 열거)를 예산 검사보다 **앞에** 두고 둘 다 실패할 때만 `None`. 무작위 3,000건 brute force 교차검증 0 불일치. `constants_confirmation_state` 5-상태 신설(스키마 선택 필드), `unevaluable_budget_exceeded`는 `--allow-unverified-invocation`으로만 우회 |
| D41 | D36(E24b/R2)의 합 항등식이 `is_int(_pc) and is_int(_cb) and ...` 형태라 **두 값이 우연히 정수일 때만** 실행 — `null` 구성요소는 검사를 무력화하고, 비음수 검사가 없어 `per_call = bounded+1, constants = -1`이 항등식을 **공허하게 만족**시킨다. 셋 다 `BOUND_KNOWN=1` 헤더. 이 저장소가 이미 세 번 고친 그 모양(D25·D28·D29)이 게이트 안에 있었음 | gen_contract_header.py(E24b) | 외부 검토(F2, **서술은 부분적으로만 정확** — "둘 다 정수일 때만"은 첫 변종만 설명하고 두 번째는 비음수 검사 부재가 원인) → 두 변종 실제 재현 | **조치 완료(E24c)**: `bound_known`일 때 두 구성요소가 비음수 정수임을 **요구**. 실측으로 좁힌 세 성질 — 스코프 유지(풀면 A8 입력 `dynamic` 2개 거부), `>= 0`이지 `> 0` 아님(constants==0은 정당), `is_int`를 요구 조건으로. 스키마 바이트 필드 `minimum: 0` |
| D40 | `CLAUDE.md`가 스모크 테스트로 문서화한 `cd native && bash build.sh`와 `scripts/62_compile_and_check_aarch64.sh`가 둘 다 `gen_contract_header.py`에서 rc=1로 죽고 있었음. 원인 3개 중 2개는 E15부터의 기존 문제(예시 계약에 ELF 스택 분석 없음 / 인라인 계약이 `bound_method` 미선언), 1개는 **E24의 N3 dtype 게이트가 만든 유형 (B) 회귀** | native/build.sh, scripts/62(E15~), gen_contract_header.py(E24) | R5 검증 중 별개로 발견 → E24b 이전 생성기로도 rc=1임을 확인해 원인 분리 | **조치 완료(E24b)**: 두 스크립트가 `--allow-unknown-stack` 전달, `scripts/62` 인라인 계약이 `bound_method`·dtype 선언. `documented_smoke_path_cases()` 신설(스크립트 미실행, 툴체인 없는 환경에서도 동작). **교훈**: "과잉 거부 위험을 측정했다"는 어떤 입력 집합에서 측정했는지에 전적으로 의존 — E24는 커밋된 계약 파일만 봤고 스크립트가 런타임에 만드는 계약을 보지 않았다 |
| D39 | 적용된 `--allow-*`/`--no-validate` override가 계약에 아무 흔적을 남기지 않아, ABI·triple·ELF·one-invocation 검사를 **전부 우회한 계약**과 전부 통과한 계약이 free-text `provenance.notes`를 직접 diff하지 않는 한 구별 불가. 게다가 그 계약의 유일한 소비자 `gen_contract_header.py`가 provenance를 **한 글자도 읽지 않아** 둘이 같은 배치 가능 헤더를 만듦 | make_contract.py, gen_contract_header.py(E15~) | 외부 검토(REVIEW_v0_19_E24_RESEARCH_REFRAME §5.2 R5) → 실제 override 계약으로 헤더 생성해 재현 | **조치 완료(E24b)**: `waive()`가 **거부 지점이 아니라 플래그 접근**을 감싼다(기계적 패치는 결합형 게이트 2곳을 놓치고, 그러면 override가 적용됐는데 `verification_grade="verified"`라 주장하는 신규 fail-open이 됨). `provenance.overrides_applied`/`verification_grade` 신설(스키마 **선택** — 필수화하면 보관 14개가 무효), 헤더 생성기가 override 계약을 기본 거부(`--allow-override-contract`) + `CONTRACT_PROVENANCE_VERIFIED`. 드리프트 주입 시 행위 시험 5건은 전부 PASS이고 소스 수준 가드만 FAIL함을 실측 |
| D38 | `subset_sum_match`가 세그먼트 24개에서 절단 — (a) 정상 단일 컴파일로 31개 세그먼트가 나오는 모델(스톡 플래그 `--iree-stream-resource-max-allocation-size=1024` 하나면 충분)에 대해 **계약 안에 "NOT matched"라는 거짓 진술**을 쓰고 `cross_target_compare.py`가 퍼뜨림, (b) 26개 이상에서 모순을 통과(경계 정확히 25개로 실측). 두 상태(`False`=모순 / 열거 포기)가 같은 값으로 붕괴 | make_contract.py(E15~) | 외부 검토(§5.2 R4, 리뷰가 유일하게 "가능"으로 표시) → 스톡 플래그로 31세그먼트 실제 생성 | **조치 완료(E24b)**: **리뷰의 문자 그대로의 처방("절단은 unevaluated로 분리하고 기본 거부")은 정직한 31세그먼트 모델을 통째로 거부해 미채택.** 열거기를 진짜 tri-state로(개수 절단 대신 `total`로 가지치기한 비트셋 DP, 예산은 총합 256 MiB). N1 게이트의 carve-out 2개 소멸. `dynamic` 계약 2개의 6개 스칼라 의도적 재생성(EVIDENCE_v0.19 §7이 예고한 비용) |
| D37 | `gen_contract_header.py`의 `shape_from()`이 fallback 체인이라 `validity`와 `interface`의 shape 표현을 **비교한 적이 없음** — 두 표현이 충돌해도 `KNOWN` 헤더 생성 | gen_contract_header.py(E14~) | 외부 검토(§5.2 R3) | **조치 완료(E24b)**: `check_shape_agreement()`를 `shape_from()` 전에 실행, **존재하는 statement끼리만** 대조(부재는 불일치가 아님 — `contract.e14_aarch64.json`은 `interface` 블록이 없는 정직한 계약이고 양쪽 존재를 요구하면 유형 (B)). dtype 절반은 **반박** — E24/N3이 이미 두 블록 합집합을 읽으므로 이미 거부됨 |
| D36 | `bounded_bytes`가 **같은 계약이 명시한 구성요소 합**과 대조되지 않음 — `bounded_bytes=1`이 `per_call=1352`+`constants=2176` 옆에 있어도 `BOUND_KNOWN=1`이고 C 게이트는 셋 중 첫 번째만 읽음 | gen_contract_header.py(E15 D13) | 외부 검토(§5.2 R2) | **조치 완료(E24b)**: `bound_known` 범위에서 `bounded == per_call + constants` 강제. **`>=`가 아니라 `==`** — `make_contract.py`가 셋을 같은 dict 리터럴·같은 `all_static` 게이트에서 계산하므로 등식이 항등이고(저장소 정수 계약 전수 확인), `>=`는 거부 집합이 같으면서 부풀린 자기모순 값만 통과시켜 정당한 ADMIT을 NOT_ADMITTED로 만듦(유형 B) |
| D35 | `--elf-analysis` JSON의 **음수 스택**이 부호 검사 없이 계약에 복사되고 `elif stack_b:`의 truthiness가 음수를 "신뢰" 버킷으로 승격 → `KNOWN` 헤더 생성. 실제 C 식으로 컴파일해 확인: `stack_needed = 262144 + (-300000) = -37856`이고 `es_stack`·`stack_needed`가 모두 signed `long`인데 `stack_needed`가 음수라 `es_stack >= stack_needed`가 **스택 0에서도, 그리고 `GetAppInfo` 실패 시의 `es_stack = -1`에서도 참** — E16/D15의 거부 분기가 조용히 멈추는데 텔레메트리는 `accounted=true`를 계속 보고. 스키마에 `minimum` 없음 | make_contract.py, gen_contract_header.py, contract.schema.json(E15~E16) | 외부 검토(§5.2 R1, "매우 낮음: 수동 변조 필요"로 분류) → **검증자가 계약을 손대지 않고 `--elf-analysis` 입력만 바꿔 정상 파이프라인으로 재현, claim blocker로 상향** | **조치 완료(E24b)**: 생성기가 음수 스택 거부(우회 플래그 없음), `make_contract.py`가 ELF 분석의 음수를 애초에 거부, `stack_b is not None and stack_b > 0`. **`<= 0`은 쓰지 않음** — `stack == 0`은 정당하고(dispatch 없는 ELF의 `max(..., default=0)`) `<= 0`은 `dynamic` 계약 2개(A8 입력)도 거부. **정정(E24c/F5)**: 원인 서술이 틀렸다 — unsigned 승격이 아니라 `stack_needed` 음수화다(양쪽 signed `long`, `info.StackSize`는 명시적 `(long)` 캐스트). 인과가 반대로 틀렸음도 실측 확인 — 실제 unsigned 비교였다면 `-37856`이 1.8e19로 wrap돼 오히려 **거부**됐을 것이다. 판정(항등식·claim blocker)과 수정은 유지 |
| D34 | E24의 EVIDENCE·README·CLAUDE.md가 `full` 레그 수치를 **143/143**으로 기록 — 그것은 이 개발 컨테이너의 값이고(PyYAML이 시스템 패키지로 존재), `pip install -r requirements.txt`만 한 fresh clone의 값은 **142/142 + 1 SKIP**이다. 또 `without-iree` 레그를 E23 시점 값(48/48+6)으로 인용했으나 실제로는 58/58+9이며 `stdlib-only`와 동일하다(IREE 도구가 없으면 `jsonschema`를 쓰는 경로가 이미 전부 SKIP되므로 두 레그가 같은 것을 측정한다). **F9(E22)·D25(E23)에 이은 같은 부류의 세 번째 반복** — "내 환경에서 돌려봤다"를 fresh checkout의 근거로 쓴 것 | docs/EVIDENCE_v0.19_E24.md, README.md, CLAUDE.md, CHANGELOG.md, EXPERIMENT_LOG.md(E24) | E24가 신설한 `stdlib-only` CI 레그의 첫 성공 실행 로그를 읽다가 발견 — **그 레그가 잡아낸 첫 대상이 E24 자신의 문서 수치였다** | **조치 완료(E24)**: 다섯 문서의 수치를 전부 CI 실측값으로 정정하고, 세 조건을 조건과 함께 병기. PyYAML은 `requirements.txt`에 추가하지 않음 — 워크플로우 YAML 검사는 본질적으로 푸시 전 로컬 가드이고(파일이 깨지면 CI는 애초에 실행되지 않는다) 그 하나를 위해 배포 의존성을 늘리는 것은 균형이 맞지 않는다. EVIDENCE_v0.19 §6.2에 방법론 교훈으로 기록 |
| D33 | E24가 CI 워크플로우에 추가한 `stdlib-only` 레그의 `run:` 스칼라에 `": "`(콜론+공백)이 들어간 Python 문자열이 있어 **워크플로우 파일 전체가 YAML 파싱 불가** — 푸시 즉시 실패했고, 실행 이름이 워크플로우 이름 대신 파일 경로로 표시됐다(단계 하나도 시작되지 않은 파싱 오류의 신호). **CI가 구조적으로 시험 실패로 보고할 수 없는 유일한 결함 부류** — 어떤 시험이 실패했는지 찾으면 아무것도 나오지 않는다 | .github/workflows/contract-negative-tests.yml(E24) | 자기 커밋의 CI 실행이 즉시 실패 → 로컬 `yaml.safe_load`로 line 53 col 60 확인 | **조치 완료(E24)**: 해당 스칼라에서 콜론+공백 제거. `workflow_yaml_cases()` 신설 — `.github/workflows/*.yml`이 파싱되고 `name`·`jobs`를 선언하는지 검사(PyYAML 부재 시 SKIP, 저장소 의존성 아님). 깨진 YAML로 되돌려 시험이 실제로 그 오류를 잡음을 확인. D24·D25와 같은 부류(하네스가 자신이 덮어야 할 조건을 볼 수 없었음) |
| D32 | 시험 하네스가 `jsonschema` 부재 환경에서 **15건의 거짓 FAIL**을 냄(98/113) — `make_contract.py`가 E15/D13에 따라 rc=3으로 하드 실패하는데, 그 15건은 ABI 불일치·낡은 dump-dir 등 **다른 것**을 검사하는 케이스다. CI의 `without-deps` 레그가 `jsonschema`를 설치해 이 조건을 한 번도 시험하지 않았고, `scripts/99_bootstrap_all.sh` 경로는 그것을 설치하지 않아 도달 가능했다 | contract_negative_tests.py, scripts/30_setup_iree.sh, CI(E22~E23) | 외부 검토(REVIEW_v0_18_FOLLOWUP N6) → 스텁 모듈로 import 차단해 재현, pip 없는 venv로 진짜 무의존성도 실측 | **조치 완료(E24)**: `schema_validator_available()` 신설 + `with_structural_override()`가 `--no-validate`를 투명 부착(블록 통째 SKIP은 jsonschema가 불필요한 43건까지 버려 채택 안 함). E15/D13 분기 자체의 시험 신설(그 분기는 어디에서도 시험되지 않았음), 부트스트랩에 jsonschema 추가, CI에 `stdlib-only` 레그 신설. CI 실측 142/142+1 · 58/58+9 · 58/58+9 |
| D31 | E23이 D27로 추가한 OnAIR 바인딩 게이트가 **저장소 기본 fixture를 거부** — `plugins/compiled_learner/runtime/contract.json`에 게이트 필수 필드 `artifact.bytes`가 없어 기본 실행 경로가 즉시 `ArtifactBindingError`. E23의 단위 시험이 E14 계약을 써서 이 통합 경로를 건드리지 않아 125/125가 통과했다. **결함 유형 (B) 과잉 거부** | plugins/compiled_learner, gen_model.py, contract.schema.json(E23) | 외부 검토(REVIEW_v0_18_FOLLOWUP N5) → 커밋된 fixture를 그대로 게이트에 넣어 재현 | **조치 완료(E24)**: `gen_model.py`를 **먼저** 수정(`sweep.py`가 그 디렉터리를 매 스텝 재작성하므로 데이터만 고치면 휘발성) → fixture에 `bytes: 10642`(재컴파일 없음, sha256은 이미 실물과 일치) → 스키마 `artifact.required`에 추가(17개 계약 전수 통과). stdlib 전용 독립 시험 함수 신설 |
| D30 | `harness/gen_contract_header.py`가 dtype을 **선택적 확장 필드**인 `interface.inputs/outputs`에서만 읽어, 그것이 없으면 dtype 집합이 빈 집합이 되고 게이트·매크로가 둘 다 `dtypes - {"f32"}`를 검사하므로 **빈 집합과 `{"f32"}`가 구분되지 않음** — 스키마 필수 필드인 단수형이 f16이라 말하는 계약이 `CONTRACT_DTYPES_ALL_F32=1`로 통과(공허참). 복수형이 f32여도 단수형이 f16이면 통과하는 변종도 있음 | gen_contract_header.py(E21 D23) | 외부 검토(REVIEW_v0_18_FOLLOWUP N3) → 보관 계약 변형 3종으로 재현 | **조치 완료(E24)**: 스키마 필수 단수형 dtype을 집합에 합치고 매크로에 `dtypes and` 요구. **strict 변종(복수형 부재 시 거부)은 실측 과잉 거부로 채택 안 함** — 이 저장소 스키마가 선언하는 정본 형태를 거부한다. 단수형을 `validity`에서도 읽도록 함(`shape_from()`과 같은 규칙 — 이걸 빠뜨리면 `contract.e14_aarch64.json`이 거부됨) |
| D29 | `harness/gen_contract_header.py`의 스택 신뢰 판정이 세 신호를 `bool()`/`not in (None,...)`으로 읽어 **필드 부재와 '신뢰함'이 구분되지 않음** — `kernel_stack_classification` 키만 지워도(또는 `null`로 두어도, 세 키를 전부 지워도, `kernel_external_call_insns`만 지워도) `KERNEL_STACK_BYTES_KNOWN=1` 헤더가 바이트 동일하게 생성. E21/D22가 물은 것은 "분석이 불신을 표명했는가"인데, 부재는 그 질문에 답할 수 없으므로 UNKNOWN이어야 한다 | gen_contract_header.py(E21 D22) | 외부 검토(REVIEW_v0_18_FOLLOWUP N4) → 보관 conv2d 계약에서 키만 제거해 재현 | **조치 완료(E24)**: 세 신호를 `is not False`/`!= 0`/`not in (...)`로 바꿔 `None`을 명시적 불신뢰로. `bound_known` 조건부 범위 제한은 넣지 않음(주변 게이트가 이미 그 범위를 제공하고, 넣으면 dynamic 경로 fail-open이 되살아남 — 반박 검증 2인이 각각 실측). `and is_int(stack)` 가드가 no-ELF 경로의 기존 오진도 함께 수정 |
| D28 | `harness/make_contract.py`가 (a) 비어 있지 않은 dump-dir에서 링크된 ELF만 없으면 `dump_elf_in_vmfb=None`이 되는데 어느 분기도 None을 보지 않아 exit 0 + notes 빈 리스트 + **배치 가능한 헤더** 생성(E21/F1은 *완전히 빈* dump-dir만 막았음), (b) 상수량의 아티팩트 측 관측이 IR과 **모순**돼도(`consts_confirmed=False`) 거부하지 않음 — E23/D25가 더 약한 증거인 "관측 불가"는 거부하는데 더 강한 "관측했더니 모순"은 통과시키는 비대칭. (a)는 다른 모델의 vmfb를 짝지어도 계약이 나와 **D10 구멍이 None 경로로 재개방**됨 | make_contract.py(E21 F1, E23 D25) | 외부 검토(REVIEW_v0_18_FOLLOWUP N1·N2) → 보관 dump-dir에서 `.so`만 제거 / 가짜 `iree-dump-module`로 재현. 도달 경로 확인: 메타 플래그 대신 구성요소 dump 플래그만 주면 이 모양이 나옴 | **조치 완료(E24)**: None 전용 catch-all 신설(False는 기존 우회 불가 분기 유지 — 섞으면 검증된 불일치를 우회 가능한 것처럼 오도), `single_invocation`을 `is True`로, 계산만 하고 버리던 두 신호를 provenance에 기록. 상수 모순은 carve-out 3개(const==0 / 세그먼트 24개 초과 / D17 정렬 패딩)로 좁혀 거부 + 별도 `--allow-unconfirmed-constants`. **리뷰의 문자 그대로의 수정은 보관 14개 중 2개를 거부**함을 실측 |
| D27 | OnAIR `CompiledLearner._load_artifact()`가 계약이 지목한 `.vmfb`를 sha256·크기 검사 없이 그대로 IREE에 넘김 — 같은 저장소의 C 경로(`native_learner.c`, cFS 앱)는 크기 선검사→sha256→자원 획득 전 거부를 이미 하고 있어, A3(모델 교체)·A5a(손상)를 C는 잡고 OnAIR는 통과시키는 비대칭 | plugins/compiled_learner(E0~) | 외부 검토(REVIEW_v0_15_LATEST F10) → 수정 전 코드 5줄 확인 + 실제 손상 파일로 재현 | **조치 완료(E23)**: `plugins/compiled_learner/artifact_binding.py` 신설(stdlib 전용), `verify_artifact_hash=True` 기본, 계약에 `artifact.bytes`/`sha256`이 없으면 검사 생략이 아니라 거부. 해시한 바로 그 바이트를 IREE에 전달. 시험 6건(EVIDENCE_v0.18 §2) |
| D26 | A5b 시나리오가 `docs/EVIDENCE_v0.12_E17.md` §2.1이 서술한 구조 손상(module.fb FlatBuffer 길이 접두사)이 아니라 A5a와 **동일한** offset 4096 임의 bit flip을 사용 — 두 방식을 구분하는 필드 자체가 없어 저장소 코드만으로는 E17의 A5b를 재현할 수 없었음(E17 결과 자체는 수작업 구조 손상으로 얻은 것이라 반증되지 않음) | e14_make_scenarios.py, e14_cfs_scenarios.py(E14~E17) | 외부 검토(REVIEW_v0_15_LATEST F8) → 두 파일 대조 + 수정 전 코드로 revert-and-confirm-fail | **조치 완료(E23)**: `harness/corrupt_vmfb.py` 신설(두 방식 실제 구현, ZIP64 STORED 외과적 패치), `corrupt_method` 필수화(누락·미인식은 `ValueError`), 보관 vmfb 8/8 + `iree.runtime` 실제 로드 거부 확인(EVIDENCE_v0.18 §1) |
| D25 | `iree-dump-module` 바이너리 부재 시 `make_contract.iree_dump_module()`·`static_mem_bound.artifact_rodata_segments()`가 uncaught `FileNotFoundError`로 전체 크래시 — D24와 같은 부류의 두 번째 지점. **E22가 D24를 확인할 때 쓴 `sys.meta_path` import 차단은 Python 모듈만 숨기고 콘솔 스크립트는 PATH에 남기므로 이 조건을 원리적으로 재현하지 못했다** | make_contract.py, static_mem_bound.py, contract_negative_tests.py(E15~E22) | **E22가 만든 CI의 `without-deps` 레그가 첫 실행에서 실제로 실패**(사람이 아니라 CI가 발견) → `env -i PATH=/usr/bin:/bin` + import 차단으로 충실히 재현 | **조치 완료(E23)**: 두 함수가 `OSError`를 잡고, 후자는 `([],[])`가 아닌 `(None,None)` 반환(“관측 못 함”≠“관측했고 없음”). 상수 독립 확인 불가는 `null`로 기록 + 기본 거부(`--allow-unverified-invocation`로만 우회), `constants_check_note` 3-상태화. 하네스에 `iree_tools_available()` 신설(SKIP 처리). 세 환경 실측 125/125·95/95+3·50/50+5(EVIDENCE_v0.18 §5) |
| D24 | `harness/contract_negative_tests.py`가 `iree.compiler.ir` 부재 환경에서 uncaught `RuntimeError`로 전체 크래시(PASS/FAIL 요약 0줄 출력 없이 죽음) — `structural_walker_checks()`의 화이트리스트 축소 시험에 `except`가 없었음(다른 두 곳은 있었음). "96/96, 환경 구축 불필요" 주장을 fresh clone에서 재현 불가능하게 만든 핵심 원인 중 하나 | contract_negative_tests.py(E15~E21) | 외부 검토(REVIEW_v0_15_LATEST F9) → sys.meta_path 훅으로 패키지 부재를 가역적으로 시뮬레이션해 재현 | **조치 완료(E22)**: `Result`에 `skip` 상태 신설, 3개 지점을 명확한 SKIP으로 정리(FAIL도 크래시도 아님). 실제 git clone + 시뮬레이션으로 77/77+3 SKIP 확인(EVIDENCE_v0.17) |
| D23 | `native/cfs_app/fsw/src/ai_learner.c`·`native/native_learner.c`의 C 인터페이스 게이트가 이벤트 메시지·주석에서 "single-f32-in/out"을 검사한다고 서술하지만 실제 조건은 `CONTRACT_NUM_INPUTS/OUTPUTS` 개수만 검사 — dtype 매크로 자체가 헤더에 없어 "개수는 맞지만 dtype만 stale"한 손수정 헤더를 C가 잡을 수 없음(주석이 명시한 위협 모델 자체를 놓침) | native/cfs_app, native_learner.c | 외부 검토(REVIEW_v0_15_LATEST F7) → 코드 대조 + 직접 실행으로 확인 | **조치 완료(E21)**: `gen_contract_header.py`가 `CONTRACT_DTYPES_ALL_F32` 매크로 신설, 양쪽 C 게이트의 필수 매크로 가드·인터페이스 검사에 반영. 새 회귀 시험 4건, revert-confirm-fail로 실효성 확인(EVIDENCE_v0.16) |
| D22 | `harness/gen_contract_header.py`가 스택 값의 정수 여부만 확인 — `elf_stack_frame.py`의 `classify()`가 스스로 "신뢰 불가"로 표시하는 조건(동적 스택 할당, 미해결 외부 호출, bucket_3/4 분류)이어도 `CONTRACT_KERNEL_STACK_BYTES_KNOWN=1`로 헤더가 생성됨 | gen_contract_header.py | 외부 검토(REVIEW_v0_15_LATEST F6) → conv2d 계약을 적대적으로 편집해 직접 실행으로 확인 | **조치 완료(E21)**: `kernel_dynamic_stack_alloc`/`kernel_external_call_insns`/`kernel_stack_classification`이 불신뢰를 나타내면 `stack_known`을 강제 False(`--allow-unknown-stack`으로 통합 우회). 새 회귀 시험 4건, revert-confirm-fail(EVIDENCE_v0.16) |
| D21 | `harness/mlir_alloc_walk.py`의 `stream.resource.pack` 처리가 비상수 index operand를 `unresolved`에 넣지 않고 조용히 버림 — 형제 분기(`stream.tensor.import`/`stream.resource.alloca`)와 다른 비대칭 패턴, 코드 자신의 주석("unresolved ones are reported, not skipped")과 실제 동작 불일치 | mlir_alloc_walk.py(E18/E19) | 외부 검토(REVIEW_v0_15_LATEST F5) → IREE 상류 실제 문법으로 합성한 MLIR을 `ir.Module.parse()`로 파싱해 재현 | **조치 완료(E21)**: 형제 분기와 동일한 `(bucket if ok else unresolved)` 패턴으로 통일. 현재 배선(E19 크로스체크)에서는 정규식 파서와의 불일치로 이미 이중 방어되고 있었음이 검증 과정에서 확인됨(severity P1로 재평가). 재현을 고정 회귀 시험으로 등록, revert-confirm-fail(EVIDENCE_v0.16) |
| D20 | E19가 "MANDATORY cross-check"라 부르는 구조적 크로스체크가 `iree.compiler.ir` 미설치 시 조용히 정규식 단독으로 저하 — "mandatory"라는 표현과 실제 opportunistic 동작이 불일치 | make_contract.py(E19) | 외부 검토(REVIEW_v0_15_LATEST F3) → 몽키패치로 재현 | **조치 완료(E21)**: 패키지 미설치도 기본 하드 실패로 변경(`--allow-missing-structural-checker`로만 우회). 검증 결과 이 동작 자체는 E19 세션이 이미 문서화·시험했던 의도적 설계였음이 확인됨("은폐"라는 리뷰의 프레이밍은 과장, severity P1로 재평가) — 다만 "mandatory" 표현과의 정책적 불일치는 타당해 기본값을 변경. F9(재현성)와 상충하는 트레이드오프가 있어 E22에서 이어 다룸 |
| D19 | `make_contract.py`의 ABI 반사(`iree.abi.declaration`) 검사가 "있는데 다름"은 하드 실패, "아예 없음"은 note만 남기는 비대칭 구조 — 컴파일러 자신의 반사 크로스체크가 통째로 생략된 채 소스 시그니처만 신뢰 | make_contract.py | 외부 검토(REVIEW_v0_15_LATEST F2) → conv2d layout IR에서 abi.declaration 절만 제거해 실제 실행으로 재현 | **조치 완료(E21)**: `abi is None`도 `--allow-missing-abi-declaration`(신규)으로만 우회 가능한 하드 실패로 승격. 새 회귀 시험 2건, revert-confirm-fail(EVIDENCE_v0.16) |
| D18 | `make_contract.py`의 one-invocation 신호(`dump_elf_in_vmfb`/`stem_in_dump`/`layout_dispatches_in_dump`)가 3값 논리인데 거부 조건이 `is False`뿐 — `--dump-dir`가 완전히 비면 세 신호 전부 `None`이 되어 "불일치를 못 찾음"이 "동일 호출임을 확인함"과 동일하게 취급됨, 설명하는 note도 전무 | make_contract.py | 외부 검토(REVIEW_v0_15_LATEST F1) → 빈 dump-dir로 실제 실행해 재현 | **조치 완료(E21)**: `dump_files`가 비면 하드 실패(`--allow-unverified-invocation`으로만 우회). 새 회귀 시험 2건, revert-confirm-fail(EVIDENCE_v0.16) |
| D17 | E19의 구조적 크로스체크(constants(sum))가 계약이 실제 채택하는 `const_b`(packed_sum, 정렬 패딩/중복제거 포함)가 아니라 `dense_sum`(패킹 이전 per-tensor 합)과 비교됨 — 구조적 추출기의 constants 합은 항상 packed_sum을 재는데(stream.resource.alloc의 폴백 경로), 잘못된 기준과 비교해 정렬 패딩이 있는 완전히 정상적인 모델이 영구적으로 하드 거부됐을 것 | E19(`make_contract.py` 구조적 크로스체크) | 이 세션 내 적대적 다중 리뷰(4관점+반박검증) → mlp16k layout IR의 패킹 크기만 편집해 실제 재현 | **조치 완료(E20)**: 비교 기준을 `const_b`로 수정, `mlir_alloc_walk.diff_against_regex` 공유 헬퍼로 3곳(make_contract.py/CLI/시험 스위트) 중복 제거, 재현 시나리오를 고정 회귀 시험으로 등록, 수정 전 코드로 되돌려 시험이 실제로 실패함을 확인(EVIDENCE_v0.15) |
| D16 | E19의 구조적 크로스체크가 계약이 실제로 서명하는 `p`(lowering_score로 고른 값)가 아니라 `whole`(정규식 파서의 "파일 마지막 print=최다lowering" 낡은 가정에 의존하는 값, make_contract.py 자신의 docstring이 스레드 스케줄링에 좌우된다고 경고하는 바로 그 가정)과 비교됨 — 14개 보관 모델은 우연히 chosen==last여서 드러나지 않았을 뿐, print 순서가 바뀌면 완전히 정상인 모델도 하드 거부될 수 있었음 | E19(`make_contract.py` 구조적 크로스체크) | 이 세션 내 적대적 다중 리뷰(4관점 중 2관점이 독립적으로 수렴) → conv2d layout IR의 두 print 청크 순서만 교환해 실제 재현 | **조치 완료(E20)**: 비교 기준을 `p`로 수정, 재현 시나리오를 고정 회귀 시험으로 등록, 수정 전 코드로 되돌려 시험이 실제로 실패함을 확인(EVIDENCE_v0.15) |
| D15 | 스택 회계(`kernel_stack_accounted`)가 admission gate가 아니라 텔레메트리 — `accounted=false`에서도 거부 분기가 없고, 위치도 IREE 세션·입력버퍼·SB 파이프 생성 이후. 게다가 빌드 스크립트가 startup에 써넣은 `base+kernel`을 앱이 그대로 되읽어 `>=`로 비교하므로 구조상 항상 참(항등식). base 262,144 B는 "E12부터 써온" 관례 상수이며 저장소 어디에도 실측 스택 사용량 근거 없음 | E14-S1(§4.1·§5, cFS `AI_LEARNER`) | 외부 검토 2건(REVIEW R4 / OPINION §8.4) → 코드 대조로 확인 | EVIDENCE_v0.9 §11.5(§11.3 합격기준 #8 재판정)로 표현 축소. **조치 완료(E16)**: 스택 확인을 Init() 최선두(자원 획득 전)로 이동하고 `accounted=false`에서 실제 거부하는 분기 신설(EID_STACK_REJECT), x86-64 native_std 실기동으로 검증(EVIDENCE_v0.11 §3). base 262,144 B가 여전히 관례 상수라는 한계는 남음 |
| D14 | one-invocation 검증(D10 수정분)이 layout IR을 검사에 결합하지 않음 — bound를 만드는 모든 수치가 layout IR에서 나오는데 `single_invocation` 판정식(`make_contract.py:389`)에 layout IR 변수가 없음. `layout_ir_sha256`은 기록만 되고 무엇과도 대조 안 됨 → 다른 컴파일 호출의 layout IR + 올바른 vmfb/dump-dir 조합이 검사를 통과할 수 있음 | E14-S1 계약 도구화 | 외부 검토 2건(REVIEW R5 / OPINION §8.5) → 코드 대조로 확인 | EVIDENCE_v0.9 §11.7로 표현 축소("대표적 산출물 혼입 탐지"); **조치 완료(E15)**: layout IR이 참조하는 dispatch 심볼이 `--dump-dir`에 실제 파일로 존재하는지 검사, 위반 시 hard fail(EVIDENCE_v0.10 §2.3, §3) |
| D13 | 계약 생성기 체인이 fail-open — (a) `static_mem_bound.py`의 정규식 파서가 한 줄(`[^\n]*`) 한정이라 미인식 자원 할당 연산이 `unresolved`가 아니라 조용히 무시됨(줄바꿈만 넣어 재현: outputs/transient_slabs가 사라져도 `unresolved=[]` 유지), (b) `gen_contract_header.py`가 `bounded_bytes`의 부호를 검사하지 않아 음수가 `CONTRACT_BOUND_KNOWN=1`로 헤더에 실리고 C 게이트(`bounded<=budget`)가 무조건 ADMIT, (c) `bound_method`가 `"NONE"` 하나만 막는 블랙리스트(소문자 `none` 등 통과) | 계약 도구 전반(`static_mem_bound.py`, `make_contract.py`, `gen_contract_header.py`) | 외부 검토 2건(REVIEW R2·R3 / OPINION §8.2·§8.3) → 직접 재현으로 확인 | EVIDENCE_v0.9 §11.9로 기록; 보관된 14개 계약 값 자체는 vmfb 재대조로 14/14 일치(반증 아님). **조치 완료(E15)**: 파서에 줄바꿈 안전 정규식 + 미인식 op 화이트리스트 승격, 헤더 생성기에 bound_method 화이트리스트·비음수 검사·입출력 개수/dtype 검사 추가, 음성 시험 51/51 PASS, 14/14 회귀 없음(EVIDENCE_v0.10) |
| D12 | `static_mem_bound.py:239`의 독립 실행 경로에서 `all_static = len(unresolved)==0`이 `entry_found`를 누락 — entry 함수를 못 찾아 IR 전체를 파싱한 경우에도 `bound_method=static_from_stream_layout`을 출력, `admission_check.py:57`이 이를 받아 ADMIT 가능 | `static_mem_bound.py` 단독 실행 경로(`make_contract.py`의 `all_static`은 `entry_found`를 포함해 영향 없음) | 외부 검토 코드 대조 중 추가 발견 | EVIDENCE_v0.9 §11.9; **조치 완료(E15)**: `all_static = entry_found and not unresolved`로 수정, 단위 시험으로 확인(EVIDENCE_v0.10 §3) |
| D11 | EVIDENCE_v0.9 §4.1·§9와 CLAUDE.md가 A5b(계약 해시가 손상 파일을 가리키는 경우, `runtime_load_failed` 경로)를 "native 레벨에서 확인했다"고 서술했으나 실제로는 native·cFS 어느 레벨에서도 실행되지 않음(§3 표에는 A5a만 존재, 문서 자체가 자기모순) | E14-S1 문서화(§4.1·§9) | 외부 검토 2건(REVIEW R1 / OPINION §8.1) → native/cfs 로그·summary.json 전수 대조로 확인 | EVIDENCE_v0.9 §11.1로 정정(서술 철회), CLAUDE.md 갱신. **실제 재현 완료(E17)**: FlatBuffer root-uoffset 구조 손상으로 native x86-64·cFS x86-64·cFS AArch64 게스트 3레벨 전부에서 실행 — admission ADMIT/binding MATCH/runtime_load_failed로 안전 거부, 크래시 0(EVIDENCE_v0.12 §2) |
| D10 | 계약 생성기의 `provenance.single_invocation`이 하드코딩 `true` — one-invocation 규칙을 검증해야 할 도구가 실제로는 검증을 안 해 서로 다른 컴파일 호출의 산출물을 섞어도 통과시킴 | E14-S1 계약 도구화 | 적대적 리뷰(워크플로우) | dump-dir 실행파일이 vmfb에 임베디드돼있는지(sha256) + dump-dir 파일명에 입력 basename이 포함되는지, 두 신호로 실제 검증 추가 |
| D9 | v0.7/v0.8이 x86-64·AArch64 스택 프레임에 서로 다른 정의(x86=`sub`만, AArch64=callee-save만)를 적용 — "AArch64만 프레임이 있다"는 서술이 정의 불일치의 인공물이었음 | E14-S1(§5, `elf_stack_frame.py`) | 두 ISA에 같은 정의 적용해 재분석 | 통일된 정의(callee-save+지역=frame_bytes, +복귀주소=invocation_stack_bytes)로 EVIDENCE_v0.9 §5.1 정정 |
| D7 | 호출당 할당 = 초기화 비용 포함 상각값 | 검토 v0.6 §5 | 지표 의미 | 정상 상태 카운터 차이(65,544) |
| D6 | 계약이 아티팩트를 식별하지 않음 | 검토 v0.6 §3 | gate 보장 조건부 | sha256·크기 결합, 헤더 생성 |
| D5 | 전체 HAL 피크를 per-call 계약과 비교 | 검토 v0.6 §4 | summary 누락 | bounded 비교 |
| D4 | 세션 해제 전 모듈 데이터 free → 해제 시 segfault | E11 하네스 종료 | 측정값 무영향 | 해제 순서 수정 |
| D3 | 상한 = slice 합 → 정렬 패딩 무시로 과소 추정 (E9-A 48<128, E9-B 84<128) | v0.3–v0.4 상한 방법 | 검토 §7 예측 → E9 실증 | post-layout transient alloca 크기로 교체; MLP 60/60 재검증 |
| D2 | 정적 상한 파서가 initializer 내 상수 임포트를 입력으로 오귀속; 런타임 컨텍스트를 721 KB 과대 산정 | E6b 해석, contract 예시 | 외부 검토 §5·§6 + E6c | 엔트리 함수 스코프 파싱, 상수 별도 집계, 정오표 |
| D1 | 가중치를 호출 인자로 전달 → 매 호출 전체 가중치 디바이스 임포트 | E1–E5 | E6 HAL 통계 `bytes_per_call == 전체 입력 크기` | E6b 재측정; 이후 모든 하네스는 `bytes_per_call` 기록 의무 |

| "slice 합은 보수적 상한" | EVIDENCE v0.3 §1.1 / v0.4 §0 | E9: 정렬 패딩으로 과소 | v0.5 |
| "cFS 배치 전 admission 수행" | REPORT v0.4 | cFS 통합 미완; 검토 §8 문장으로 교체 | v0.5 |
